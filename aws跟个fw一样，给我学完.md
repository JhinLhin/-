# IAM: Identity Access & Management (IAM)

这一章 CCP 主要考你两件事：

1. **IAM 的三件套：Users / Groups / Roles / Policies 怎么配**
2. **安全最佳实践：MFA、最小权限、不要把 Access Key 写进代码**

```json
AWS Account（账户ID唯一）
 ├── Root User（根用户，只有1个）
 ├── IAM Users（可以创建很多个）
 ├── IAM Groups（用户组）
 └── IAM Roles（角色）
```

------

## 1) 一句话定义（必背）

- **IAM**：控制“谁（identity）能对哪些 AWS 资源做什么（permissions）”。
- **User**：真人账号（Console 密码 + 可选 Access Key）。
- **Group**：用户集合，用来批量分配权限。
- **Role**：**临时权限身份**，通常给 **AWS 服务/应用**去“扮演”，避免长期密钥。
- **Policy**：权限规则（JSON），定义 Allow/Deny、Action、Resource。

------

## 2) IAM 权限模型（考试最爱）

### 2.1 Policy 是怎么决定你能不能做事的？

- IAM 会把你身上的所有 policy 合起来算：
  - 直接挂在 **User** 上
  - 来自 **Group** 的
  - 通过 **Assume Role** 得到的
- **记住一个铁律（CCP 常考）：显式 Deny > Allow**
  （只要任何一条明确 Deny，你就被拒绝）

### 2.2 Policy 三种（认识即可）

- **AWS-managed**：AWS 给的通用权限包
- **Customer-managed**：你自己可复用维护的
- **Inline**：只绑在某一个 user/group/role 上（不可复用）

------

## 3) IAM Policy 结构（要能看懂题目）

必记字段：

- **Effect**：Allow / Deny
- **Action**：允许做哪些 API（如 `s3:ListBucket`）
- **Resource**：作用到哪个资源 ARN（如某个 bucket）
- **Statement**：可多个

> CCP 不会让你手写复杂 JSON，但会让你识别 “Action/Resource/Allow”。

------

## 4) Users vs Groups vs Roles（最高频对比）

| 概念  | 给谁用         | 关键点                          | 题干关键词                                   |
| ----- | -------------- | ------------------------------- | -------------------------------------------- |
| User  | 真实人         | 长期身份                        | “employee / person”                          |
| Group | 一群人         | 权限批量管理                    | “dev team / QA team”                         |
| Role  | 服务/应用/临时 | **临时凭证，不用存 access key** | “EC2 needs access S3” “Lambda call DynamoDB” |

✅ **超高频秒选**：

- “让 EC2 访问 S3，别在机器上放 access keys” → **IAM Role（Instance Profile）**
- “Lambda 需要访问其他服务” → **Lambda Execution Role**

------

## 5) MFA（CCP 必考）

### 一句话

> MFA = 密码之外再加一层（你有的设备生成一次性码）

- 强烈建议：**Root account 必开 MFA**
- 价值：密码泄露也不容易被盗号

### MFA 设备（认识即可）

- **Virtual MFA**（手机 App：Google Authenticator/Authy）
- **Hardware MFA**（实体 token）
- **U2F 安全钥匙**（USB Key）

------

## 6) Password Policy（常考“安全控制”）

能设定：

- 最小长度
- 复杂度（大小写/数字/特殊字符）
- 禁止重复
- 过期轮换
- 配合强制 MFA（题目若问最佳实践 → MFA + 强密码 + 轮换）

------

## 7) IAM 安全审计工具（会选就行）

- **Credential Report**：全账号所有 IAM 用户的凭证状态（是否有密码/Access Key、是否启用等）
- **Access Advisor**：某用户/角色“哪些服务权限最近用过”（帮你删多余权限）
- **Policy Simulator**：模拟这套 policy 到底 allow/deny 什么（上线前验证）

------

## 8) Shared Responsibility（IAM 版）

- **AWS 负责**：IAM 服务本身的可用性与底层安全、提供托管策略等
- **你负责**：用户/组/角色/策略配置是否正确、最小权限、密钥保护、MFA、定期审计

------

## 9) 用户访问 AWS 的方式（CCP 常考“哪种工具”）

| 方式           | 用途                         | 关键词                   |
| -------------- | ---------------------------- | ------------------------ |
| **Console**    | 网页操作                     | “UI / beginners”         |
| **CLI**        | 命令行自动化，直接调 AWS API | “scripts / automation”   |
| **SDK**        | 程序里调用 AWS（boto3 等）   | “in code / application”  |
| CloudFormation | IaC（用模板建资源）          | “infrastructure as code” |

### CLI vs SDK（秒选）

- “终端里敲命令/写脚本” → **CLI**
- “用 Python/Java 在应用里调用 S3” → **SDK**

------

## 10) 本章 10 分钟速记卡（直接背）

- IAM = 控制访问：谁能对哪些资源做什么
- Users（人）/ Groups（批量权限）/ Roles（服务临时身份）/ Policies（JSON 权限）
- **显式 Deny > Allow**
- 角色给 EC2/Lambda 用，避免把 access key 写进代码
- MFA：Root 必开
- 审计：Credential Report / Access Advisor / Policy Simulator
- 访问方式：Console / CLI / SDK / CloudFormation
- Shared Responsibility：AWS 管 IAM 服务；你管权限配置与密钥安全

# EC2：Virtual Machines

## 1) 本章你必须会的“一句话定义”

- **EC2**：AWS 上的“云端虚拟机”（Virtual Machine），你租 CPU/RAM/网络/磁盘来跑程序。
- **AMI**：EC2 的“系统镜像”（OS + 预装软件/配置模板）。
- **Security Group（SG）**：挂在 EC2 上的**虚拟防火墙**，控制**进/出**流量。
- **User Data**：EC2 第一次开机时自动执行的初始化脚本（bootstrap）。

------

## 2) 考试必背关键词清单（背这个就够）

- EC2 = VM；AMI；Instance Type；EBS vs Instance Store
- **User Data：只在 first boot 跑一次**（默认）
- **Security Group：stateful（有状态）**；默认 **inbound 全拒**、**outbound 全放**
- 常见端口：**22 SSH / 80 HTTP / 443 HTTPS / 3389 RDP**
- 购买模式：**On-Demand / Reserved Instances / Savings Plans / Spot / Dedicated Host / Dedicated Instance / Capacity Reservation**
- **Shared Responsibility（EC2）**：AWS 负责云底层；你负责 OS/应用/配置/数据

------

## 3) EC2 配置维度（CCP 常考“组成题”）

你看到题目问“启动一台 EC2 需要什么/由什么构成”，直接背这句：

> **EC2 Instance = AMI（OS） + Instance Type（CPU/RAM） + Storage（EBS/Instance Store） + Network（VPC/Subnet） + Security Group +（可选）User Data**

再加两块常见能力：

- **ELB**：把流量分给多台 EC2（提高可用性）
- **Auto Scaling**：自动加/减 EC2 数量（应对流量波动）

------

## 4) Instance Types（只按“考试会怎么问”记）

### 4.1 四大类 = 看瓶颈选资源

- **General Purpose（通用）**：均衡（Web/小型DB/开发测试）
- **Compute Optimized（计算）**：CPU 强（批处理/高性能Web/分析/ML inference）
- **Memory Optimized（内存）**：RAM 大（内存数据库/缓存/实时分析）
- **Storage Optimized（存储）**：高 IOPS/本地盘（NoSQL/数据仓库/大数据）

### 4.2 考试快速选型口诀

- **CPU 不够 → Compute**
- **内存不够 → Memory**
- **磁盘吞吐/IO 不够 → Storage**
- **都差不多 → General**

> CCP 不会让你背具体型号参数（m5/c5/r5/i3），主要考“哪类适合哪种场景”。

------

## 5) EBS vs Instance Store（高频易混淆）

- **EBS（Elastic Block Store）**：**持久化**块存储（像硬盘），实例停了数据还在（取决于设置），常用且安全。
- **Instance Store**：**临时**本地盘，**实例停止/终止就可能丢数据**（适合缓存/临时文件/中间结果）。

**秒选题干关键词**

- “需要持久化/不丢数据”→ **EBS**
- “临时缓存/可丢/极高本地IO”→ **Instance Store**

------

## 6) User Data（CCP 最爱考的细节）

你要记住 3 点：

1. **在 Launch 时提供**
2. **只在首次启动（first boot）执行一次（默认）**
3. 用于 **bootstrap**：装软件、拉配置、启动服务
   - 例如安装 Apache、从 S3 下载配置、启动应用等

**常见陷阱**

- 不是每次 reboot 都跑（题目如果暗示“每次启动都跑”要小心）

------

## 7) Security Groups（必考核心：规则 + 默认行为）

### 7.1 SG 的关键事实（考试直接问）

- SG 作用在 **实例级别（instance-level）**
- **默认**：inbound 全拒；outbound 全放
- 规则可随时改，立即生效
- 一个实例可以挂多个 SG
- 规则按：协议（TCP/UDP/ICMP）+ 端口 + source/destination（IP/CIDR 或其他 SG）

> CCP 常考“哪个组件像防火墙？”→ **Security Group**

### 7.2 Classic Ports（见到就秒选）

- **22** SSH（远程登录 Linux）
- **3389** RDP（远程桌面 Windows）
- **80** HTTP
- **443** HTTPS
- （20/21 FTP：了解即可）

### 7.3 最小权限（best practice）

- 只开必要端口
- SSH 不要对 0.0.0.0/0 全开（题目问安全做法 → 限制到你的 IP 段）

------

## 8) EC2 购买/计费模式（CCP 超高频：选最合适）

下面是你考试真正要会的：**看到场景选哪种**。

### 8.1 On-Demand（随用随付）

- **不承诺**、灵活、单价最高
- 适合：短期、不可预测、临时项目/测试/突发流量

### 8.2 Reserved Instances（RI：按“具体实例属性”省钱）

- 1/3 年；折扣大（可到 ~72%）
- 适合：**长期稳定**（例如长期跑的数据库/稳定业务）
- **Convertible RI**：更灵活（可换类型/家族/OS 等），折扣略低

### 8.3 Savings Plans（按“使用额度”承诺省钱）

- 承诺每小时花多少钱（例如 $10/hour，1/3年）
- 相比 RI：更“按用量承诺”，通常更灵活（尤其在实例大小/OS/tenancy 等维度）
- 适合：长期稳定但可能会换实例规格的团队

> **CCP 快速对比记忆**：
> RI = “锁定某些实例属性省钱”；Savings Plan = “锁定花费额度省钱”。

### 8.4 Spot（最便宜但会被中断）

- 折扣最高（可到 ~90%），但**随时可能被回收**
- 适合：可中断/可重试：批处理、离线分析、渲染、分布式任务
- 不适合：关键在线服务、数据库

### 8.5 Dedicated Host vs Dedicated Instance（只要掌握差异点）

- **Dedicated Host**：整台物理机给你用，可控放置，适合**合规/许可证 BYOL（按插槽/核心计费）**，最贵
- **Dedicated Instance**：硬件只给你，但**你不能控制放置**，停止再启动可能换到新硬件

### 8.6 Capacity Reservation（锁“容量”，不锁折扣）

- 在某个 AZ **预留 On-Demand 容量**，保证你要用时一定有机器
- **没折扣**（仍按 On-Demand），即使不开机也可能要付（题目常这样考）
- 适合：必须在特定 AZ、短期但不能断供的场景

------

## 9) 一页“场景题秒选表”（考前背它）

| 题干关键词                              | 秒选答案                               |
| --------------------------------------- | -------------------------------------- |
| 短期 / 不可预测 / 临时测试              | **On-Demand**                          |
| 长期稳定（1-3 年）最省钱                | **Reserved Instances / Savings Plans** |
| 长期稳定但可能换规格                    | **Savings Plans**（更灵活）            |
| 超便宜 + 可中断/可重试                  | **Spot**                               |
| 合规 + 自带许可证 BYOL + 需要物理机控制 | **Dedicated Host**                     |
| 硬件独占但不关心物理放置                | **Dedicated Instance**                 |
| 必须保证某 AZ 有容量（不一定便宜）      | **Capacity Reservation**               |

------

## 10) Shared Responsibility（EC2 必考）

### AWS 负责

- 数据中心、物理安全、底层硬件、虚拟化层、网络基础设施等

### 你负责

- **EC2 里的 OS（打补丁）、软件安装、应用安全配置**
- **Security Group / VPC 网络配置**
- 数据与权限（IAM、加密、访问控制）

**一句话背诵版**

> AWS 负责“云的安全”，你负责“云里你跑的东西的安全”。

------

## 本章考前 10 分钟速记卡片（直接背）

- EC2 = 云 VM；AMI = 系统镜像
- Instance Type：General/Compute/Memory/Storage（按瓶颈选）
- EBS 持久；Instance Store 临时
- User Data：launch 提供；**first boot 只跑一次**；用来 bootstrap
- Security Group：实例级防火墙；默认 inbound 拒、outbound 放；常见端口 22/80/443/3389
- 购买：On-Demand 灵活；RI/Plans 长期省；Spot 最便宜但会断；Dedicated Host 合规+BYOL；Capacity Reservation 保证 AZ 容量不打折
- Shared Responsibility：AWS 管底层；你管 OS/应用/配置/数据/SG

------

# EC2 Instance Storage

## 1) 一句话把每个东西钉死（考试最常问的“你到底用哪个”）

- **EBS**：给**一台 EC2**用的**持久化块存储**（像云硬盘），**同一时间只能挂一台**，**绑定某个 AZ**。
- **EFS**：给**很多台 EC2 同时挂载**的**共享文件系统**（NFS），可跨 AZ，高可用。
- **FSx**：AWS 托管的“专业文件系统”（Windows/高性能 HPC 等），按场景选。
- **Instance Store**：EC2 的**本地临时盘**（ephemeral），**停机/故障可能丢数据**，性能很强但不持久。
- **Snapshot（EBS）**：EBS 的“时间点备份”，**增量**，可复制到其他 Region/Account。
- **AMI**：EC2 的“镜像模板”（OS + 软件 + 配置），用于快速复制一模一样的机器。
- **EC2 Image Builder**：自动化、可定时地产生/更新 AMI（更合规、更安全）。

------

## 2) 必背关键词（背这套就能做题）

- **EBS：block / persistent / one instance at a time / AZ-bound / Delete on Termination**
- **EBS Snapshot：point-in-time / incremental / stored in S3 / copy跨Region/Account / DLM自动化 / Archive tier / Recycle Bin**
- **EFS：NFS / shared / mount 100s EC2 / multi-AZ**
- **EFS-IA：不常访问更便宜（按策略自动转）**
- **FSx Windows：SMB + NTFS + Active Directory**
- **FSx Lustre：HPC/ML/超高吞吐低延迟**
- **Instance Store：ephemeral / high IOPS / lost on stop/terminate / you manage backups**
- **Shared Responsibility（存储）：AWS 负责 durability/硬件；你负责数据备份、权限、加密策略使用**

------

## 3) 一张“秒选表”：题干一出现就选谁

| 题干关键词                                            | 秒选                        |
| ----------------------------------------------------- | --------------------------- |
| “需要像硬盘一样的块存储”“数据库磁盘”“单台EC2持久化”   | **EBS**                     |
| “很多台 EC2 同时读写同一份文件”“共享目录”“NFS”        | **EFS**                     |
| “Windows 文件共享”“SMB/NTFS”“Active Directory”        | **FSx for Windows**         |
| “HPC/机器学习训练/视频处理”“极高吞吐/百万IOPS/亚毫秒” | **FSx for Lustre**          |
| “缓存/临时文件/中间结果”“高IO但能丢”                  | **Instance Store**          |
| “备份 EBS”“恢复到某个时间点”“跨Region迁移”            | **EBS Snapshot**            |
| “快速复制一模一样的 EC2 环境/预装软件”                | **AMI（或 Image Builder）** |

------

## 4) EBS（CCP 必考点）

### 4.1 EBS 本质（一定要记）

- **网络挂载的 block storage**
- **持久化**：实例终止后仍可保留数据（取决于删除策略）
- **同一时间只能挂一台 EC2**
- **绑定一个 AZ**（想跨 AZ → 通常用 snapshot/复制来做）
- 在同一 AZ 内会做复制以提升耐久性（你不需要自己管硬件）

### 4.2 Delete on Termination（超级爱考）

- **默认：Root volume（系统盘）会被删**
- **默认：额外挂载的数据盘不会被删**
- 想“实例删了但数据要留” → 关掉 root 的 delete-on-termination 或把数据放到非 root EBS

### 4.3 Snapshot（EBS 备份）

- **Point-in-time** 备份
- **Incremental**：只保存变化块（省空间/省钱）
- 存在 “S3 上”（你不用自己操作 S3，但考试会这么描述）
- **Copy**：可跨 Region、跨 Account
- 自动化：**DLM（Data Lifecycle Manager）**
- **Snapshot Archive**：更便宜（可到 75%），但**恢复要 24–72 小时**（题目问“恢复慢但便宜”→ 这条）
- **Recycle Bin**：误删快救命（设置 1 天～1 年保留）

------

## 5) EFS（共享文件系统：NFS）

- **Fully managed NFS**
- **可同时挂载到很多台 EC2（100s）**
- **跨 AZ 可访问**（高可用/高耐久）
- 典型场景：共享网站内容、共享用户上传、共享配置/日志目录（很多机要看同一份文件）

### EFS-IA（不常访问更省钱）

- **Infrequent Access**：给“冷文件”更便宜
- 可按策略把 **60 天未访问**的文件自动转到 IA（题干出现“自动转冷/很少读”→ 选 EFS-IA）

------

## 6) FS(File System)x（“我需要特定文件系统”才用它）

### FSx for Windows File Server

- **SMB + NTFS**
- **Active Directory** 集成
- 适合：Windows 应用、共享盘、企业 AD 环境

### FSx for Lustre

- 超高性能文件系统（HPC/ML/视频处理）
- 关键词：**高吞吐、百万 IOPS、sub-ms latency**
- 题干出现“HPC/训练/渲染/超高速共享存储”→ 优先想到它

------

## 7) EC2 Instance Store（临时本地盘）

- **直接挂在物理机上的临时盘**
- **性能非常好（高 I/O）**
- **不持久**：实例 stop/terminate 或硬件故障可能丢
- 适合：cache、buffer、scratch、临时中间结果
- **备份/复制你自己负责**（常见考点：不适合存关键数据）

------

## 8) Shared Responsibility（EC2 存储版）

### AWS 负责

- EBS/EFS 的底层硬件、耐久性设计、AZ 内复制
- 提供加密能力（比如 KMS 集成能力）

### 你负责

- **你的数据备份策略**（EBS snapshot、EFS replication/策略等）
- **访问控制**（IAM、SG/NACL、文件权限）
- **是否启用/如何使用加密**（敏感数据你要主动配置）

------

## 9) AMI & Image Builder（考“镜像是什么/为什么用”）

### AMI（是什么 + 为什么）

- AMI = 启动 EC2 的模板（OS + 软件 + 配置）
- 价值：**启动更快、环境一致、可重复部署**
- **Region 级**：AMI 通常“在特定 Region”，需要的话可 copy 到其他 Region
- 来源三类：
  - Public AMI（AWS 提供）
  - Your AMI（你自己做）
  - Marketplace AMI（别人做/可能收费）

### AMI 的基本流程（题目会考顺序）

1. 先有一台 EC2
2. 装软件/配环境
3. 从它 **Create AMI**
4. 用这个 AMI 再 launch 出一模一样的新 EC2

### EC2 Image Builder

- 作用：**自动**创建/更新 AMI（可定时、可合规、可持续打补丁）
- 计费：服务本身免费，付底层资源费（CCP 常见问法：不是“额外按服务收费”那种）

------

## 本章 10 分钟速记卡片（考前背）

- **EBS**：持久化 block，**单实例**，**绑定 AZ**；root 默认删、数据盘默认不删
- **Snapshot**：时间点、**增量**、可跨 Region/Account、DLM 自动化；Archive 便宜但恢复慢；Recycle Bin 防误删
- **EFS**：NFS 共享文件系统，可挂 100s EC2，跨 AZ；**EFS-IA** 冷数据省钱
- **FSx Windows**：SMB/NTFS/AD；**FSx Lustre**：HPC/ML 超高性能
- **Instance Store**：本地临时盘，高 IO 但会丢；关键数据别放
- **AMI**：机器模板；**Image Builder**：自动做/更新 AMI
- Shared Responsibility：AWS 管底层耐久；你管备份/权限/加密使用

------

# Elastic Load Balancing & Auto Scaling Groups

## 1) 一句话把核心概念钉死

- **Scalability（可扩展）**：负载变大/变小，系统能“撑得住/缩得回去”。
- **High Availability（高可用）**：某个机器/AZ 挂了，系统还能继续服务（靠冗余+故障切换）。
- **Vertical Scaling（纵向）**：换更大的机器（scale up/down）。
- **Horizontal Scaling（横向）**：加更多机器（scale out/in）。
- **Elasticity（弹性）**：横向扩缩 **自动化**（按负载自动加减）。
- **Agility（敏捷）**：资源/应用能快速部署、快速变更。

------

## 2) 必背关键词（背完就能做题）

- Vertical vs Horizontal
- High Availability = **Multi-AZ**
- **ELB**：分流 + 健康检查 +（常见）SSL 终止
- **ASG**：min / desired / max；自动替换不健康实例；自动扩缩；可 Multi-AZ
- **ALB（L7 HTTP/HTTPS）** vs **NLB（L4 TCP 高性能）** vs **CLB（老）**
- Scaling 策略：Manual / Dynamic（Step + Target Tracking）/ Scheduled / Predictive

------

## 3) 超高频“秒选表”（题干一出现就选）

| 题干关键词                                 | 秒选                                        |
| ------------------------------------------ | ------------------------------------------- |
| “某台服务器过载，需要分摊流量”             | **ELB**                                     |
| “需要健康检查 + 自动把坏实例踢掉/不发流量” | **ELB（health checks）**                    |
| “流量波动大，自动加/减 EC2”                | **ASG（Elasticity）**                       |
| “必须跨可用区容灾”                         | **Multi-AZ（ELB + ASG 跨 AZ）**             |
| “HTTP/HTTPS、按URL/应用层路由”             | **ALB（Layer 7）**                          |
| “超高吞吐、低延迟、TCP/高性能”             | **NLB（Layer 4）**                          |
| “固定时间点扩容（比如周五5点）”            | **Scheduled Scaling**                       |
| “让CPU稳定在40%”                           | **Target Tracking**                         |
| “CPU>70%加2台、CPU<30%减1台”               | **Step/Simple Scaling（CloudWatch alarm）** |
| “预测未来流量提前扩容”                     | **Predictive Scaling**                      |

------

## 4) Scalability vs High Availability（最爱考的概念题）

### 4.1 纵向 vs 横向

- **Vertical（纵向）**：一台机器变强
  - 优点：简单
  - 缺点：有上限、单点风险仍在
  - 常见：数据库（题目经常这样暗示）
- **Horizontal（横向）**：更多机器一起扛
  - 优点：更抗故障、可无限扩展（理论上）
  - 典型组合：**ELB + ASG**

### 4.2 High Availability（高可用）怎么做？

- **核心 = Multi-AZ**
- 一般和横向一起出现：多 AZ 放多台实例，某个 AZ 挂了还能服务。

------

## 5) ELB（Elastic Load Balancing）必考点

### 5.1 ELB 是什么（考试表述）

- **托管的负载均衡服务**：把流量分给多个目标（EC2/容器/IP）
- AWS 负责：可用性、维护、升级（你只做少量配置）

### 5.2 为什么用 ELB（题目会怎么问）

- **分流**：避免单点过载
- **容错**：某台坏了自动绕开
- **健康检查**：只把流量发给健康目标
- **SSL Termination**：HTTPS 在 LB 结束加密（后端可用 HTTP）

### 5.3 三种 ELB（CCP 记到这个程度就够）

- **ALB（Application Load Balancer）**：**HTTP/HTTPS，L7**（应用层）
- **NLB（Network Load Balancer）**：**TCP，L4**（高性能/低延迟）
- **CLB（Classic）**：旧的（L4/L7），考试通常当“legacy/older”理解即可

**秒杀口诀**

- “网站/HTTP/HTTPS”→ **ALB**
- “TCP/超高性能”→ **NLB**

------

## 6) ASG（Auto Scaling Group）必考点

### 6.1 ASG 的目标（题目常直接问）

- 保证合适数量的 EC2 在跑：**min / desired / max**
- **Scale out/in**：负载高加机器，负载低减机器（省钱）
- **Replace unhealthy**：自动替换不健康实例
- 可跨 **Multi-AZ** 做高可用
- 可与 **ELB 集成**：新实例自动注册到 LB

### 6.2 Scaling 策略（按题干关键词选）

- **Manual**：手动调容量（很少考但要认识）
- **Dynamic**
  - **Step/Simple**：CloudWatch 告警触发（CPU>70% +2）
  - **Target Tracking**：保持某指标稳定（CPU≈40%）
- **Scheduled**：固定时间扩缩（规律性业务）
- **Predictive**：机器学习预测未来需求（提前扩）

------

## 7) ELB + ASG 经典架构（CCP 常考“最佳实践”）

> **Multi-AZ 的 ELB 前面接流量，后面 ASG 跨多个 AZ 管一组 EC2**
> 效果：

- **高可用**：某个 AZ/实例坏了还能扛
- **弹性**：流量高自动加实例，低自动减实例
- **成本优化**：只跑需要的容量

------

## 本章 10 分钟速记卡片（直接背）

- Scalability：能扩/能缩；High Availability：故障也能服务（**Multi-AZ**）
- Vertical：换大机；Horizontal：加机器
- Elasticity：自动横向扩缩；Agility：快速部署变更
- **ELB**：分流 + 健康检查 + SSL 终止；ALB(L7 HTTP/HTTPS) / NLB(L4 TCP) / CLB(旧)
- **ASG**：min/desired/max；自动扩缩 + 替换坏实例；可跨 AZ；可自动注册到 ELB
- Scaling：Step（告警+加减）/ Target Tracking（保持指标）/ Scheduled（定时）/ Predictive（预测）

------

# Amazon S3

这章在 CCP 的考法非常固定：

> **S3 是对象存储（Object Storage）**，核心考 **Bucket/Object/安全/版本/复制/存储层级/静态网站**，外加 **Snow Family + Storage Gateway（迁移与混合云）**。

------

## 1) 一句话定义（必背）

- **S3**：AWS 的**对象存储**（Object Storage），把文件作为 **Object** 存在 **Bucket** 里。
- **Bucket**：装对象的容器；**名字全局唯一**；创建在某个 **Region**。
- **Object**：文件本体 + key（路径名）+ metadata/tags +（可选）version id。
- **Key**：对象的“完整路径名”（S3 没真目录，只有 key 里有 `/`）。

------

## 2) 必背关键词清单（考试覆盖）

- Buckets：**global unique name**、**region-level**
- Objects：**key=full path**、最大 **5TB**、>**5GB** 用 **multipart upload**
- Security：**IAM policy（user-based）** + **Bucket policy（resource-based）** +（少见）ACL
  **explicit DENY wins**
- Block Public Access：默认应开（防数据泄露）
- Static website hosting：403 → bucket policy 没允许 public read
- Versioning：bucket-level；旧对象可能有 **null version**；suspend 不会删旧版本
- Access logs：记录所有访问（允许/拒绝都记）
- Replication：**CRR/SRR**，**必须 source+dest 都开启 versioning**，异步
- Storage classes：Standard / Standard-IA / One Zone-IA / Intelligent-Tiering / Glacier Instant / Glacier Flexible / Deep Archive
- Durability：**11 9s（99.999999999%）**（所有 class 都非常耐久）
  Availability：随 class 不同
- Object Lock / Glacier Vault Lock：**WORM**
- Snow family：网络传 >1 周 → 用 Snow；Snowcone/ Snowball / Snowmobile
- Storage Gateway：把 on-prem 和 S3 桥接（混合云存储）

------

## 3) 超高频“秒选表”（看到题干就选）

| 题干关键词                          | 秒选                                                         |
| ----------------------------------- | ------------------------------------------------------------ |
| “存文件/备份/静态资源/数据湖”       | **S3**                                                       |
| “对象最大 5TB / 大文件上传”         | **Multipart upload（>5GB）**                                 |
| “禁止桶变公网”                      | **Block Public Access 开启**                                 |
| “跨账号访问桶”                      | **Bucket Policy（resource-based）**                          |
| “谁能访问 S3？”                     | **IAM policy + Bucket policy，且显式 Deny 优先**             |
| “静态网站托管/网站 URL”             | **S3 Static Website Hosting**                                |
| “403 Forbidden 访问网站”            | **Bucket policy 没 public read**（或 Block Public Access 阻挡） |
| “防误删/可回滚”                     | **Versioning**                                               |
| “跨区域复制容灾/合规/低延迟”        | **CRR**                                                      |
| “同区域复制（日志聚合/多账号环境）” | **SRR**                                                      |
| “很少访问但要快速取回”              | **Standard-IA / One Zone-IA**                                |
| “自动分层省心”                      | **Intelligent-Tiering**                                      |
| “归档/冷数据/取回慢且便宜”          | **Glacier / Deep Archive**                                   |
| “写一次读多次，合规不可删改”        | **S3 Object Lock / Glacier Vault Lock（WORM）**              |
| “数据太大，网络传超过一周”          | **AWS Snow Family**                                          |
| “on-prem 也想像本地盘一样用 S3”     | **Storage Gateway**                                          |

------

## 4) S3 基础：Buckets & Objects（CCP 经典概念题）

### 4.1 Bucket（桶）

- **名字全局唯一**（所有账号/所有 Region）
- **创建在某个 Region**（S3 看起来像全局服务，但 bucket 是 region-level）
- 命名规则（了解级）：小写、无下划线、3-63、不能像 IP

### 4.2 Object（对象）

- **Key = 完整路径名**：`s3://bucket/folder1/folder2/file`
- S3 没目录，只有 key 里带 `/`（控制台“假装有文件夹”）
- **最大对象 5TB**
- **>5GB 必须 multipart upload**
- 可有：metadata、tags（最多 10）、version id（若开 versioning）

------

## 5) S3 Security（CCP 高频）

### 5.1 两种授权模型（必背）

- **User-based**：IAM Policy（给 user/group/role）
- **Resource-based**：Bucket Policy（最常考），ACL（较少考）

✅ 判断能不能访问（考试原句常出现）：

> **允许 =（IAM 允许 或 Bucket policy 允许）并且没有显式 DENY**

### 5.2 Block Public Access（防数据泄露必考点）

- 这套开关就是为了防公司数据“不小心公开”
- 如果 bucket 不该公网访问，**保持开启**
- 可在 account level 统一设置

------

### 6) S3 Static Website Hosting（必考小坑）

- S3 能托管**静态网站**
- 典型考点：**403 Forbidden**
  - 不是“网站坏了”，而是 **bucket policy 没允许 public read**（或 Block Public Access 阻挡）

------

### 7) Versioning（非常爱考）

- **bucket-level 开启**
- 同 key 覆盖会产生新版本
- 开启前已有对象：版本号可能是 **null**
- **Suspend versioning 不会删除旧版本**
- 价值：防误删、易回滚

------

### 8) Access Logs（审计）

- 记录对 bucket 的所有请求（包括被拒绝的）
- 输出到另一个 S3 bucket
- 用于排障/审计/安全分析

------

### 9) Replication（CRR & SRR）

**必背前提：source 与 destination 都要开启 Versioning**

- **CRR（Cross-Region）**：合规/容灾/跨区域低延迟/跨账号
- **SRR（Same-Region）**：日志聚合、生产/测试账号同步等
- **异步复制**，需配置 IAM 权限

------

## 10) Storage Classes（最常考“选哪个最省钱还满足需求”）

### 10.1 Durability vs Availability（CCP 喜欢问差别）

- **Durability（耐久）**：数据不丢的概率 → S3 给你 **11 个 9**（所有 class 都很高）
- **Availability（可用）**：一年里服务可用多久 → 不同 class 不同（Standard 通常最高）

### 10.2 记忆法：按访问频率 + 取回速度 + 成本

- **S3 Standard**：常访问，低延迟高吞吐
- **Standard-IA**：少访问但要快速取回（备份/DR）
- **One Zone-IA**：更便宜但只在单 AZ（AZ 挂了会丢）→ 适合可重建/第二份备份
- **Glacier Instant Retrieval**：归档但要毫秒取回（偶尔拿一次）
- **Glacier Flexible Retrieval**：取回分钟~小时（更便宜）
- **Deep Archive**：最便宜，取回最慢（小时级~更久），长期保存
- **Intelligent-Tiering**：自动分层（付少量监控费），适合访问模式不确定；**无 retrieval fee（按你的笔记）**

✅ 秒选口诀：

- “经常访问”→ Standard
- “很少访问但要立刻取”→ Standard-IA / Glacier Instant
- “极少访问、能等、超便宜”→ Glacier Flexible / Deep Archive
- “访问模式不确定”→ Intelligent-Tiering
- “单 AZ 更便宜、可丢/可重建”→ One Zone-IA

------

## 11) Object Lock & Glacier Vault Lock（合规：WORM）

- **S3 Object Lock**：对象版本在指定时间内**不能删/不能改**（WORM）
- **Glacier Vault Lock**：把 policy 锁死，之后不能随便改（合规保留）

------

## 12) Snow Family（离线迁移 + 边缘计算）

核心考试规则：

> **如果网络传输要超过 1 周 → 考虑 Snow 家族**

- **Snowcone**：最小最便携（~8TB），可边缘计算/传输
- **Snowball Edge**：TB~PB 级迁移
- **Snowmobile**：超大规模（PB~EB），>10PB 更适合

**流程（题目可能问顺序）**

1. 下单设备
2. 安装客户端/OpsHub
3. 拷数据到设备
4. 寄回 AWS
5. 数据进 S3
6. 设备擦除

------

## 13) Hybrid Storage：Storage Gateway（混合云）

> Storage Gateway = on-prem 与 S3 的桥梁
> 用于：备份/恢复/灾备/分层存储
> （类型不要求你记细）

------

## 本章 10 分钟速记卡（背完就能做题）

- S3 = 对象存储；Bucket 全局唯一且在 Region；Object key=完整路径；最大 5TB，>5GB 用 multipart
- 安全：IAM policy + Bucket policy；显式 Deny 优先；Block Public Access 防泄露
- 静态网站：403=没有 public read（或被 Block Public Access 挡）
- Versioning：防误删/回滚；null version；suspend 不删旧版本
- Replication：CRR/SRR 必须两边开 versioning，异步
- Storage class：Standard（热）/ IA（温）/ One Zone-IA（更便宜但单AZ）/ Intelligent（自动）/ Glacier（冷）/ Deep Archive（最冷最慢）
- WORM：Object Lock / Vault Lock
- 数据太大网络传>1周：Snow；混合云：Storage Gateway

------

# Other Compute

这一章本质在考你：

> **什么时候用 EC2？什么时候用容器？什么时候用 Lambda？什么时候用 Batch？**

只要你能做到“场景 → 秒选服务”，这一章就稳了。

------

## 1️⃣ Docker（考试考概念，不考细节）

### 一句话定义

> Docker = 把应用打包成“容器”，在哪都能跑，行为一致。
>
> 容器（Container）是镜像（Image）的运行实例，二者是静态与动态的关系。镜像是一个包含应用及其依赖的只读模板，好比“类”或“设计图”；而容器是基于镜像创建的、可读写的进程，好比“对象”或“实体”。一个镜像可启动多个相互隔离的容器。 
>
> 更准确地说：
>
> - **容器本质上就是一个正在运行的进程（或一组进程）**
> - 这个进程通常就是你要运行的应用
> - 当这个“主进程”结束时，容器就会停止
>
> **❓容器里应用是否在运行？**
>
> ✅ **是的。容器存在的前提就是有应用进程在运行。**
>
> 容器不是虚拟机，不是“开着一个系统等你进去”
>  它本质上就是一个隔离环境里的应用进程。
>
> **🔎 更精确地理解**
>
> 在 Docker 里：
>
> - 镜像（Image） = 静态文件系统 + 启动指令
> - 容器（Container） = 运行中的进程 + 独立的文件系统 + 隔离环境
>
> 当你执行：
>
> ```
> docker run nginx
> ```
>
> Docker 会：
>
> 1. 基于 nginx 镜像创建容器
> 2. 启动 nginx 进程
> 3. nginx 进程在运行 → 容器就是“运行状态”
> 4. 如果 nginx 进程退出 → 容器就停止
>
> **🧊 一句话核心区别**
>
> > **镜像是模板（只读）**
> >  **停止的容器是曾经运行过的实例（带运行痕迹，可读写层还在）**
>
> ------
>
> **🏗 用“类 vs 对象”类比**
>
> | 概念         | 类比                  | 是否包含运行痕迹 |
> | ------------ | --------------------- | ---------------- |
> | 镜像         | 类 / 安装包 / 设计图  | ❌ 没有           |
> | 运行中的容器 | 对象 / 正在运行的程序 | ✅ 有             |
> | 停止的容器   | 已经关闭的程序实例    | ✅ 有             |
>
> 停止的容器 ≠ 镜像
>  它只是“关机了”，但“这个实例还存在”。

核心特征（必背）

- 容器轻量（共享宿主 OS 内核）
- 启动快（秒级）
- 可移植（环境一致）
- 易扩缩

### Docker vs VM（高频概念题）

| Docker        | VM              |
| ------------- | --------------- |
| 轻量，共享 OS | 重量级，完整 OS |
| 秒级启动      | 分钟级启动      |
| 适合微服务    | 适合多 OS 场景  |

🧠 记忆点：
Docker 是“应用级隔离”，VM 是“操作系统级隔离”。

------

## 2️⃣ ECR（容器镜像仓库）

> ECR = AWS 私有 Docker 镜像仓库

- 存 Docker images
- 集成 ECS / EKS / Fargate
- 类似 Docker Hub（但私有 + AWS 集成）

🧠 题干出现：

- “Store container images securely”
- “Private container registry”

👉 选 **ECR**

------

## 3️⃣ ECS & Fargate（容器运行方式）

### ECS（Elastic Container Service）

> ECS = AWS 的容器编排服务
>
> **1. 最内层：Container (容器) —— “干活的员工”**
>
> 这是最小的实际工作单元。
>
> - **技术定义：** 就是你的 Docker 容器。你的代码（比如一个 Python Flask 应用，或者一个 Nginx 服务器）就跑在这里面。
>
> - **特点：** 它通常是从 Docker Image（镜像）启动的。
>
> - **餐厅类比：**
>
>   > **Container = 厨师**。 他是真正切菜、炒菜（运行代码）的人。
>
> **2. 中间层：Task (任务) —— “工位/小组”**
>
> 这是 ECS 调度的**原子单位**（Atomic Unit）。这是初学者最容易混淆的地方。
>
> `① 一个 Task 内的所有容器`
>
> `✔ 一定在同一台机器上运行（同一个 EC2 或同一个 Fargate 节点）`
>
> - **技术定义：** Task 是一个**包装盒**。ECS 不直接跑 Container，它跑的是 Task。
>
>   - **1:1 关系：** 一个 Task 里面通常包含 **1个** 核心 Container（比如你的 Web 服务）。
>   - **1:N 关系：** 一个 Task 也可以包含 **多个** Container（比如 1 个 Web 服务 + 1 个日志收集器 Sidecar）。
>   - **关键点：** 同一个 Task 里的所有 Container 共享同一个网络接口（IP）和存储卷，它们总是被部署在同一台机器上，同生共死。
>
> - **餐厅类比：**
>
>   > **Task = 独立灶台（工位）**。 这个灶台通常只有一个大厨（核心 App）。 但有时候，这个灶台也会配一个帮厨（Sidecar Container）负责递盘子或打扫卫生。大厨和帮厨共用这个灶台，如果灶台塌了，俩人都没法干活。
>
> **3. 最外层：Service (服务) —— “大堂经理”**
>
> 这是**管理层**。它负责确保你的 Task 按照预期运行。
>
> `Service 的多个 Task 副本：`
>
> - `可能分布在不同 EC2`
> - `也可能多个挤在同一台（取决于资源和调度策略）`
>
> - **技术定义：** Service 负责**编排**和**维护** Task 的状态。
>
>   - **数量控制：** 你告诉 Service：“我要 3 个 Task 副本”。Service 就会确保永远有 3 个 Task 在运行。
>   - **自我修复：** 如果其中一个 Task 崩溃了（挂了），Service 会立刻发现，并启动一个新的 Task 来补位。
>   - **负载均衡：** Service 通常会把 Task 挂载到 Load Balancer (ELB) 上，让流量能找到这些 Task。
>   - `ECS Service 中 Task 数量 >1 时，本质就是同一个任务模板的多实例副本，用于扩容和容错。`
>
> - **餐厅类比：**
>
>   > **Service = 后厨经理**。 经理不炒菜，但他负责盯着灶台。 他的指令是：“今晚生意好，必须保证有 **5 个灶台（Task）** 同时开火。” 如果 3 号灶台的厨师晕倒了（Task 挂了），经理会立刻叫一个新的厨师团队来占领一个新的灶台（启动新 Task），确保总数还是 5 个。
>
> **✔ 一句话理解**
>
> > **Cluster = 一组可供 ECS 调度 Task 的计算资源集合**
> >
> > 不同 **ECS Cluster** 的区别，本质上是：**它们是不同的“调度域 + 管理边界”**。Cluster 之间不会共享任务/调度/容量；你把它当成“两个独立的池子”就对了。
> >
> > **一个 Service 只能在一个 Cluster 里跑**
> >
> > **一个 Task 只能属于一个 Cluster**
> >
> > **不同 ECS Cluster 默认不会共享同一台 EC2。**
> >  一台 EC2 实例同一时间只能注册到 **一个 Cluster**。
>
> ------
>
> **✔ 为什么需要 Cluster**
>
> 因为 ECS 需要一个“地方”来放 Task，所以必须先定义资源池：
>
> Cluster 可以包含：
>
> - 多台 **EC2 实例**（EC2 launch type）
> - 或 **Fargate 计算容量**（无服务器模式）
> - 或两者混合
>
> ECS 调度器会在 Cluster 内找合适资源来放 Task。
>
> ✅ **同一个 Task 里的所有 container 一定在同一台宿主机/同一个运行环境里**，因为它们共享：
>
> - 网络命名空间（awsvpc 里共享同一个 ENI/IP）
> - 本地存储卷（task volume）
> - 生命周期（一起起、一起停）

#### 两种运行模式（高频必考）

1. **EC2 Launch Type**
   - 你管理 EC2
   - 你负责服务器
2. **Fargate Launch Type**
   - 不管 EC2
   - AWS 管服务器
   - 你只管 CPU / RAM

🧠 秒选逻辑：

- 想控制底层 EC2 → ECS (EC2 mode)
- 不想管理服务器 → **Fargate**

------

## 4️⃣ Fargate（Serverless 容器）

> Fargate = Serverless 容器计算

- 不需要管理 EC2
- 按 vCPU + RAM 付费
- 自动扩缩
- 适合微服务 / API / 容器化应用

🧠 题干出现：

- “Run containers without managing servers”
- “Serverless containers”

👉 选 **Fargate**

------

## 5️⃣ Serverless（CCP 超爱考概念）

### Serverless = 你不管服务器

- 不管理
- 不预置
- 不扩缩
- 只部署代码

⚠️ 重要：

> Serverless 不等于没有服务器
> 只是你看不到、也不用管。

------

## 6️⃣ AWS Lambda（超级高频）

### 一句话定义

> Lambda = Serverless Function（函数即服务）

- 事件触发
- 自动扩缩
- 按调用次数 + 执行时间计费
- 最长执行时间：**15 分钟**

------

### EC2 vs Lambda（考试最爱）

| EC2        | Lambda       |
| ---------- | ------------ |
| 持续运行   | 按需运行     |
| 管服务器   | 不管服务器   |
| 手动扩缩   | 自动扩缩     |
| 无时间限制 | 最长 15 分钟 |

------

### Lambda 什么时候用？

题干出现：

- S3 上传触发
- API 请求触发
- Event-driven
- 不想管理服务器
- 轻量计算
- 短时间执行

👉 选 **Lambda**

------

### Lambda 计费（必记两个维度）

1. 调用次数
2. GB-seconds（内存 × 时间）

免费额度：

- 100 万次/月
- 400,000 GB-seconds

⚠️ 重要考点：

> 增加内存 = CPU & 网络也变强

------

## 7️⃣ API Gateway

> API Gateway = 托管 API 服务

功能：

- 暴露 REST / HTTP API
- 集成 Lambda
- 内置：
  - 认证
  - Throttling（限流）
  - 缓存
  - API Keys

🧠 高频组合题：

> “Create fully serverless API”

👉 API Gateway + Lambda

------

## 8️⃣ AWS Batch

> Batch = 批处理计算服务

- 运行大规模批处理作业
- 可用 EC2 或 Spot
- 使用 Docker
- 自动管理计算资源

🧠 关键词：

- 批处理
- 有开始和结束
- 大规模数据处理
- 机器学习训练
- 渲染
- 10万+作业

👉 选 **AWS Batch**

------

### Batch vs Lambda（高频对比）

| Batch              | Lambda        |
| ------------------ | ------------- |
| 长时间任务         | 最长 15 分钟  |
| 大规模计算         | 轻量短任务    |
| 批处理             | 事件驱动      |
| 可用 Spot 优化成本 | 纯 Serverless |

🧠 秒选逻辑：

- “Minutes to hours” → Batch
- “Event triggered / short function” → Lambda

------

## 9️⃣ Amazon Lightsail

> Lightsail = 简化版 AWS

- 固定价格
- 简单
- 适合初学者
- 有模板（WordPress / LAMP）

⚠️ 关键限制：

- 无 Auto Scaling
- AWS 集成少

🧠 题干：

- “Simple web app”
- “Predictable pricing”
- “Small project”

👉 选 **Lightsail**

------

## 🔟 一张全局“秒选图”

| 场景               | 服务                 |
| ------------------ | -------------------- |
| 运行虚拟机         | EC2                  |
| 运行容器           | ECS                  |
| 不想管服务器跑容器 | Fargate              |
| 存 Docker 镜像     | ECR                  |
| 事件触发函数       | Lambda               |
| Serverless API     | API Gateway + Lambda |
| 大规模批处理       | AWS Batch            |
| 简单便宜网站       | Lightsail            |

------

## 🧠 本章 10 分钟速记卡

- Docker = 容器
- ECR = 私有镜像仓库
- ECS = 容器编排
- Fargate = Serverless 容器
- Lambda = Serverless 函数（≤15分钟）
- API Gateway = 暴露 API
- Batch = 批处理计算
- Lightsail = 简化 EC2

------

## 🎯 最重要的一句话

> Lambda = 事件驱动短任务
> Batch = 大规模批处理
> Fargate = 不管服务器的容器
> EC2 = 自己管服务器

------

如果你愿意，我可以：

- 给你做 **15 道 CCP 高频场景选择题**
- 或者给你做一张 **“EC2 vs Containers vs Lambda 决策树”**
- 或者帮你做一个 **整章综合思维导图**

你现在已经到 CCP 核心部分了，稳住 👌

# Cloud Monitoring

> **CloudWatch = 监控“性能/指标/日志”**
> **CloudTrail = 记录“谁做了什么 API 操作”**
> **X-Ray = 追踪“一次请求在分布式系统里走了哪”**
> 再加两张“健康看板”（全局 vs 你账号）。

------

## 1) 一句话定义（必须背）

- **CloudWatch**：监控与可观测性（**Metrics + Alarms + Logs + Events/EventBridge + Dashboards**）
- **CloudTrail**：审计与治理（记录 **API calls**：谁、何时、从哪、做了什么）
- **X-Ray**：分布式追踪（Tracing），找延迟瓶颈/错误链路
- **CodeGuru**：代码审查 + 性能剖析（ML 建议）
- **Service Health Dashboard**：AWS 全世界“总体服务状态”
- **Personal Health Dashboard**：只告诉你“**哪些 AWS 事件影响到你账号的资源**”

------

## 2) 必背关键词清单（考点压缩版）

- CloudWatch：**Metrics / Alarms / Logs / Dashboards / Events → EventBridge**
- EC2 metrics：默认 **5 分钟**；Detailed monitoring **1 分钟（付费）**
- Logs（EC2）：**默认不会自动上报**，需要 **CloudWatch Agent** + IAM 权限
- Billing metric：**Total Estimated Charge 只在 us-east-1**
- Alarm states：**OK / ALARM / INSUFFICIENT_DATA**
- CloudTrail：默认开启（至少可查到历史）；**90 天保留**；要长期留 → **S3 + Athena**
- CloudTrail events：**Management events（默认）** vs **Data events（默认不记录）**
- CloudTrail Insights：检测异常写操作模式（也会发 EventBridge event）
- EventBridge：CloudWatch Events 的升级版；**default / partner / custom event bus**
- X-Ray：service map、trace；定位瓶颈/错误/依赖
- 健康看板：Status（全局） vs Personal Health（与你相关）

------

## 3) “秒选表”：题干关键词 → 选哪个服务

| 题干关键词                                | 秒选                                         |
| ----------------------------------------- | -------------------------------------------- |
| CPU/Network/Latency/Errors 等性能指标     | **CloudWatch Metrics**                       |
| 指标超过阈值要通知/自动操作               | **CloudWatch Alarms**（可发 SNS / 触发动作） |
| 收集/查询应用日志                         | **CloudWatch Logs**                          |
| “EC2 上的日志要上报到 CloudWatch”         | **安装 CloudWatch Agent + IAM 权限**         |
| 定时触发（cron）或事件触发自动化          | **EventBridge（CloudWatch Events）**         |
| “谁删了资源/谁改了安全组/谁调用了 API？”  | **CloudTrail**                               |
| 想检测异常 API 行为（突然爆发的写操作等） | **CloudTrail Insights**                      |
| 微服务链路追踪、定位慢在哪                | **X-Ray**                                    |
| AWS 整体服务是否在宕机                    | **Service Health Dashboard**                 |
| 这次 AWS 故障是否影响到我的资源           | **Personal Health Dashboard**                |
| 代码质量/安全建议 or 运行时剖析           | **CodeGuru Reviewer/Profiler**               |

------

## 4) CloudWatch（CCP 最核心）

### 4.1 Metrics（指标）

- 指标 = 你要监控的数值（CPUUtilization、NetworkIn…）
- 可做 **Dashboards** 汇总展示

常见重要指标（记住“会考的那几个”）

- **EC2**：CPU、Network、Disk I/O
  - 默认：**每 5 分钟**
  - Detailed monitoring：**每 1 分钟（付费）**
- **EBS**：读写（Read/Writes）
- **RDS**：CPU、Free storage、Read/Write IOPS
- **S3**：请求数、延迟、错误（AllRequests 等）
- **Lambda**：Invocations、Errors、Duration
- **Billing**：Total Estimated Charge（**仅 us-east-1**）
- **Custom metrics**：你自己推送

### 4.2 Alarms（告警/自动动作）

- 当 metric 超阈值 → 告警 + 自动化
- 能做的事（考试常见）：
  - 发 **SNS** 通知
  - 触发 **Auto Scaling**
  - 对 EC2 做动作：stop/terminate/reboot/recover
- 状态必背：**OK / ALARM / INSUFFICIENT_DATA**

### 4.3 Logs（日志）

- CloudWatch Logs：集中存日志，支持设置 retention

#### EC2 的关键坑（超爱考）

> **默认 EC2 日志不会自动进 CloudWatch**
> 要：**装 CloudWatch Agent** + 配 IAM 权限（也可 on-prem）

------

## 5) Events → EventBridge（事件驱动/定时）

### CloudWatch Events（旧名字）

- 监听 AWS 资源变化事件（如 EC2 state change）
- 也能做 schedule（cron）

### EventBridge（新一代）

- CloudWatch Events 的进化版
- 三种 event bus（CCP 认识就够）：
  - **Default**：AWS 服务事件
  - **Partner**：SaaS（Zendesk/DataDog…）
  - **Custom**：你自己的应用事件

------

## 6) CloudTrail（审计：谁干的）

### 一句话

> CloudTrail 记录你账号里发生的 **API 调用历史**（Console/CLI/SDK/服务）

#### 常考点

- **资源被删了/配置被改了 → 先查 CloudTrail**
- 可以把日志送到 **S3 或 CloudWatch Logs**
- Trail 可全 Region 或单 Region

### 6.1 CloudTrail Events（两大类）

- **Management events（默认记录）**：创建/修改资源（也能分 read vs write）
- **Data events（默认不记录）**：高频数据面操作
  - S3 对象级（GetObject/PutObject/DeleteObject）
  - Lambda Invoke 等

### 6.2 Insights（异常检测）

- 基于正常模式建立 baseline
- 发现异常写操作/突发 IAM 行为/触顶等
- 结果：
  - 控制台可见
  - 送 S3
  - **生成 EventBridge event**（可自动化）

### 6.3 Retention（保留期）

- CloudTrail 事件默认 **90 天**
- 超过 90 天：**S3 存储 + Athena 查询**

------

## 7) X-Ray（分布式追踪）

- 追踪一次请求穿过多个服务的路径（trace）
- 价值关键词：**bottleneck、dependencies、errors、SLA、throttling、impacted users**
- CCP 只要会：**不是日志，不是指标，是 tracing**

------

## 8) CodeGuru（认识即可）

- **Reviewer**：静态代码审查（安全/最佳实践/低效），支持 Java/Python，集成 GitHub 等
- **Profiler**：运行时性能剖析（找最耗 CPU 的代码行、heap、异常检测），开销小

------

## 9) 两个健康看板（考“区别”）

- **Service Health Dashboard**：所有服务、所有 Region 的总体状态（全局）
- **Personal Health Dashboard**：只告诉你“哪些事件影响到你资源”，并给 remediation 建议（个性化）

------

## 10) 本章 10 分钟速记卡（直接背）

- **CloudWatch**：Metrics + Alarms + Logs + Dashboards + Events/EventBridge
  - EC2 metrics 默认 5 分钟；Detailed 1 分钟付费
  - EC2 logs 默认不上报 → Agent + IAM
  - Billing metric（Total Estimated Charge）只在 us-east-1
  - Alarm states：OK / ALARM / INSUFFICIENT_DATA
- **EventBridge**：CloudWatch Events 升级版；default/partner/custom bus
- **CloudTrail**：API 审计；90 天；长期 S3+Athena；Management 默认，Data 默认不记录；Insights 检测异常
- **X-Ray**：trace 微服务请求链路，找瓶颈/错误
- **Status vs Personal Health**：全局状态 vs 只影响你

------

# VPC

这章 CCP 的考法很固定：

> **公有子网怎么上网？私有子网怎么出网？谁当防火墙？怎么连 VPC/连本地？怎么私网访问 S3？**

你只要能做到“题干关键词 → 秒选组件”，就稳。

------

## 1) 一句话定义（必背）

- **VPC**：你在 AWS 里的“私有网络”（**Region 级**资源）。
- **Subnet**：VPC 里的网段分区（**AZ 级**资源）。
- **Public Subnet**：能直接连互联网（通常 route table 有到 IGW 的路由）。
- **Private Subnet**：不能被互联网直接访问（通常无到 IGW 的路由）。
- **Route Table**：决定“去哪里走”的路由规则（子网关联）。

------

## 2) 超高频“秒选表”（题干一出现就选）

| 题干关键词                                             | 秒选                              |
| ------------------------------------------------------ | --------------------------------- |
| “EC2 需要公网访问/对外提供网站”                        | **Public Subnet + IGW**           |
| “私有子网 EC2 需要下载补丁/访问外网，但不允许外网进来” | **NAT Gateway**                   |
| “VPC 连接互联网”                                       | **Internet Gateway (IGW)**        |
| “只允许出站，不允许入站”                               | **NAT（私网出网）**               |
| “子网级防火墙，带 ALLOW + DENY”                        | **NACL**                          |
| “实例/ENI 级防火墙，只能 ALLOW，且 stateful”           | **Security Group**                |
| “排查网络连不通，要看流量记录”                         | **VPC Flow Logs**                 |
| “两套 VPC 私网互通（不走公网）”                        | **VPC Peering（CIDR 不能重叠）**  |
| “私网访问 S3/DynamoDB，不走公网”                       | **VPC Endpoint Gateway**          |
| “私网访问其他 AWS 服务（如 SSM、CloudWatch…）”         | **VPC Endpoint Interface**        |
| “本地机房到 AWS 加密通道（走公网）”                    | **Site-to-Site VPN（CGW + VGW）** |
| “本地到 AWS 专线（更稳定更快）”                        | **Direct Connect**                |
| “很多 VPC + 本地网络要统一互联（hub-and-spoke）”       | **Transit Gateway**               |

------

## 3) IGW vs NAT（最爱考的对比）

### Internet Gateway（IGW）

- **作用**：让 VPC 能与互联网通信
- **典型场景**：Public subnet 里的资源要被公网访问（Web server）

### NAT Gateway

- **作用**：让 **Private subnet** 里的实例**只出不进**访问互联网
- 用途：下载更新、拉依赖、访问外部 API
- **AWS 托管**（相比 NAT Instance 更省心）

🧠 口诀：

- **IGW = 公网进出**（给 Public Subnet）
- **NAT = 私网只出**（Private Subnet 出网）

------

## 4) NAT Gateway vs NAT Instance（CCP 只需记住结论）

- **NAT Gateway**：托管、高可用、自动扩展（更推荐）
- **NAT Instance**：你自己维护（要打补丁/做故障切换），更麻烦

题目一般问：“哪个更简单/托管/高可用？” → **NAT Gateway**

------

## 5) Security Group vs NACL（防火墙必考点）

|          | Security Group               | NACL                              |
| -------- | ---------------------------- | --------------------------------- |
| 作用层级 | **实例/ENI**                 | **子网**                          |
| 规则类型 | **只允许 ALLOW**             | **ALLOW + DENY**                  |
| 状态     | **Stateful**（回包自动放行） | **Stateless**（回包也要显式允许） |
| 规则评估 | 所有规则一起评估             | 按规则编号顺序匹配                |

🧠 秒选：

- “需要 DENY 规则/子网级控制” → **NACL**
- “只想控制某台 EC2 的入站出站” → **Security Group**

------

## 6) VPC Flow Logs（排障神器）

- 记录 IP 流量到：
  - VPC / Subnet / ENI 级别
- 用于排查：
  - 子网到互联网是否通
  - 子网之间是否通
  - 公网到子网是否通
- 日志可送到 **CloudWatch Logs 或 S3**
- 还能记录一些 AWS 托管接口流量（如 ELB/RDS 等相关接口）

题干： “troubleshoot connectivity” → **Flow Logs**

------

## 7) VPC Peering（两套 VPC 私网互通）

- 两个 VPC 私网直连，不走公网
- 可跨账号
- **CIDR 不能重叠**（必考）
- CCP 常见隐藏点：**非传递（non-transitive）**
  也就是 A↔B，B↔C，不代表 A↔C 自动通

------

## 8) VPC Endpoints（私网访问 AWS 服务）

> 目的：**不经过公网**访问 AWS 服务（更安全、延迟更低）

两类（必背）

- **Gateway Endpoint**：只给 **S3 & DynamoDB**
- **Interface Endpoint**：其他大多数服务（本质是私网 ENI）

🧠 秒选：

- “private access S3 without internet” → **Gateway Endpoint**

------

## 9) On-Prem 连接：VPN vs Direct Connect（常考对比）

### Site-to-Site VPN

- **走公网**，但**加密**
- 组件：本地 **CGW** + AWS **VGW**
- 建立快、成本低

### Direct Connect

- **物理专线**，更稳定更快
- 私网链路
- 建立慢（通常要更久）

🧠 口诀：

- **VPN：快上、走公网、加密**
- **DX：专线、稳定快、部署慢**

------

## 10) Transit Gateway（大规模互联）

- 统一的“网络枢纽路由器”
- 适合：
  - 连接 **成百上千 VPC**
  - 连接 VPC + on-prem
- 典型架构：hub-and-spoke

题干： “many VPCs / centralized routing” → **Transit Gateway**

------

## 本章 10 分钟速记卡（直接背）

- VPC=私网（Region）；Subnet=分区（AZ）；Route Table 决定路由
- IGW：Public 子网上网/被公网访问
- NAT Gateway：Private 子网出网（只出不进）
- SG：实例级、只 ALLOW、stateful
- NACL：子网级、ALLOW+DENY、stateless
- Flow Logs：查网络流量/排障（送 CW Logs 或 S3）
- Peering：VPC 私连，CIDR 不重叠，非传递
- Endpoints：私网访问 AWS；Gateway=S3+DynamoDB，Interface=其他
- VPN：公网加密（CGW+VGW）；DX：专线私网更稳更快
- Transit Gateway：大规模 VPC/on-prem 中心互联

------

如果你想把 VPC 这章变成“见题秒选”，我可以给你出 **15 道 CCP 高频场景题**（重点：IGW vs NAT、SG vs NACL、Endpoint 类型、VPN vs DX、Peering vs Transit Gateway）。

# Other AWS Services

这一章在 CCP 的作用：

> **不要求你会配置**，主要考你“看到场景能认出服务名字”。
> 所以最有效的学习方式：**每个服务记 1 句话 + 3 个关键词 + 常见对比**。

------

## 1) 一页速记：场景 → 秒选服务

| 题干场景关键词                                   | 秒选                                    |
| ------------------------------------------------ | --------------------------------------- |
| 给员工提供云桌面（VDI），Windows/Linux 桌面      | **WorkSpaces**                          |
| 只在浏览器里“流式”运行桌面应用（不提供完整桌面） | **AppStream 2.0**                       |
| 做 VR/AR/3D Web 应用                             | **Sumerian**                            |
| 连接/管理 IoT 设备安全上云                       | **IoT Core**                            |
| 把 S3 视频转码成手机可播放格式                   | **Elastic Transcoder**                  |
| GraphQL + 实时同步 + 离线同步（移动/Web）        | **AppSync**                             |
| 快速做全栈 Web/移动端（BaaS + 前端库 + CI/CD）   | **Amplify**                             |
| 真机/真实浏览器自动化测试（多设备并发）          | **Device Farm**                         |
| 集中式跨服务备份（计划/保留/跨区/跨账号）        | **AWS Backup**                          |
| 灾备：持续块级复制，快速恢复到 AWS               | **Elastic Disaster Recovery (DRS)**     |
| 大量数据从本地同步到 S3/EFS/FSx（增量+定时）     | **DataSync**                            |
| 迁移前“盘点本地服务器/依赖关系”                  | **Application Discovery Service**       |
| Lift-and-shift 迁移服务器到 AWS（最小停机）      | **Application Migration Service (MGN)** |
| 做迁移商业评估/成本基线/迁移计划商业论证         | **Migration Evaluator**                 |
| 统一看迁移进度与工具状态（迁移控制台）           | **Migration Hub**                       |
| 混沌工程：注入故障做演练                         | **Fault Injection Simulator (FIS)**     |
| 编排 Lambda 工作流（可视化、并行、重试）         | **Step Functions**                      |
| 卫星地面站通信与数据下传到 AWS                   | **Ground Station**                      |
| 做营销活动：分群+模板+短信/邮件/推送/双向        | **Pinpoint**                            |

------

## 2) 各服务“1 句话 + 关键词”（CCP 够用）

### WorkSpaces（云桌面）

- **一句话**：托管 VDI 桌面（Windows/Linux），给员工一台“云电脑”
- 关键词：**Desktop-as-a-Service / VDI / per-hour or monthly**

### AppStream 2.0（应用流式）

- **一句话**：把**桌面应用**流式到浏览器（不用完整云桌面）
- 关键词：**application streaming / browser / per-app instance type**

✅ 高频对比：

- **WorkSpaces = 整个桌面**
- **AppStream = 只流式一个应用到浏览器**

------

### Sumerian（VR/AR/3D）

- **一句话**：快速做 VR/AR/3D 应用（偏内容与体验）
- 关键词：**VR / AR / 3D**

------

### AWS IoT Core（物联网）

- **一句话**：安全、可扩展地把 IoT 设备接入 AWS，并与云端服务联动
- 关键词：**devices / secure / serverless-ish integration**

------

### Elastic Transcoder（转码）

- **一句话**：把 S3 里的媒体文件转成不同设备可播放格式
- 关键词：**media transcoding / input S3 / output formats**

------

### AppSync（GraphQL 实时数据）

- **一句话**：GraphQL API + 实时订阅 + 离线同步（移动/网页数据同步）
- 关键词：**GraphQL / real-time / offline sync**

------

### Amplify（全栈开发平台）

- **一句话**：帮你快速搭建并部署全栈 Web/移动应用（前端库 + 后端托管 + CI/CD）
- 关键词：**full-stack / BaaS / CI-CD**
- 常见关系：**Amplify 常在后台用 AppSync**

------

### Device Farm（真机测试）

- **一句话**：在真实手机/平板/浏览器上并发跑自动化测试
- 关键词：**real devices / cross-browser / parallel tests**

------

### AWS Backup（统一备份）

- **一句话**：集中管理 AWS 各类资源备份（计划、保留、跨区/跨账号）
- 关键词：**centralized backup / retention / cross-region**

------

### Elastic Disaster Recovery DRS（灾备）

- **一句话**：把服务器做持续块级复制，出事可快速在 AWS 恢复
- 关键词：**disaster recovery / continuous replication / fast recovery**

------

### DataSync（数据同步搬迁）

- **一句话**：把大量数据从本地/其他存储同步到 S3/EFS/FSx（支持定时+增量）
- 关键词：**on-prem to AWS / scheduled / incremental**

------

### Application Discovery Service（迁移盘点）

- **一句话**：收集本地服务器清单、利用率、依赖关系，辅助迁移规划
- 关键词：**inventory / dependency mapping / discovery**

------

### Application Migration Service MGN（Lift-and-shift）

- **一句话**：把现有服务器“原样搬上云”（rehost），停机更少
- 关键词：**lift-and-shift / rehost / minimal downtime**

------

### Migration Evaluator（迁移商业评估）

- **一句话**：做迁移商业论证：现状基线、目标状态、成本与计划
- 关键词：**business case / baseline / planning**

------

### Migration Hub（迁移总控台）

- **一句话**：集中查看迁移资产与迁移进度（整合 MGN/DMS 等状态）
- 关键词：**central dashboard / tracking / orchestration templates**

------

### Fault Injection Simulator FIS（混沌工程）

- **一句话**：对 AWS 工作负载注入故障做演练，找隐藏问题
- 关键词：**chaos engineering / inject faults / experiments**

------

### Step Functions（工作流编排）

- **一句话**：把多个步骤（常见是多个 Lambda）编排成可视化工作流（重试/并行/分支）
- 关键词：**orchestration / workflow / retries & parallel**

------

### Ground Station（卫星）

- **一句话**：管理卫星地面站通信，把卫星数据快速送进 AWS
- 关键词：**satellite / ground stations / ingest to AWS**

------

### Pinpoint（营销活动）

- **一句话**：做大规模营销/通知活动：分群、模板、计划、双向消息
- 关键词：**campaigns / segmentation / SMS-email-push**
- 对比点：SNS/SES 更偏“发消息”；Pinpoint 更偏“做活动和人群运营”。

------

## 3) 本章最常考的 3 个对比（必背）

1. **WorkSpaces vs AppStream**：桌面 vs 应用流式
2. **AppSync vs API Gateway**：GraphQL+实时+离线 vs 通用 API（REST/HTTP）
3. **Backup vs DRS vs DataSync**：
   - Backup：备份与保留策略
   - DRS：灾备快速恢复（复制）
   - DataSync：数据搬运/同步（迁移数据）

------

## 4) 10 分钟速记卡

- WorkSpaces=云桌面；AppStream=浏览器流式应用
- AppSync=GraphQL 实时&离线；Amplify=全栈开发+部署平台
- Device Farm=真机测试；Elastic Transcoder=媒体转码
- Backup=集中备份；DRS=灾备恢复；DataSync=数据同步搬迁
- Discovery=迁移盘点；MGN=lift-and-shift；Migration Hub=迁移总控
- FIS=混沌故障注入；Step Functions=工作流编排
- Ground Station=卫星；Pinpoint=营销活动

------

如果你下一步想巩固记忆，我可以直接给你做一套 **“30 题闪卡式选择题”**（每题一句场景，5 秒内秒选服务）。