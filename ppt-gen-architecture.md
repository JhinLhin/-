# PPT 生成工具 — 架构文档（大白话版）

> 本文只描述**我负责的这一段**：从"用户给数据"到"交出 pptx + 预览"。
> 模板入库（ingestion）和模板填充（populate）由其他人负责，本文只定义与他们的接口。

---

## 一、这个东西到底在干嘛

一句话：**用户丢一堆数据进来，我们从固定的模板库里挑几张、把数据排进去、生成 PPT。**

关键约束有三条，整个设计都是围着它们转的：

1. **模板库是固定的。** 我们只会填数据，不会造模板。
2. **用户不知道有哪些模板。** 所以不能指望用户告诉我们该用哪个、该做几页。
3. **要在真正生成之前就知道行不行。** 不能等填完了才发现放不下。

由此得出核心的设计哲学：

> **系统做决定，用户拿到的是解释，不是选择题。**

---

## 二、整体流程

```
用户数据
   ↓
[planner]      一次 LLM 调用 → 直接输出 plan JSON
   ↓            （同时输入：catalog 里所有模板的 slot schema）
[validation]   纯代码逐页校验
   ↓
plan 报告      每页状态 + 缩略图 + 放不下的内容    ←──┐
   ↓                                                  │ 用户一句话
确认（人点按钮 / auto_confirm）                       │ → 全量重跑
   ↓                                              ────┘
[populate]     别人负责
   ↓
[assembly]     多页合并成一个 pptx
   ↓
[rendering]    pptx → png 预览
   ↓
交付：下载链接 + 预览图
```

### 为什么是这个形状

**LLM 提议，代码裁决。**

- planner 那一步可以随便发挥——它爱怎么排怎么排。
- 但输出必须过 validation 那一关。过不了的，谁说了都没用。

这就是整个方案确定性的来源。LLM 不可靠没关系，因为它说了不算。

**没有中间的"大纲"环节。** 不是先定几页、再挑模板、再填内容。是**一次调用直接出最终 plan**（模板 ID + 每个 slot 的值）。多一个中间态就多一层出错、多一次调用、多一堆状态要管。

---

## 三、每个模块负责什么

| 模块 | 干什么 | 有没有 LLM |
|---|---|---|
| `contracts/` | 三方共享的数据结构定义（pydantic）| — |
| `catalog/` | 读 ingestion 的产物，提供模板列表 + slot schema | 无 |
| `planner/` | **一次 LLM 调用**：数据 + schema → plan JSON。也负责重新规划 | 有（唯一一处）|
| `validation/` | **纯代码**逐页校验，产出报告 | 无（关键）|
| `plancache/` | plan_id → plan，带 TTL，确认时直接取，不重跑 LLM | — |
| `assembly/` | 多页 pptx 合并 | 无 |
| `rendering/` | pptx → png 预览 | 无 |
| `storage/` | 产物落盘 + 下载链接 | — |
| `pipeline/` | 编排 + 出错降级 | — |
| `api/` | 对外的 tool 定义 | — |

```
src/deckgen/
├── contracts/
├── catalog/
├── planner/        ★ 唯一用 LLM 的地方
├── validation/     ★ 整套方案的承重墙
├── plancache/
├── assembly/
├── rendering/
├── storage/
├── pipeline/
└── api/
```

### 被砍掉的模块（记录一下，避免以后又想加回来）

| 砍掉的 | 为什么不需要 |
|---|---|
| `session/` 状态机 | 没有多轮问答，只需要一个 plan 缓存 |
| `elicitation/` 逐页表单 | 用户数据一次性给全，不需要一格格问 |
| `planning/outliner` | 大纲和填充合并成一次调用了 |
| `selection/retriever` `scorer` `binder` | 选模板由 LLM 在 plan 里直接完成 |

---

## 四、plan 长什么样

planner 的输出：

```json
[
  {"template_id": "cover",     "slots": {"title": "...", "subtitle": "..."}},
  {"template_id": "kpi_3",     "slots": {"cards": [{"label":"...","value":"..."}, ...]}},
  {"template_id": "bullets_3", "slots": {"items": ["...", "...", "..."]}}
]
```

模板 json 里的 slot 定义（来自 ingestion）：

```json
{
  "template_id": "kpi_3",
  "slots": [
    {"id": "title", "type": "text", "required": true, "max_chars": 40},
    {"id": "cards", "type": "list", "required": true, "min_items": 3, "max_items": 3},
    {"id": "footnote", "type": "text", "required": false, "max_chars": 80}
  ]
}
```

---

## 五、validation 检查什么

全部是确定性比较，**零 LLM，毫秒级**：

- `template_id` 在 catalog 里存在吗
- `required` 的 slot 填了吗
- 类型对得上吗（文本不能塞进 chart slot）
- 文本超 `max_chars` 吗
- 列表条目数在 `min_items` ~ `max_items` 之间吗
- 有没有原始数据完全没被用上（→ 报告里的"放不下"）

每页产出一个状态：

| 状态 | 含义 |
|---|---|
| `ready` | 可以填 |
| `overflow` | 超容量，可自动压缩，**但必须留痕** |
| `invalid` | 填不了 |

**全部 `invalid` 才算真失败。** 部分失败就跳过那几页继续做，不要因为一页不行就整体拒绝。

---

## 六、数据怎么匹配模板

这里最容易想歪。**不是语义映射，是形状装配。**

能不能装进去，只看三件事：

1. required 的 slot 有东西填
2. 类型兼容
3. 每项在容量限内

**语义不是约束。** 用户的"区域销售数据"填进一个叫 `bullets_3` 的模板，只要是 3 条文本、每条不超长，就完全合法。模板不该管里面装的是销售还是招聘。

几种常见情况：

| 情况 | 结论 |
|---|---|
| 数据有 5 个指标，模板要 3 个 | 可以。挑 3 个重要的，或拆成两页 |
| 数据有 2 个指标，模板要 3 个 | 不行，除非 `min_items` 允许 2 |
| optional slot 没数据 | 可以，留空 |
| 数据剩一段没地方放 | 可以，报告里说明 |

**数据不必用完。** 这条要明确写进 planner 的 prompt，否则它会强行把所有东西塞进去，然后处处 overflow。

---

## 七、页数由系统决定

用户不知道模板能承载什么，所以**推导不出**合理页数。这件事归我们。

- 不问用户"你要几页"
- 页数是数据 + 模板容量算出来的结果
- **但必须在报告里解释为什么是这个数**

如果用户自己主动提了页数（"我要 10 页"），planner 顺手读到就尽量靠拢，但**绝不为凑数编内容**。做不到就说明差多少、缺什么。

### 一个关键的不对称

| 方向 | 能不能做 |
|---|---|
| **往少改**（6 → 3） | **永远可以**。合并、砍内容、降粒度，全是减法，减法永远安全 |
| **往多改**（6 → 9） | **有硬上限**。只能靠拆分 + 加结构页（目录/分隔/结束页），用尽就到头了 |

往多改到不了目标时，**尽力 + 诚实报告差额**：

> 拆到 8 页是极限了，再往上就得塞没依据的内容。这是 8 页的方案；要凑满 9 页的话，告诉我第 9 页放什么。

用户不会因为"只做到 8 页"生气，会因为"9 页里有 2 页是废话"生气。

### 红线

**不为凑数编造内容。** 一旦破例，"生成前就知道行不行"这个承诺就没意义了。

加一道纯代码守卫抓凑数（可后置）：
- 单页正文总字数过低 → 抓"标题党空页"
- 与其他页内容重复度过高 → 抓"换个说法再来一遍"

命中就标 `⚠ 内容偏空`，报告里显示出来。

---

## 八、用户要改怎么办

**不做表单，不做逐页编辑。** 用户用自由文本说，我们全量重跑：

```
用户："第 3 页不要折线图，改成表格；再加一页结束页"
  ↓
把 [原 plan + 这句话] 一起丢回 planner
  ↓
重出完整 plan → 重新校验 → 重新出报告
```

一次调用，一轮往返。**不需要写任何 diff / patch 逻辑**，也不存在"改了第 4 页导致模板要换"的级联问题。

这条回环也是我们敢让系统全权决定页数的原因：**决定错了，用户一句话就能推翻。**

### 迁就用户会不会失控？

不会。约束不在 planner 身上，在 validation 身上。

planner 可以随便迁就——但输出必须过 validation。模板不存在、required 没填、字数超了，用户再怎么要求也出不来。

我们答应的永远只是"重新安排一下"，不是"给你造个新版式"。

---

## 九、Hand-off（别的 agent 喂数据给我们）

**核心逻辑完全不需要知道对面是人还是 agent。** 差别只有一个：谁来确认。

| | 交互模式 | Hand-off 模式 |
|---|---|---|
| plan 生成 | 一样 | 一样 |
| 校验 | 一样 | 一样 |
| 确认 | 返回报告等用户点 | `auto_confirm=True` 直接走 |
| 返回 | 文件 + 预览 | 文件 + 预览 + **完整 plan 报告** |

**报告是同一份，只是消费者从人变成了 agent。**

不要在 hand-off 时弹选择器——上游 agent 点不了按钮，等于把自动化链路掐断。

### 对外接口

```python
generate_deck(data: str, auto_confirm: bool = False)
```

就这两个参数。之前设想过的 `target_count` / `count_mode` / `on_count_mismatch` 全部不需要——页数由系统定，用户想改就说话。

---

## 十、状态怎么存

极简，一个带 TTL 的字典就够，不一定需要 Redis：

```python
{plan_id: {"plan": [...], "validation": [...], "created_at": ...}}   # TTL 1h
```

- **确认时**：凭 `plan_id` 取缓存直接 populate，**不重跑 LLM**
- **修改时**：凭 `plan_id` + 修改文本，重新生成覆盖

没有状态机，没有 session，没有 next_action 协议。

---

## 十一、几个实现上的坑

- **渲染依赖很重。** pptx → png 基本要靠 LibreOffice headless（`soffice --convert-to pdf`）再转 png。Docker 镜像会变大，**提前验证**，别最后才发现。
- **两种 png 别混淆。** 模板文件夹里的 png 是"版式长什么样"（用在 plan 报告的缩略图）；输出的 png 是"生成结果预览"。路径和命名分开。
- **预览失败要降级。** 缩略图挂了不该导致整体失败——pptx 照给，preview 标注缺失。
- **overflow 自动压缩要留痕。** 报告里明说"这段从 142 字压到 98 字"，别偷偷改。
- **slide_id 用 uuid，不要用序号也不要用 template_id。** 同一个模板可以在一个 deck 里用多次。
- **catalog 索引更新。** ingestion 新增模板后怎么知道？MVP 用定时扫描 + 文件 hash 判断变更就够。

---

## 十二、待确认的问题

按优先级排。**第 1 条是硬前提，没解决前不建议继续往下写代码。**

### 🔴 P0 — 阻塞项

**1. 模板 json 里的 slot 有没有存容量约束？**（→ ingestion 团队）

具体是这四个字段：`required` / `max_chars` / `min_items` / `max_items`。

- 没有的话，validation 层无从校验，"populate 前就知道行不行"这个核心承诺是空的，只能退回"填完再看"。
- 这是**唯一的硬前提**，其他都好办。
- 如果答案是"没存"，最该做的事不是继续设计 flow，而是先把这个字段要下来。

### 🟡 P1 — 影响体验，要在上线前定

**2. optional slot 空着时，populate 怎么渲染？**（→ populate 团队）

- 把占位元素隐藏掉，还是留一块空白？
- 如果是后者，那张 slide 会有个突兀的空洞——plan 明明"合法"，出来的图却很难看。

**3. 有没有 required 的 image slot？**（→ catalog / ingestion）

- 用户 prompt 里全是文本、一张图都没有的场景很常见。
- 如果某模板有必填图片位，应该在 **catalog 层就过滤掉**，别让 planner 选了再被 validation 打回来。
- 除非模板自带默认图 —— 那也要确认。

**4. slot 有没有语义标签？**（→ ingestion）

- 比如某个 slot 标成"公司 logo""日期""章节号"。
- **有** → 就不是纯形状约束了，planner 需要知道这些标签。
- **没有** → 更好，匹配难度直接降一个量级。

### 🟢 P2 — 影响效果，可以边做边定

**5. 同类版式有没有多个容量档？**（→ ingestion）

- 比如 bullets 有没有 3/4/5 条的不同版本，KPI 卡片有没有 2/3/4 个的版本。
- **有** → 拆分/合并的弹性大，页数调整命中率高。
- **没有** → 页数基本被数据锁死。不影响方案成立，但报告里的解释文案要更主动，别让用户觉得系统在无视他。

**6. 多页的视觉风格一致性怎么保证？**

- 如果模板库里有多种设计风格，planner 独立挑每一页可能拼出五种风格的 deck。
- 可选方案：模板按 theme 分组，整个 deck 锁一个 theme；或在 prompt 里要求同族优先。
- 取决于模板库实际情况 —— 如果本来就是统一风格，这条自动消失。

**7. 产物生命周期。**

- 下载链接要不要 TTL？要不要清理？要不要防未授权访问？
- 不阻塞开发，但上线前要有答案。

### ⚪ P3 — 暂不实现，记录备查

**8. 数据太少怎么办？**

- 目前策略：直接返回"信息太少，请补充 XX"，让用户重发。
- 之前设想的"退化成逐页问答向导"**MVP 不做** —— 一整套多轮状态机的成本远高于一句话提示。
- 等有真实数据看到触发频率再说。

**9. 预先勾选式的模板推荐。**

- 形态：菜单项预先勾上，用户点掉不要的。纠错成本仍是"点一下"。
- 需要真实使用数据支撑（用户常选什么组合），MVP 不做。

----

> 对，但有个重要区别：**它不只是"选"，是"选 + 填"一步出。**
>
> 如果 planner 只输出 `["cover", "kpi_3", "bullets_3"]` 这样一串模板 ID，那就还得再跑一次"往里填内容"，而且第二步很可能发现填不进去——回到你最早担心的"填完才知道行不行"。所以 planner 的输出必须直接是**模板 ID + 每个 slot 的具体值**。选择这个动作是被"填"这个动作**顺带完成**的。
>
> ## 为什么敢把 match 交给 LLM
>
> 因为**它不是最终裁决者**。三层保险：
>
> 1. **schema 全量喂进去** — catalog 里所有模板的 slot 定义（含容量约束）都在 prompt 里。LLM 是看着"每个坑多大"在排，不是凭感觉挑
> 2. **输出结构受约束** — `template_id` 只能从给定列表里选，slot key 只能是该模板已有的。用 structured output / JSON schema 卡死
> 3. **validation 一票否决** — 编了不存在的 template_id、字数超了、required 漏了，全部拦下
>
> 所以 LLM 干的是**提议**这件事，不是**保证**这件事。它错了有人接得住。
>
> ## 这活儿本来适合谁干
>
> 值得想一下"如果不用 LLM 会怎样"：
>
> | 做法                | 结果                                                         |
> | ------------------- | ------------------------------------------------------------ |
> | 纯代码规则匹配      | 得先把用户的自然语言数据解析成结构化 IR，才能谈匹配——**这一步本身就得用 LLM**，而且更容易解析错 |
> | 向量检索 + 打分排序 | 语义相似度和"能不能装下"是两回事。检索出来的高分模板照样可能 overflow |
> | **LLM 一次出 plan** | 理解语义 + 拆分组织 + 塞进容量，一次做完                     |
>
> 第三种之所以合适，是因为这个任务的本质是**"把一堆非结构化内容重新组织成 N 个受限的格子"**——这正好是 LLM 擅长而规则系统很难做的事。规则系统擅长的是"检查格子填对没有"，那部分我们留给了 validation。
>
> 分工其实很清楚：**LLM 负责判断力，代码负责确定性。**
>
> ## Prompt 里必须写死的几条
>
> 这是 planner 质量的主要来源，比代码重要：
>
> - 只能用给定列表里的 `template_id`
> - **不得编造数据里不存在的内容**（红线）
> - **数据不必用完**，放不下的列在 `unused` 字段里
> - 每个 slot 的值必须满足该 slot 的 `max_chars` / `min_items` / `max_items`
> - 用户若提到目标页数，尽量靠拢，但不得为凑数编内容；做不到就在 `note` 里说明差多少
>
> 最后一点——**`unused` 字段一定要有**。没有它，planner 会本能地把所有内容硬塞进去，处处 overflow；有了它，"这段我放不下"就成了一个合法的输出选项，它才敢诚实。

> **该问，但别问"use case 是什么"。** 这么问只会得到"就是做 PPT 啊"这种答案。
>
> 要问的是**"给我 5–10 条真实的用户输入原文"**。一句抽象描述换不来任何设计决策，十条真实输入能一次性回答你列的一大半问题——数据一般多长、是不是表格、有没有图、用不用指定页数、语气是什么样。**样本比描述值钱一个数量级。**
>
> ------
>
> ## 先分类：你列的问题里，有几个其实不用问
>
> | 你的问题                        | 状态                                                       |
> | ------------------------------- | ---------------------------------------------------------- |
> | 数据不满足 template column name | **不用问**，已经定了：形状装配，不看语义                   |
> | 太多/太少怎么办                 | **不用问**，已经定了：多则挑选或拆分，少则该 slot 不 ready |
> | 怎么拆                          | **不用问**，planner 的 prompt 里的事，不是别人能回答的     |
> | LLM 动态改 template column name | **不能做**，见下                                           |
> | 用户非要有图                    | **要问**（P1 那条 image slot）                             |
> | 用户指定 N 张但不合理           | **不用问**，已经定了：少则可以，多则尽力 + 报差额          |
>
> ### 关于"LLM 动态调整 template column name"——这个要明确否掉
>
> slot 的 `id` 是**契约**，是你和 populate 团队之间的接口。LLM 改了它，populate 那边就找不到该往哪填了。
>
> 如果你指的是"模板里那个 slot 显示出来的**标题文字**"（比如卡片上写的"营收"要不要变成"用户数"），那是另一回事——**那本来就是内容，本来就该由 planner 填**。区别在于：
>
> - `slot.id` = `"card_1_label"` — **固定不变**，是接口
> - `slot` 的值 = `"营收"` — **随数据变**，是内容
>
> 问 ingestion 团队一句就能确认：**模板上的那些标签文字，是写死在 pptx 里的，还是也算一个可填的 slot？** 如果写死了，那你的模板其实是有语义的（"这是个财务模板"），匹配逻辑要重新考虑。这个挺关键的。
>
> ------
>
> ## 按对象整理的问题清单
>
> ### 给产品方 / 需求方
>
> 1. **给我 10 条真实用户输入原文。** 最重要的一条。
> 2. 典型输出是几页？3 页还是 30 页？（30 页的话 planner 一次调用可能撑不住，要分批）
> 3. 用户会不会传文件（Excel / Word / 图片），还是只有文本？
> 4. 有没有主题域限制？只做商业汇报，还是什么都能做？
> 5. 生成后用户主要在哪改——我们这里改，还是下载了在 PowerPoint 里改？（后者的话预览质量要求可以降低）
> 6. 上游 agent 是谁？它给的数据是已经结构化好的，还是也是自然语言？
>
> ### 给 ingestion 团队
>
> 1. **slot 有没有 `required` / `max_chars` / `min_items` / `max_items`？**（P0，唯一硬前提）
> 2. 模板上的标签文字是写死的还是可填 slot？（上面说的那个）
> 3. slot 有没有语义标签（"logo""日期""章节号"）？
> 4. 有没有 required 的 image slot？
> 5. 同类版式有没有多个容量档（bullets 3/4/5 条）？
> 6. 现在总共多少个模板？还会增长到多少？
> 7. 模板有没有 theme / 风格分组？
> 8. **模板 json 的完整 schema 能不能直接发我一份？** 比逐条问快。
> 9. 新模板入库后，我这边怎么知道？（定时扫描还是有通知）
>
> ### 给 populate 团队
>
> 1. **optional slot 空着时怎么渲染？** 隐藏还是留空洞。
> 2. 你们的输入格式具体长什么样？我按什么结构给你？
> 3. 文本超长时你们会自动缩字号，还是直接溢出？（如果会自动缩，我的 `max_chars` 校验可以放松一点）
> 4. 一次填一页还是一次填一个 deck？谁负责合并成一个 pptx——你们还是我？
> 5. 填充失败时你们返回什么？部分成功怎么表示？
> 6. 图片你们要 URL、本地路径、还是 base64？
> 7. 有没有幂等保证？同一个 plan 重试两次会不会出两份文件？
>
> ------
>
> ## 你没问但很关键的几个
>
> **A. 一次生成要多久，能接受多久？** LLM 一次调用 + populate + LibreOffice 转 png，30 页的话可能一两分钟。如果产品要求 10 秒内返回，架构上就得改成异步 job + 轮询，`api/` 层的形态完全不同。**这个越早知道越好。**
>
> **B. 谁来兜底"生成质量差"？** plan 合法 ≠ 好看。文字排版正确但内容平庸，用户抱怨的时候是谁的问题？如果这属于你的范围，那 prompt 工程的投入要加大。
>
> **C. 用户能不能自己指定模板？** 如果产品要给一个"换个版式"的按钮，那 catalog 得暴露一个"这一页可以换成哪几个模板"的接口——需要一个"容量兼容"的查询。不难，但要提前知道。
>
> **D. 多语言？** `max_chars` 对中文和英文完全是两回事。中文一个字占的宽度约等于两个英文字符。如果要支持多语言，容量校验得按**渲染宽度**算，不是字符数。这个坑很深，要早问。
>
> **E. 数据里有敏感信息吗？** 要发给 LLM。如果是财务/HR 数据，可能有合规要求。
>
> ------
>
> ## 一个建议
>
> 把这些问题**塞进那 10 条样本输入里去回答**。拿到样本后你自己跑一遍：
>
> > 这条输入，我手动来排的话会排成几页、用哪几个模板？排不进去的是什么？
>
> 手动做三五条，你对"到底难在哪"的直觉会比问十个问题都准。而且你会立刻发现哪些模板根本没人用得上、哪些容量档是缺的——**这些是问不出来的，只能试出来。**





> 对，就是 ingestion 那边存的 template png，直接复用，你不用生成任何东西。
>
> **有必要，但价值不在"好看"，在于它是你这套设计的信任基础。**
>
> ## 为什么
>
> 你的整个方案建立在一个前提上：**系统全权决定页数和版式，用户接受这个决定。** 用户凭什么接受？
>
> 纯文字报告是这样的：
>
> ```
> 1. cover
> 2. kpi_3
> 3. bullets_3
> ```
>
> `kpi_3` 是什么？用户不知道。他能做的只有盲目点"生成"，然后看到结果——**这就退回到"生成完才知道行不行"了**，只不过省了填充的时间，没省掉用户的心理成本。
>
> 有缩略图，用户扫一眼就知道"哦，三个数字卡片横排，行"。**确认才是真的确认，不是碰运气。**
>
> 而且你还答应了用户可以自由文本改。没有图，他改什么？他连现在是什么样都不知道。
>
> ## 成本几乎为零
>
> - png 已经存在，不用生成
> - 只是在报告里插几个图片 URL
> - 换来的是：用户不用等 populate 就能判断
>
> 这个投入产出比在整个系统里应该是最高的。
>
> ## 三个实现细节
>
> **尺寸。** 一个 deck 十几张 200KB 的 png 拼起来不小。让 ingestion 那边顺手存一个缩略图版本（宽 300–400px），或者你在 catalog 层生成一次缓存起来。别每次传原图。
>
> **别和输出预览混淆。** 这个前面提过一次，值得再强调：
>
> - **template png** = 版式长什么样（空的）→ 用在 plan 报告
> - **output png** = 生成结果预览（填了内容）→ 用在最终交付
>
> 两者路径和命名要分开，不然后期调试会很痛苦。
>
> **重复用同一模板时。** 一个 deck 里三页都用 `bullets_3` 很正常，报告里就是同一张图出现三次。这没问题，别去做去重——用户要看的是"第 3 页长这样"，不是"模板库里有什么"。
>
> ## Hand-off 时不用给
>
> 上游 agent 看不了图。返回结构里把 `thumbnail_url` 设成可选字段，`auto_confirm=True` 时不填就行。agent 要的是结构化的 slot 值和 validation 结果。
>
> ------
>
> 顺带一个可以问 ingestion 的：**png 现在是什么尺寸、有没有现成的缩略图版本？** 如果他们存的是全尺寸渲染图，你可以顺便请他们加一个 thumbnail——在他们那边做比在你这边做便宜，因为模板入库是一次性的，而报告是每次请求都要出的。
