# AWS SAA-C03 学习笔记（中文完整版）

> 由英文原版 24 篇笔记按顺序翻译整理而成。

# 第 1 章：EC2 基础（EC2 Fundamentals）

## 1. EC2 基础概念

**Amazon Elastic Compute Cloud（Amazon EC2，弹性计算云）** 在 AWS 云中提供可扩展的计算能力。它让你无需提前投资购买硬件，从而可以更快地开发和部署应用程序。

- **AMI（Amazon Machine Image，亚马逊机器镜像）：**
    - 一个模板，包含启动实例所需的软件配置（操作系统、应用服务器和应用程序）。
    - **区域级作用域（Region Scoped）：** AMI 被锁定在特定区域内。要在另一个区域使用某个 AMI，必须先**复制（copy）**它过去。
    - **公有 vs 私有：** 你可以创建自己的 AMI，并选择保持私有，或者公开共享给别人。
- **User Data（用户数据）：**
    - 在实例**首次启动**时运行命令的脚本。
    - 用于引导（bootstrap）实例，例如：安装更新、安装软件、从 S3 下载文件。
    - 以 `root` 权限运行。
- **Instance Metadata（实例元数据）：**
    - 关于实例本身的数据（例如：私有 IP、公有 IP、主机名、IAM 角色名称）。
    - 可以在实例内部通过这个 URL 访问：`http://169.254.169.254/latest/meta-data/`
    - _考试提示：_ 不要把 **User Data**（启动脚本）和 **Metadata**（实例信息）搞混。

## 2. EC2 实例类型基础

理解实例系列（instance family）对于为工作负载选择合适的资源至关重要。

**命名规则：** 例如 `m5.2xlarge`

- **m**：实例类别（系列）
- **5**：代数（数字越大越新）
- **2xlarge**：大小（CPU / 内存容量）

### 主要实例系列（助记口诀："FIGHT DR MC. P.X."）

|系列代号|类型|使用场景|
|---|---|---|
|**G, P, Tr, Inf**|**加速计算（Accelerated Computing）**|图形处理、浮点数计算、机器学习、AI。|
|**I, D, H**|**存储优化（Storage Optimized）**|高 IOPS、数据仓库、分布式文件系统。**I** 系列非常适合需要高 I/O 的数据库（如 NoSQL）。|
|**M**|**通用型（General Purpose）**|计算/内存/网络均衡。适合应用服务器、游戏服务器、小型数据库。|
|**C**|**计算优化（Compute Optimized）**|高性能处理器。批处理、媒体转码、科学建模、专用游戏服务器。|
|**R, X, Z**|**内存优化（Memory Optimized）**|大内存。内存数据库（Redis、Memcached）、大数据实时处理（Apache Spark）。|
|**T**|**可突发型（Burstable）**|通用型，但 CPU 可以"突发"（burst）。使用 **CPU 积分（CPU Credits）** 机制。适合有空闲时间的工作负载（如开发环境）。|

- _考试提示：_ 如果题目场景提到"高性能计算（HPC）"或"批处理"，就想到 **C** 系列。如果提到"内存数据库"或"缓存"，就想到 **R** 系列。

## 3. 安全组（Security Groups）与常见端口概览

**安全组**相当于 EC2 实例的**虚拟防火墙**，用于控制进出流量。

- **作用范围：** 作用于**实例级别**，而不是子网级别。
- **有状态（Stateful）：** 如果你允许某流量进入（IN），那么对应的响应流量会自动被允许出去（OUT），反之亦然。
- **规则：**
    - 只能创建**允许（ALLOW）**规则（不能创建拒绝 DENY 规则）。
    - 默认隐式**拒绝所有入站**流量。
    - 默认**允许所有出站**流量。
- **引用其他安全组：** 你可以授权一个特定的安全组 ID，而不是 CIDR 网段。这是多层应用（multi-tier）的最佳实践（例如：让数据库安全组只接受来自 Web 应用安全组的流量）。

### 需要记住的常见端口

|端口|协议|服务|
|---|---|---|
|**22**|SSH|Linux 登录（Secure Shell）|
|**3389**|RDP|Windows 登录（远程桌面）|
|**80**|HTTP|非加密的 Web 流量|
|**443**|HTTPS|加密的 Web 流量|
|**21**|FTP|文件传输|
|**3306**|TCP|MySQL / Aurora / MariaDB|
|**5432**|TCP|PostgreSQL|

- _考试提示：_ 如果你无法连接到实例（连接**超时 time out**），几乎总是**安全组**的问题。如果报错是"Connection Refused（连接被拒绝）"，那说明安全组多半没问题，而是实例上的应用程序没有在运行。

## 4. SSH 概览

**SSH（Secure Shell，安全外壳协议）** 用于远程操控 Linux 实例。

- **密钥对（Key Pairs）：** AWS 使用公钥加密体系。
    - **公钥：** 由 AWS 存储在 EC2 实例上。
    - **私钥：** 由你自己保存（`.pem` 文件）。**一旦丢失，无法找回。**
- **Putty（Windows 10 以下版本）：** 需要用 PuttyGen 把 `.pem` 转换成 `.ppk` 格式。
- **chmod 400：** 在 Mac/Linux 上，连接前必须先收紧密钥文件的权限：`chmod 400 my-key-pair.pem`。
- **EC2 Instance Connect：** 基于浏览器的 SSH 连接方式，无需手动管理密钥文件（使用 IAM 权限）。

## 5. EC2 实例购买选项

选择正确的计费模式是考试中常见的成本优化题。

| 选项 | 描述 | 适用场景 |
| --- | --- | --- |
| **按需实例（On-Demand）** | 按秒（Linux/Windows）或按小时计费。无承诺，成本最高。 | 短期、突发或不可预测的工作负载。首次上线的应用。 |
| **预留实例（Reserved - Standard 标准型）** | 承诺 1 年或 3 年。最高可享 72% 折扣。可以修改可用区（AZ）、作用范围、网络类型。 | 稳定持续的用量（例如核心数据库）。 |
| **预留实例（Reserved - Convertible 可转换型）** | 承诺 1 年或 3 年。最高可享 54% 折扣。可以更换实例**系列**、操作系统、租赁模式和付款方式。 | 稳定用量但软件需求可能变化的场景。 |
| **Savings Plans（节省计划）** | 承诺特定的用量（例如每小时 10 美元），期限 1 年或 3 年。分为 **EC2 Instance Savings Plan**（限定系列/区域）和 **Compute Savings Plan**（可灵活用于 EC2、Fargate、Lambda）。 | 最灵活的长期选项。在"灵活性 + 成本"类考题中，Compute Savings Plan 是非常热门的答案。 |
| **竞价实例（Spot Instances）** | 使用 AWS 的闲置容量。最高可享 90% 折扣。可能被中断，中断前有 2 分钟通知。 | 无状态、容错、时间灵活的工作负载（批处理任务、图像处理、CI/CD）。 |
| **专用主机（Dedicated Host）** | 整台物理服务器完全归你使用。可以看到插槽（socket）/核心（core）信息。 | 有合规要求、需要 BYOL（自带许可证，按插槽/核心计费）的场景。 |
| **专用实例（Dedicated Instance）** | 实例运行在专属于你的硬件上（单租户），但你无法控制实例的具体摆放位置。 | 需要单租户隔离、但不需要控制具体硬件的合规场景。 |
| **容量预留（Capacity Reservation）** | 在特定可用区预留容量。**没有任何折扣**（无论你是否运行实例，都按按需价格收费）。 | 确保在关键事件期间（例如"黑色星期五"）能够在特定可用区启动实例。 |

## 6. 竞价实例（Spot Instances）与竞价舰队（Spot Fleet）

竞价实例是最便宜的选择，但存在被终止的风险。

- **竞价价格（Spot Price）：** 市场价格随供需变化。你需要设定一个最高价（max price）。
- **中断（Interruption）：** 如果竞价价格超过你的最高价，或者 AWS 需要收回容量，你会收到 **2 分钟的提前警告**。
- **竞价请求类型：**
    - _一次性（One-time）：_ 被中断后，请求即关闭。
    - _持久性（Persistent）：_ 被中断后，请求会重新打开，等容量可用时再次尝试启动新实例。
- **Spot Fleet（竞价舰队）：** 由竞价实例和（可选的）按需实例组成的集合。
    - 它会尝试用多种实例类型的组合来满足一个目标容量（例如 50 个 vCPU）。
    - **策略（Strategies）：**
        - **lowestPrice（最低价）：** 从价格最低的资源池启动（成本最优，但中断风险较高）。
        - **diversified（分散）：** 分布到所有资源池（高可用，中断风险较低）。
        - **capacityOptimized（容量优化）：** 从容量最充足的资源池启动（最能减少中断）。_考试常考。_
- _考试提示：_ 如果题目问的是"关键工作负载"或"数据库"的"成本优化"，**绝对不要**选竞价实例。竞价实例只适合**可以被中断**的工作负载。


---

# 第 2 章：EC2 - 解决方案架构师助理级进阶

## 主题：EC2 高级网络与特性

### 1. 私有 IP vs 公有 IP vs 弹性 IP

理解 IP 地址的行为对考试非常关键，尤其是实例生命周期事件（停止 Stop / 启动 Start）时的表现。

#### 私有 IP（Private IP）

- **定义：** 用于 VPC 内部通信的内网 IP 地址。
- **持久性：** 在实例的整个生命周期内保持不变。即使你停止（Stop）再启动（Start）实例，私有 IP **不会改变**。
- **作用范围：** 只能在 VPC 内部访问，或通过 VPN / Direct Connect 访问（除非使用 NAT）。

#### 公有 IP（Public IP）

- **定义：** 可以从互联网访问的 IP 地址。
- **行为：**
    - 如果子网设置中启用了 `Auto-assign Public IP`（自动分配公有 IP），或在启动时请求了公有 IP，则会自动分配。
    - **动态的：** 如果你停止再启动实例，公有 IP **会改变**。
    - 不能手动在实例之间转移。

#### 弹性 IP（Elastic IP，EIP）

- **定义：** 分配给你 AWS 账户的一个静态、固定的公有 IP 地址。
- **使用场景：** 通过快速把该地址重新映射（remap）到另一台实例，来掩盖实例故障（实现快速故障切换）。
- **行为：**
    - 在你主动释放（release）之前，它一直归你所有。
    - **静态的：** 停止/启动实例时不会改变。
    - **作用范围：** 区域级（不能把一个 EIP 从 us-east-1 移到 us-west-2）。
- **费用：**
    - 如果 EIP 附加在一台正在运行的实例上，且是该实例上_唯一_的 EIP，则免费。
    - **以下情况会收费：** 实例处于停止状态、EIP 未附加到任何实例、或者一台运行中的实例附加了多个 EIP。

### 2. EC2 置放群组（Placement Groups）

置放群组决定实例在底层硬件上的摆放方式，以优化性能、持久性或可用性。

|类型|描述|使用场景|限制|
|---|---|---|---|
|**Cluster（集群）**|实例被紧密地放在同一个可用区（AZ）内。提供低延迟和高网络吞吐量（10Gbps 以上）。|高性能计算（HPC）、紧耦合应用、超级计算。|**只能在单个 AZ 内。** 如果机架故障，所有实例可能同时挂掉，风险高。|
|**Spread（分散）**|实例被放置在不同的底层硬件上（不同的机架）。最大化可用性。|关键应用，要求每个实例都与其他实例的硬件故障相互隔离。|每个置放群组在每个 AZ 内最多 **7 台实例**。|
|**Partition（分区）**|实例分散在多个逻辑分区上（每个分区是一组机架）。一个分区内的实例不会与其他分区的实例共享硬件。|分布式与大数据工作负载：Hadoop、Cassandra、Kafka。|可以跨多个 AZ。可扩展到数百台实例。|

### 3. 弹性网络接口（ENI）- 概览

ENI（Elastic Network Interface）是 VPC 中的一个逻辑网络组件，相当于一块虚拟网卡。

- **作用范围：** 绑定到特定的**可用区（AZ）**。不能把在 AZ A 中创建的 ENI 附加到 AZ B 中的实例上。
- **属性：** 一个 ENI 可以拥有：
    - 一个主私有 IPv4 地址。
    - 一个或多个辅助（secondary）私有 IPv4 地址。
    - 每个私有 IPv4 对应一个弹性 IP。
    - 一个公有 IPv4 地址。
    - 一个或多个安全组。
    - 一个 MAC 地址。

#### ENI 附加类型

1. **主 ENI（eth0）：** 实例启动时自动创建。**不能从实例上分离**。
2. **辅助 ENI（eth1 及以后）：** 可以独立创建，并随时附加到实例或从实例分离。

### 4. ENI - 补充阅读（考试提示）

- **故障切换场景：** 你可以创建一个低成本的故障切换方案——创建一个带私有 IP 和弹性 IP 的辅助 ENI。如果主实例故障，就把这个 ENI 移到备用实例上。
- **双宿主（Dual Homing）：** 你可以给一台实例附加两个 ENI：
    - **ENI 1：** 连接到管理子网（用于 SSH/RDP 访问）。
    - **ENI 2：** 连接到数据子网（用于应用流量）。
- **安全组：** 安全组是附加到 **ENI** 上的，而不是实例本身。这意味着同一台实例上的不同 ENI 可以有不同的安全组规则。

### 5. EC2 休眠（Hibernate）

EC2 休眠功能允许你暂停一台实例并在稍后恢复，同时保留内存（RAM）中的状态。

- **工作原理：** 休眠时，内存（RAM）中的内容会被写入**根 EBS 卷（Root EBS Volume）**。再次启动时，从磁盘重新加载内存内容。
- **优点：**
    - 比完整的停止/启动快得多的启动速度。
    - 应用程序无需"预热"或重新加载缓存。
- **要求（考试重点）：**
    - 根 EBS 卷必须**加密**。
    - 必须在**启动时**启用（不能在已运行的实例上开启）。
    - 根卷必须有足够空间来存储内存内容。
    - 不支持裸金属（bare metal）实例，也不支持使用实例存储（Instance Store）的实例。
- **生命周期表现：**
    - **实例 ID：** 保留。
    - **私有 IP：** 保留。
    - **弹性 IP：** 保留。
    - **普通公有 IP：** 丢失（和停止/启动一样）。
- **最长时限：** 实例休眠状态不能超过 **60 天**。


---

# 第 3 章：EC2 实例存储（EC2 Instance Storage）与 EFS

## 1. EBS（Elastic Block Store，弹性块存储）概览

- **定义：** 一种持久化的块级存储设备，可以附加到单台 EC2 实例上。它相当于一块虚拟硬盘（本质是网络磁盘）。
- **锁定在可用区（AZ）内：** 一个 EBS 卷绑定到特定的 AZ。要把卷移动到另一个 AZ，必须先做快照（snapshot），再在新的 AZ 中恢复。
- **持久性：**
    - 默认情况下，**根卷（Root Volume，即安装操作系统的卷）** 设置为**随实例终止而删除（Delete on Termination）**。
    - 额外的数据卷默认通常设置为**保留（Keep）**。
    - 可以通过 "DeleteOnTermination" 属性修改这个行为。

## 2. EBS 卷类型

考试必须掌握 SSD（重性能/IOPS）和 HDD（重吞吐量）的区别。

### 固态硬盘（SSD）- 针对事务型工作负载优化（IOPS）

|类型|名称|使用场景|关键特性|
|---|---|---|---|
|**通用型**|**gp3**|**默认首选**。启动卷、虚拟桌面、开发/测试环境。|IOPS 和吞吐量可以**独立**调整。基准 3,000 IOPS 和 125 MiB/s。比 gp2 便宜。|
|**通用型**|**gp2**|旧一代选择。|IOPS 与卷大小挂钩（每 GB 3 IOPS）。最高 16,000 IOPS。小容量卷使用"突发余额（Burst Balance）"机制。|
|**预置 IOPS**|**io2 Block Express**|**关键任务型**应用，亚毫秒级延迟。|最高持久性（**99.999%**）。最高 256,000 IOPS。支持**多重挂载（Multi-Attach）**。|
|**预置 IOPS**|**io1**|需要持续高 IOPS 的关键数据库。|最高 64,000 IOPS。支持**多重挂载**。相比 io2 属于旧一代。|

### 机械硬盘（HDD）- 针对大规模流式工作负载优化（吞吐量）

_不能用作启动卷（Boot Volume）。_

|类型|名称|使用场景|关键特性|
|---|---|---|---|
|**吞吐量优化**|**st1**|大数据、数据仓库、日志处理（Kafka、Splunk）。|最高吞吐量 500 MiB/s。针对顺序 I/O 优化。|
|**冷 HDD**|**sc1**|不常访问的数据、归档数据。|**成本最低**的 EBS 卷。最高吞吐量 250 MiB/s。|

## 3. EBS 快照（Snapshots）

- **机制：** 增量备份。只保存自上次快照以来发生变化的数据块。
- **存储位置：** 存储在 **Amazon S3** 中（但你无法在自己的 S3 桶里直接看到它们）。
- **作用范围：** 区域级。要在另一个区域使用快照，必须先**复制（Copy）**过去。
- **关键功能：**
    - **回收站（Recycle Bin）：** 可以将已删除的快照保留一段指定的时间（1 天到 1 年），以便从误删除中恢复。
    - **快速快照恢复（Fast Snapshot Restore，FSR）：** 消除从快照创建的卷的"初始化"（预热）延迟。价格昂贵。
    - **数据生命周期管理器（Data Lifecycle Manager，DLM）：** 基于计划和标签，自动化快照的创建、保留和删除。

## 4. AMI（Amazon Machine Image，亚马逊机器镜像）

- **定义：** 用于创建 EC2 实例的蓝图（包含操作系统、应用服务器和应用程序）。
- **作用范围：** **绑定区域**。要在另一个区域启动某个 AMI，必须先把 AMI 复制到那个区域。
- **来源：**
    - **公共 AMI：** AWS 官方提供（Amazon Linux 2、Ubuntu 等）。
    - **自建 AMI：** 从你自己定制过的实例创建。
    - **AWS Marketplace：** 来自第三方供应商的付费/订阅 AMI（例如加固的安全镜像、防火墙设备镜像）。
- **生命周期：** 创建（从实例/快照）→ 注册（Register）→ 复制（跨区域）→ 注销（Deregister，即删除）。

## 5. EC2 实例存储（Instance Store，"临时存储"）

- **定义：** 直接连接在运行你实例的宿主机上的物理存储。
- **性能：** 极高的 I/O（数百万 IOPS）和低延迟。
- **易失性（考试重点）：** 它是**临时的（ephemeral）**。
    - **数据会保留：** 在**重启（Reboot）**时。
    - **数据会丢失：** 在**停止（Stop）**、**终止（Terminate）**或**硬件故障**时。
- **使用场景：** 缓存、缓冲区、临时数据、高性能计算（HPC）、在别处有副本的临时内容。
- **快照：** 实例存储卷**不能**做快照。

## 6. EBS 多重挂载（Multi-Attach）

- **功能：** 允许_一个_ EBS 卷同时附加到_多台_ EC2 实例上。
- **限制：**
    - 仅限**同一个 AZ** 内。
    - 仅限 **io1** 和 **io2 Block Express** 卷类型。
    - 仅支持基于 Nitro 架构的实例。
    - **文件系统警告：** 必须使用**集群感知文件系统**（例如 GFS2、OCFS2）。普通文件系统（ext4、XFS、NTFS）会导致数据损坏。
- **使用场景：** 集群化的 Linux 应用（例如 Teradata）、高可用更新。

## 7. EBS 加密

- **机制：** 使用 AWS KMS（密钥管理服务）配合 AES-256 加密。
- **加密范围：** 启用加密后，以下内容全部被加密：
    1. 卷内的静态数据（data at rest）。
    2. 实例与卷之间传输中的数据。
    3. 从该卷创建的所有快照。
    4. 从这些快照创建的所有卷。
- **性能：** 对延迟的影响极小。
- **默认加密：** 可以在特定区域为整个账户开启"默认加密"。

## 8. Amazon EFS（Elastic File System，弹性文件系统）

- **定义：** 托管的 NFS（网络文件系统），可以同时挂载到多台 EC2 实例上。
- **协议：** NFSv4。**仅支持 Linux** 实例（POSIX 兼容）。
- **作用范围：** **跨多个 AZ**。数据存储在一个区域内的多个 AZ 中（高可用）。
- **扩展：** 自动扩展存储容量和吞吐量（可达 PB 级）。
- **存储类别：**
    - **EFS Standard（标准）：** 频繁访问，跨多 AZ。
    - **EFS Standard-IA（不频繁访问）：** 针对不是每天都访问的文件，成本更低。
    - **EFS One Zone（单区）：** 数据只存储在一个 AZ 内（成本更低，可用性也更低）。
- **性能模式：**
    - **General Purpose（通用）：** Web 服务、内容管理系统 CMS（默认模式）。
    - **Max I/O（最大 I/O）：** 大数据、并行处理（延迟更高，但吞吐量更大）。

## 9. 对比：EBS vs EFS vs 实例存储

|特性|EBS（弹性块存储）|EFS（弹性文件系统）|实例存储（Instance Store）|
|---|---|---|---|
|**类型**|块存储（网络磁盘）|文件存储（网络文件系统）|块存储（物理磁盘）|
|**持久性**|持久（停止实例后数据保留）|持久（停止实例后数据保留）|**临时**（停止即丢失）|
|**访问方式**|单实例（大多数情况）|**数千台实例**同时访问|仅限单实例|
|**可用性**|单 AZ|**跨多 AZ**（区域级）|单 AZ（绑定宿主机）|
|**性能**|低延迟|延迟比 EBS 高|**延迟最低**（IOPS 最高）|
|**备份**|快照|AWS Backup / EFS 备份|无（数据是临时的）|
|**操作系统支持**|Linux 和 Windows|**仅 Linux**|Linux 和 Windows|


---

# 第 4.1 章：可用性、可扩展性与负载均衡器（AWS Load Balancers）

## 1. 可用性与可扩展性

### **可扩展性（Scalability）**

- **定义：** 系统通过自我调整来应对更大负载的能力。
- **可扩展 ≠ 高可用。** 一个系统可以是可扩展的但不是高可用的（例如：一台超大的单体服务器）。

### **高可用（High Availability，HA）**

- **定义：** 让你的应用至少运行在 **2 个可用区（AZ）** 中。
- **目标：** 即使一个数据中心整体故障，服务也能存活。
- **公式：** 可用性 =（总时间 - 停机时间）/ 总时间。

## 2. 垂直扩展 vs 水平扩展

|特性|垂直扩展（Scale Up，向上扩）|水平扩展（Scale Out，向外扩）|
|---|---|---|
|**概念**|升级机器配置（例如 `t2.micro` → `t2.large`）。|增加_更多_机器（例如 1 台实例 → 3 台实例）。|
|**停机**|**有**（升级硬件通常需要停止/重启）。|**无**（无缝增减节点）。|
|**上限**|受硬件限制（会遇到天花板）。|理论上无限。|
|**使用场景**|数据库（RDS）、老旧的非分布式应用。|Web 应用、现代分布式系统。|
|**工具**|更改实例类型。|自动扩展组（ASG）、负载均衡器。|

## 3. 弹性负载均衡器（ELB）概览

ELB 将传入的流量分发到多个可用区中的多个下游目标（EC2 实例、容器、IP 地址）。

### **负载均衡器的类型**

1. **应用负载均衡器（ALB）：** 第 7 层（HTTP/HTTPS/gRPC）。智能路由。
2. **网络负载均衡器（NLB）：** 第 4 层（TCP/UDP）。超高性能。
3. **网关负载均衡器（GLB）：** 第 3 层（IP）。用于安全设备。

## 4. 应用负载均衡器（ALB）- 第 7 层

- **适合场景：** 微服务、基于容器的应用（Docker）、Web 应用。
- **协议：** HTTP、HTTPS、HTTP/2、WebSocket、gRPC。

### **监听器与规则（Listeners & Rules）**

- **一个监听器 → 多个目标组：** ALB 是"聪明的"。一个监听器（例如 443 端口）可以根据规则把流量路由到不同的后端。
- **路由规则：**
    1. **基于路径（Path-based）：** `example.com/users` → 目标组 A，`example.com/orders` → 目标组 B。
    2. **基于主机名（Host-based）：** `one.example.com` → 目标组 A，`two.example.com` → 目标组 B。
    3. **基于查询字符串 / 请求头：** 根据 `?id=123` 或自定义请求头路由。

### **目标组（Target Groups）**

- 可以指向：**EC2 实例**、**ECS 任务**、**Lambda 函数**、**私有 IP 地址**。

## 5. 网络负载均衡器（NLB）- 第 4 层

- **适合场景：** 实时数据、游戏、金融、TCP/UDP 流量。
- **协议：** TCP、UDP、TCP_UDP、TLS。
- **性能：** 每秒数百万请求，超低延迟。

### **关键特性**

- **静态 IP：** NLB 在**每个 AZ 提供一个静态 IP**。（ALB _没有_这个能力；ALB 只给你一个 DNS 名称）。
- **跨可用区负载均衡（Cross-Zone Load Balancing）：** **默认关闭**（为了省成本）。需要手动开启。
- **一个监听器 → 一个目标组：** 总体来说 NLB 是"傻瓜式"透传的。它把 TCP 流量转发给一个目标组，_不会_查看路径或请求头。

### **NLB 后面接 ALB 的架构**

- **模式：** **客户端 → NLB → ALB → EC2**。
- **为什么这么做？（考试考点）：**
    1. 你的应用需要一个**固定的静态 IP**（NLB 的功能），同时又需要 **HTTP 基于路径的路由**（ALB 的功能）。
    2. 你想使用 **AWS PrivateLink**（要求 NLB）来对外暴露一个 Web 应用（跑在 ALB 后面）。

## 6. 网关负载均衡器（GLB）- 第 3 层（网关）

- **使用场景：** 部署、扩展和管理**第三方虚拟设备**（防火墙、入侵检测/防御系统 IDS/IPS、深度包检测）。
- **工作原理：**
    1. 流量进入 VPC。
    2. 路由表把流量导向 **GLB 端点（Endpoint）**。
    3. GLB 把流量路由到第三方设备进行检查。
    4. 设备检查完毕后送回。
    5. 流量继续送达应用。
- **协议：** 使用 **GENEVE** 协议，端口 **6081**。
- **透明性：** 源和目标 IP 地址都会被保留（应用能看到原始的客户端 IP）。

## 7. ELB 关键特性与机制

### **粘性会话（Sticky Sessions / Session Affinity，会话保持）**

- **概念：** 确保某个客户端总是连接到负载均衡器后面的_同一台_后端实例。
- **ALB：** 使用 **Cookie** 实现。
    - _基于应用的 Cookie：_ 应用自定义的 Cookie。
    - _基于时长的 Cookie：_ 由负载均衡器生成（`AWSALB` cookie）。
- **NLB：** 使用**源 IP 亲和性**（对客户端 IP 做哈希来选定服务器）。

### **跨可用区负载均衡（Cross-Zone Load Balancing）**

- **不开启时：** 流量在_可用区之间_平均分配，而不是在实例之间。（如果 AZ A 只有 1 台实例，AZ B 有 9 台，那么 AZ A 那台会承受 50% 的流量 → 过载）。
- **开启时：** 流量在_所有实例之间_平均分配，无论在哪个 AZ。
- **默认值：**
    - **ALB：** 默认**开启**。
    - **NLB/GLB：** 默认**关闭**。

### **SSL/TLS 证书与 SNI**

- **SSL 终止（SSL Termination）：** 由负载均衡器负责加密/解密（把 CPU 工作从 EC2 上卸载下来）。
- **SNI（Server Name Indication，服务器名称指示）：**
    - **问题：** 你只有一个 ALB 监听器（443 端口），却要托管多个域名（`a.com`、`b.com`），且它们使用不同的 SSL 证书。
    - **解决：** SNI 让客户端在 TLS 握手时"说明"自己想访问哪个主机名，ALB 就能为该主机名加载**正确的证书**。

### **连接排空（Connection Draining / Deregistration Delay，注销延迟）**

- **场景：** 某台实例被标记为"不健康"，或正在缩容（scale in）。
- **机制：**
    1. ELB 停止向该实例发送_新的_请求。
    2. ELB 等待_已有的_"进行中"请求完成。
    3. **注销延迟（Deregistration Delay）：** 等待的时长（默认 **300 秒**）。
- **状态：** 实例状态会显示为 `Draining`（排空中）。
- **作用：** 防止用户在部署或扩缩容期间遇到"连接被重置（Connection Reset）"错误。

## 8. 高级考试场景（重点）

### **客户端 IP 保留（X-Forwarded-For）**

- **问题：** ALB 转发流量时，EC2 实例看到的来源是 **ALB 的私有 IP**，而不是用户的真实 IP。
- **解决（ALB）：** 实例必须读取 **`X-Forwarded-For`** HTTP 请求头来获取原始客户端 IP。
    - `X-Forwarded-Port` 和 `X-Forwarded-Proto` 分别提供端口和协议信息。
- **NLB 注意：** 当目标类型是**实例 ID** 时，NLB 会**自动**保留客户端 IP。如果目标类型是 **IP 地址**，则需要手动开启客户端 IP 保留。

### **安全组链式引用（安全性）**

- **场景：** 确保没有人能绕过负载均衡器直接访问 EC2。
- **配置：**
    1. **ALB 的安全组：** 允许来自 `0.0.0.0/0`（全网）的 HTTP/HTTPS 入站流量。
    2. **EC2 的安全组：** **只允许**来自 **ALB 安全组 ID** 的 HTTP 入站流量。
- **关键：** EC2 的安全组不要用 IP 网段，永远引用 ALB 的安全组 ID。

### **加权目标组（蓝绿部署 Blue/Green Deployment）**

- **功能：** 在一个监听器上给多个目标组分配权重。
- **使用场景：** 从版本 1（蓝）安全地迁移到版本 2（绿）。
    - 先让 V1 承接 90% 流量，V2 承接 10%。
    - 监控 V2 的错误率，然后逐步调整权重（50/50 → 0/100）。

### **WAF（Web 应用防火墙）集成**

- **ALB：** 原生支持 WAF。可以附加 WAF Web ACL 来拦截 SQL 注入或 XSS 攻击。
- **NLB：** **不支持 WAF。** 要在 NLB 上使用 WAF，必须在它前面放一个 CloudFront 或 ALB。

### **访问日志（Access Logs）**

- ELB 可以记录请求的详细信息（客户端 IP、延迟、请求路径、服务器响应）。
- **存储位置：** 日志存储在 **S3 桶**中。
- **要求：** 必须给 S3 附加一个桶策略（Bucket Policy），允许 ELB 服务写入。访问日志_默认不开启_。

### **慢启动模式（Slow Start Mode）**

- **概念：** 当一个新目标加入目标组时（例如扩容之后），负载均衡器会线性地逐步增加发给它的流量（爬坡），而不是一下子全量打过去。
- **目标：** 防止一台"冷"实例（需要时间预热缓存）被瞬间压垮。


---

# 第 4.2 章：自动扩展组（Auto Scaling Groups，ASG）

## 1. 自动扩展组（ASG）基础

**目标：** 在负载增加时向外扩展（增加实例），在负载下降时向内收缩（减少实例）以节省成本。同时保证运行的实例数量始终在最小值和最大值之间。

- **最小容量（Min Size）：** ASG 中的实例数量永远不会低于这个值。
- **最大容量（Max Size）：** ASG 中的实例数量永远不会超过这个值。
- **期望容量（Desired Capacity）：** 你希望_此刻_运行的实例数量。
    - _注意：_ 如果你手动修改期望容量，ASG 会立即启动或终止实例来匹配它。

### **启动模板（Launch Template）vs 启动配置（Launch Configuration）（考试重点）**

考试经常问该用哪个。**永远选择启动模板（Launch Template）。**

|特性|启动模板（较新/推荐）|启动配置（旧版）|
|---|---|---|
|**可修改？**|**可以**（支持版本管理）|**不行**（不可变——必须重新创建）|
|**实例类型**|支持混合实例（竞价 + 按需）|只支持单一实例类型|
|**功能**|T2/T3 Unlimited、专用主机等|功能有限|
|**考试结论**|**永远用这个**|只有在被遗留系统限制强制要求时才用|

## 2. 扩展策略（详细）

SAA-C03 考试非常侧重于判断_哪种_策略适合特定场景。

### **A. 动态扩展策略（Dynamic Scaling）**

基于实时指标（如 CPU、网络进出流量）的响应式扩展。

#### **1. 目标跟踪扩展（Target Tracking Scaling，最常用）**

- **概念：** "我想把平均 CPU 保持在 50%。"
- **工作原理：** 你设置一个目标值（例如 ASG 平均 CPU 40%）。ASG 会自动创建 CloudWatch 告警。CPU > 40% 就加实例，CPU < 40% 就减实例。
- **考试关键词：** "维持某个平均指标"、"最容易设置"。

#### **2. 阶梯扩展（Step Scaling）**

- **概念：** "如果 CPU 略高，加 1 台；如果 CPU 非常高，加 5 台。"
- **工作原理：** 根据告警超出的幅度定义不同的阶梯。
    - _示例：_
        - CPU 50-60% → 加 1 台实例。
        - CPU > 60% → 加 3 台实例。
- **关键特性：** 扩容（scale-out）时**不需要**等待冷却期（对流量尖峰的反应比简单扩展更快）。

#### **3. 简单扩展（Simple Scaling，旧版）**

- **概念：** "告警触发，就加 1 台实例。"
- **工作原理：** 依赖单个 CloudWatch 告警。
- **缺点：** 必须等**冷却期（Cooldown Period，默认 300 秒）**结束后才能再次扩展。一般更推荐阶梯扩展。

### **B. 预测性扩展（Predictive Scaling）**

- **概念：** 使用机器学习分析历史数据，预测未来流量。
- **工作原理：** 预测未来 48 小时的负载，并_提前_安排扩展动作。
- **考试场景：** "有规律的周期性业务模式"，或"在负载到来_之前_主动扩容"。

### **C. 计划性扩展（Scheduled Scaling）**

- **概念：** 基于已知的时间表进行扩展。
- **考试场景：**
    - "每周五下午 5 点流量激增。"
    - "下周一上午 9 点有大型营销活动。"

## 3. ASG 重要机制

### **扩展冷却期（Scaling Cooldowns）**

- **定义：** 一次扩展动作之后的"暂停"期（默认 300 秒），让指标有时间稳定下来。
- **效果：** 冷却期内，ASG 不会启动或终止更多实例（简单扩展严格遵守冷却期；阶梯扩展在扩容时可以不受它限制）。
- **技巧：** 如果 ASG 频繁地忽上忽下（"抖动 thrashing"），就增大冷却期。

### **健康检查（EC2 vs ELB）**

- **EC2 状态检查（默认）：** ASG 检查实例虚拟机是否在运行。如果硬件故障，ASG 会替换它。
- **ELB 健康检查（可选但很重要）：** ASG 通过负载均衡器检查_应用程序_层面的健康状况。
    - _场景：_ 实例在运行（EC2 检查通过），但应用崩溃了（HTTP 500 错误）。
    - _开启 ELB 检查时：_ ASG 发现应用挂了，终止该实例并替换。
    - _未开启 ELB 检查时：_ ASG 以为实例是健康的，继续把流量发给一个坏掉的应用。

### **终止策略（Termination Policy）**

缩容（移除实例）时，先杀掉哪一台？

1. **默认策略：**
    1. 先找实例数量_最多_的可用区（AZ）（以保持平衡）。
    2. 删除使用**最旧启动配置/模板**的实例。
    3. 如果有多台符合条件，删除距离**下一个计费小时**最近的那台。
- **考试提示：** 可以开启**实例缩容保护（Instance Scale-In Protection）**，防止特定实例在缩容时被终止（适合长时间运行的任务）。

## 4. 高级功能（考试高频）

### **生命周期钩子（Lifecycle Hooks）**

允许你在实例创建或终止过程中暂停，执行自定义操作。

- **Pending:Wait（等待就绪）：** 实例已创建，但在开始接收流量前暂停。（场景：安装软件、下载大数据集）。
- **Terminating:Wait（等待终止）：** 实例在被销毁前暂停。（场景：把日志上传到 S3、导出最终数据）。
- **集成方式：** 通常触发 EventBridge 或 SNS → Lambda 函数。

### **实例刷新（Instance Refresh）**

- **目标：** 把 ASG 中的所有实例更新到新的启动模板（例如新 AMI 或新的 User Data），且不停机。
- **机制：** 执行"滚动更新（Rolling Update）"，一批一批地替换实例。
- **检查点（Checkpoints）：** 可以设置百分比（例如"替换 20% 后等待 1 小时"）来验证稳定性。

### **预热池（Warm Pools）**

- **问题：** 实例启动和安装软件耗时太长（例如 5 分钟以上），导致动态扩展来不及响应。
- **解决：** 维护一组预先初始化好、处于`已停止（Stopped）`状态的 EC2 实例池。
- **好处：** ASG 扩容时直接从预热池取实例（启动一台已停止的实例比新建一台快得多）。

## 5. 场景速查表

| 场景 | 解决方案 |
| --- | --- |
| **流量完全不可预测/突发** | **目标跟踪扩展**（设置 CPU/网络指标）。 |
| **流量有已知的每周规律** | **计划性扩展**。 |
| **需要在负载到来之前扩容（基于历史）** | **预测性扩展**。 |
| **需要防止某台实例被终止** | 开启**缩容保护（Scale-In Protection）**。 |
| **应用启动后要花 10 分钟安装** | 使用**预热池**（停止状态）或**黄金 AMI（Golden AMI）**。 |
| **需要在实例终止_之前_运行脚本** | **生命周期钩子**（`Terminating:Wait`）。 |
| **给所有 ASG 实例更新操作系统补丁** | **实例刷新**（滚动更新）。 |
| **可用区之间实例数量不平衡** | ASG 会尝试**再平衡（Rebalance）**（在一个 AZ 启动，在另一个 AZ 终止）。 |


---

# 第 5 章：RDS、Aurora 与 ElastiCache

## 1. Amazon RDS 概览

- **是什么？** 托管的关系型数据库服务（Relational Database Service）。AWS 负责管理操作系统、打补丁、备份和硬件；你负责应用优化和数据库表结构（schema）。
- **支持的引擎：** PostgreSQL、MySQL、MariaDB、Oracle、Microsoft SQL Server、IBM Db2。
- **实例类型：**
    - _标准型_（m 系列）：通用。
    - _内存优化型_（r 系列、x 系列）：适合负载较重的生产数据库。
    - _可突发型_（t 系列）：开发/测试或低负载。
- **存储类型：**
    - **通用型 SSD（gp2/gp3）：** 性价比高，适合事务型工作负载。
    - **预置 IOPS SSD（io1/io2/Block Express）：** 高性能、低延迟、关键任务型。（注：EBS 层面 io2 支持多重挂载；RDS 使用 io1/io2 来应对高强度 I/O。）
    - **磁性存储（Magnetic）：** 旧版（考试中除非"成本"是_唯一_考量且性能完全不重要，否则避开这个答案）。
- **自动扩容：** RDS 存储自动扩展（Storage Auto Scaling）可以在不停机的情况下自动增加存储空间（你需要设定一个上限）。

## 2. RDS 只读副本（Read Replicas）vs 多可用区（Multi-AZ，高可用）

_这是必考题，必须把区别背得滚瓜烂熟。_

| 特性 | **只读副本（Read Replicas）** | **多可用区（Multi-AZ，备用实例）** |
| --- | --- | --- |
| **主要目标** | **可扩展性**（分担读请求） | **高可用 / 灾难恢复** |
| **复制方式** | **异步**（最终一致性） | **同步**（强一致性） |
| **主/被动** | 全部活跃（主库写、副本读） | 活跃主库 / 被动备用库 |
| **故障切换** | 手动（必须把副本提升为主库） | **自动**（DNS 指向备用库） |
| **承接流量** | 只承接**只读**流量 | 备用库**不承接任何流量**（处于闲置状态） |
| **范围** | 可以**跨 AZ**或**跨区域** | **区域内**（跨 2 个 AZ） |
| **备份** | 通常从主库做备份 | 从备用库做备份（避免 I/O 挂起） |

> **重要数字：**
> - **RDS 只读副本：** MySQL/MariaDB/PostgreSQL 最多 **15** 个副本；Oracle 最多 **5** 个。
> - **Multi-AZ：** 通常是 **1 个备用**实例（一共 2 份数据）。
> - **关于 Multi-AZ 集群的说明：** MySQL/PostgreSQL 有一种较新的部署选项，叫"带两个可读备用实例的 Multi-AZ（Multi-AZ with two readable standbys）"。但在标准考题中，如果只说 "Multi-AZ"，就按经典的**主/被动**架构理解——备用库不能承接流量。

- **考试提示：** 如果题目问"提升读密集型分析应用的性能"，选**只读副本**。
- **考试提示：** 如果题目问数据中心故障时的"容错"或"持久性"，选 **Multi-AZ**。

## 3. RDS Custom（仅限 Oracle 和 Microsoft SQL Server）

- **使用场景：** 你要迁移一个遗留应用，它需要**访问底层操作系统（OS）**，或者需要在数据库服务器上安装自定义软件/代理。
- **管理模式：**
    - 标准 RDS：**不能** SSH/RDP 登录实例。
    - **RDS Custom：** **可以** SSH/RDP 登录实例。
- **责任转移：** 因为你有操作系统访问权限，**你**要自己负责操作系统补丁。如果你的改动干扰了 AWS 的自动化钩子，AWS 会暂停自动化管理。
- **引擎：** 仅支持 Oracle 和 Microsoft SQL Server。

## 4. Amazon Aurora

- **架构：** AWS 原生的关系型数据库（兼容 MySQL 和 PostgreSQL）。
- **存储（"魔法"所在）：**
    - 数据存储在一个**共享集群卷（Cluster Volume）**中（虚拟存储），而不是本地 EBS。
    - **3 个 AZ，6 份副本：** Aurora 把你的数据写成 6 份，分布在 3 个 AZ。
    - **自愈能力：** 自动隔离坏掉的磁盘块。
    - **自动扩容：** 存储自动增长（最高 128 TB）。
- **可用性与副本：**
    - 写可用性：6 份副本中需要 4 份正常。
    - 读可用性：6 份副本中需要 3 份正常。
    - **Aurora 副本：** 最多 **15** 个（支持自动扩展）。
    - 如果主实例（Master）故障，某个 Aurora 副本会被自动提升为主（非常快，< 30 秒）。
- **端点（Endpoints，考试重点）：**
    - **集群端点（写端点 Writer Endpoint）：** 指向唯一的主实例，用于写入。
    - **读端点（Reader Endpoint）：** 在所有只读副本之间做负载均衡，用于报表/分析。
    - **自定义端点（Custom Endpoint）：** 指向一部分实例的子集（例如把"分析节点"和"Web 节点"分开）。

## 5. Amazon Aurora - 高级概念

- **Aurora Serverless（v1 和 v2）：**
    - 针对**间歇性或不可预测的工作负载**的自动扩缩容。
    - 无需预置实例。按秒（v2）或按分钟（v1）计费。
    - _考试场景：_ "一家创业公司的数据库每天只有 2 小时被重度使用，其余时间闲置。" → **Aurora Serverless**。
- **Aurora 全球数据库（Global Database）：**
    - **物理复制：** 在存储层进行复制（延迟 < 1 秒）。
    - **从区域：** 最多支持 **10 个从区域**（注意考试版本，以前是 5 个）。
    - **跨区域灾难恢复：** 可以在 1 分钟内把某个从区域提升为主区域。
    - _考试场景：_ "关键任务应用要求跨区域 RPO < 1 秒、RTO < 1 分钟。"
- **Aurora Backtrack（回溯，仅限 MySQL）：**
    - 无需从备份恢复，直接把数据库"倒带"回某个时间点。
    - Aurora MySQL 独有。
- **Aurora 机器学习：**
    - 让你通过 SQL 语句为应用添加基于机器学习的预测能力。
    - **集成：** 直接集成 **Amazon SageMaker**（任意 ML 模型）和 **Amazon Comprehend**（情感分析）。
    - _考试要点：_ 无需搭建复杂的 ML 管道，调用一个 SQL 函数即可。
- **Babelfish for Aurora PostgreSQL：**
    - **目标：** 让 Microsoft SQL Server 应用只需极少的代码改动就能跑在 Aurora PostgreSQL 上。
    - **工作原理：** 让 Aurora PostgreSQL 能理解 T-SQL（Transact-SQL），并监听 1433 端口。
    - _考试场景：_ "把遗留的 SQL Server 应用迁移到 AWS 开源数据库上，且重构工作量最小。"

## 6. RDS 与 Aurora - 备份、监控与维护

- **备份与恢复架构（考试重点）：**
    - **RDS 备份：**
        - **快照：** 对存储卷做传统快照。
        - **性能影响：** 单 AZ 部署时 I/O 可能短暂挂起；Multi-AZ 部署时从备用库做备份（无影响）。
        - **保留期：** 自动备份 1-35 天；手动快照可无限期保留。
    - **Aurora 备份：**
        - **持续备份：** 备份本质上是持续不断地流式写入 S3。
        - **性能影响：** 对数据库**零性能影响**（没有 I/O 冻结）。
        - **恢复：** 时间点恢复（PITR）比 RDS 更快。
- **数据库克隆（仅限 Aurora）：**
    - **概念：** 使用**写时复制（Copy-on-Write）**协议，从现有集群创建一个新集群。
    - **速度：** 即时完成（初始时不复制全量数据）。
    - **成本：** 只为新克隆上_发生的变更_付费。
    - _考试场景：_ "需要立刻用生产数据创建一个 Staging 测试环境，且不想付高昂的存储费用。"
- **监控工具（必须分清区别！）：**
    - **CloudWatch：** _虚拟化层（Hypervisor）_指标。CPU、网络、磁盘 I/O。
        - _局限：_ 看不到_是哪个_查询或进程在消耗资源。
    - **增强监控（Enhanced Monitoring）：** _操作系统级_代理。
        - _关键指标：_ 内存、**进程列表**（哪个进程在占用 CPU？）、Swap。
        - _粒度：_ 最细到 1 秒。
    - **Performance Insights（性能洞察）：** _数据库级_仪表板。
        - _关键指标：_ **数据库负载（DB Load / AAS）**、等待事件、Top SQL 查询。
        - _考试场景：_ "数据库很慢，CPU 100%。是哪条 SQL 查询导致的？" → **Performance Insights**。
- **维护窗口（Maintenance Windows）：**
    - 引擎版本升级或操作系统打补丁时需要。
    - **RDS Multi-AZ：** 先给备用库打补丁 → 故障切换 → 再给原主库打补丁。（切换时有短暂停机）。

## 7. RDS 安全

- **静态加密（Encryption at Rest）：** 在创建时启用，使用 **KMS**（AES-256）。已存在的未加密数据库不能直接加密（流程：做快照 → 复制快照并加密 → 从加密快照恢复）。
- **传输加密（Encryption in Transit）：** 支持 SSL/TLS。可通过参数组强制启用（Postgres/MySQL 设置 `rds.force_ssl=1`）或使用 SQL 命令。
- **IAM 数据库认证：**
    - 不用数据库密码，改用 IAM 令牌（有效期 15 分钟）。
    - 最适合：Lambda 函数或 EC2 实例访问 RDS。免去硬编码凭证。
- **安全组：** 控制**网络**访问（只允许来自应用安全组的 3306/5432 端口流量）。

## 8. RDS Proxy

- **问题：** 打开和关闭数据库连接的开销很"贵"（消耗 CPU/内存）。高并发应用（比如 Lambda）可能耗尽数据库连接（即"连接风暴 Connection Storms"）。
- **解决：** RDS Proxy 位于应用和数据库之间。
    - **连接池：** 复用已有连接。
    - **减少故障切换时间：** 可将 Aurora 故障切换时间缩短最多 66%（绕过 DNS 传播延迟）。
- **考试关键词：** "Lambda"、"连接太多"、"连接超时"、"缩短故障切换时间"。

## 9. ElastiCache 概览

- **是什么？** 托管的内存缓存服务（支持 Redis 和 Memcached）。帮助把读密集型负载从数据库上卸载下来。

### Redis vs Memcached（对比）

| 特性 | **Redis** | **Memcached** |
| --- | --- | --- |
| **数据类型** | **复杂**（字符串、列表、集合、有序集合、哈希、位图） | **简单**（字符串、对象） |
| **持久化** | **支持**（AOF/RDB）——重启后数据仍在 | **不支持**——重启即丢失 |
| **可用性** | **Multi-AZ** + 自动故障切换 | 无 Multi-AZ（仅分片） |
| **扩展** | 只读副本扩展 + 集群模式 | 多线程（垂直/水平扩展） |
| **功能** | 发布/订阅、地理空间、事务、**排行榜** | 简单缓存 |
| **合规** | HIPAA、FedRAMP | HIPAA、FedRAMP |

- **考试速记：**
    - 需要"排行榜"、"排序"、"持久化"或"高级数据类型"？**选 Redis。**
    - 需要"简单的多线程缓存"或"对象缓存"？**选 Memcached。**

## 10. 面向解决方案架构师的 ElastiCache（策略）

### 缓存策略

1. **懒加载（Lazy Loading / Cache-Aside，旁路缓存）：**
    - 应用先查缓存。未命中就查数据库，写入缓存，再返回给用户。
    - _优点：_ 只缓存被请求过的数据。缓存节点故障不致命。
    - _缺点：_ "缓存未命中"有代价（要 3 次网络调用）。数据可能过期（stale）。
2. **写穿透（Write-Through）：**
    - 应用同时写数据库和缓存。
    - _优点：_ 数据永不过期，读取快。
    - _缺点：_ 写入有代价（写变慢）。可能浪费内存缓存了从不被读取的数据。

### 使用场景：会话存储（Session Store）

- **场景：** 一个无状态 Web 应用横向扩展到多台 EC2。用户的登录会话存在哪里？
- **方案 A：** DynamoDB（不错，但延迟比缓存高）。
- **方案 B：** **ElastiCache（Redis）**。
    - _为什么？_ 亚毫秒级延迟。可以设置 TTL（存活时间）让会话自动过期。

### 大规模缓存

- 如果看到"全球"或"跨区域"缓存需求 → **Redis Global Datastore**。

## 11. 考试必备补充（务必掌握！）

### AWS Secrets Manager 集成

- **问题：** 在代码里硬编码数据库密码是安全违规。
- **解决：** 把凭证存到 **AWS Secrets Manager**。
- **关键功能：** **自动轮换（Automatic Rotation）。** Secrets Manager 可以自动轮换 RDS 的密码，并把新密码提供给应用，全程无需人工干预。
- _考试关键词：_ "轮换凭证"、"移除硬编码密码"、"自动获取密钥"。

### RDS 自动备份 vs 手动快照

- **自动备份（Automated Backups）：**
    - 默认开启。
    - 保留期：1 到 35 天（设为 0 即禁用）。
    - 支持时间点恢复（PITR）。
    - _删除实例时自动备份也会被删除_（除非勾选"保留最终备份"）。
- **手动快照（Manual Snapshots）：**
    - 用户手动发起。
    - 保留期：**无限期**（永不过期）。
    - _即使原 RDS 实例被删除，快照仍然保留。_
- _考试场景：_ "公司需要为审计保留数据库备份 5 年。" → 用**手动快照**（或 AWS Backup 服务），因为自动备份最多只有 35 天。

### 参数组：静态参数 vs 动态参数

- **动态参数：** 修改后**立即生效**（无需重启）。
- **静态参数：** 修改后需要**手动重启**数据库才能生效。
- _考试场景：_ "你修改了一个参数来开启日志功能，但没有生效。" → 检查是不是静态参数，以及实例是否重启过。

### 数据库迁移服务（DMS）

- **是什么？** 在源数据库保持运行的情况下把数据库迁移到 AWS（停机时间极短）。
- **模式转换工具（SCT，Schema Conversion Tool）：** 如果引擎不同（例如 Oracle → Aurora PostgreSQL），在 DMS 之前_先用_ SCT 转换表结构和存储过程。
- **同构迁移（Homogeneous）：** 相同引擎（MySQL → RDS MySQL）。
- **异构迁移（Heterogeneous）：** 不同引擎（SQL Server → Aurora）。使用 SCT + DMS。


---

# 第 6 章：Route 53

## 1. 什么是 DNS？（域名系统）

- **概念：** "互联网的电话簿"。它把便于人类记忆的主机名（例如 `www.google.com`）翻译成机器可读的 IP 地址（例如 `172.217.1.1`）。
- **层级结构：**
    - **根（Root）：** 域名最末尾的那个点（由 IANA 管理）。
    - **顶级域名（TLD）：** .com、.org、.gov。
    - **二级域名（SLD）：** amazon.com、google.com。
    - **子域名（Subdomain）：** www、api、blog。
- **记录（Records）：** DNS 数据库中的具体指令（A、AAAA、CNAME、NS、SOA 等）。

## 2. Route 53 概览

- **定义：** 一个高可用、可扩展的云端域名系统（DNS）Web 服务。
- **主要功能：**
    1. **域名注册：** 可以直接购买域名。
    2. **DNS 路由：** 把互联网流量路由到资源。
    3. **健康检查：** 监控资源的健康状况。
- **全球服务：** Route 53 是**全球级**服务（类似 IAM 和 S3），虽然它指向的资源是区域级的。
- **SLA：** 它是唯一提供 **100% 可用性 SLA** 的 AWS 服务。
- **托管区域（Hosted Zones）：**
    - **公有托管区域：** 供互联网解析的记录。
    - **私有托管区域：** 供内部 VPC 解析的记录（要求 VPC 的 `enableDnsHostnames` 和 `enableDnsSupport` 设置为 true）。

## 3. Route 53 - TTL（Time To Live，存活时间）

- **定义：** DNS 解析器或本地计算机缓存某条 DNS 记录的时长（以秒为单位）。
- **强制性：** 除 **Alias（别名）记录**外，所有记录类型都必须设置 TTL。
- **高 TTL（例如 24 小时）：**
    - 到 Route 53 的查询流量更少。
    - 成本更低。
    - **缺点：** 记录修改后要很久才能生效（传播慢）。
- **低 TTL（例如 60 秒）：**
    - 到 Route 53 的查询流量更多。
    - 成本更高。
    - **优点：** 修改传播快（适合频繁更换 IP 的场景）。

## 4. Route 53：CNAME vs Alias（考试重中之重）

### CNAME（规范名称记录）

- 把一个**主机名指向另一个主机名**。
- 例如：`app.mydomain.com` → `lb-123.us-east-1.elb.amazonaws.com`。
- **限制：** **不能**在**根域名（Zone Apex，即裸域名）**上创建 CNAME 记录，例如 `mydomain.com`。

### Alias（别名）记录

- **AWS 专属**功能。
- 把一个**主机名指向一个 AWS 资源**（ELB、CloudFront、S3 静态网站、Elastic Beanstalk、API Gateway、VPC 接口端点、Global Accelerator）。
- **关键优势：** **可以**用在**根域名**上（`mydomain.com`）。
- **行为：** 表现得像 A 记录（返回 IP），但内部映射到 AWS 资源。
- **成本：** 对 AWS 资源的 Alias 查询免费。
- **TTL：** Alias 记录不能设置 TTL（Route 53 遵循目标资源的 TTL）。

> **考试提示：** 如果题目问如何把_根域名_（zone apex）指向 ELB 或 S3 桶，答案永远是 **Alias**，绝不是 CNAME。

## 5. 路由策略（Routing Policies）

### A. 简单路由（Simple Routing）

- **使用场景：** 域名只有一个资源承担某项功能时使用。
- **行为：**
    - 可以向客户端返回一个或多个值（IP）。
    - 如果返回多个值，客户端会随机选一个。
- **限制：** **不支持**健康检查（简单路由无法自动避开不健康的实例）。

### B. 加权路由（Weighted Routing）

- **使用场景：** **A/B 测试**、蓝绿部署、或者有意地在区域之间分配流量。
- **行为：** 给每条记录分配一个"权重"（0-255）。
- **逻辑：** 流量占比 = 该记录权重 / 所有权重之和。
    - _示例：_ 记录 A（权重 10），记录 B（权重 90）。记录 A 得到 10% 的流量。
- **注意：** 权重设为 0 即停止向该资源发送流量。

### C. 延迟路由（Latency Routing）

- **使用场景：** **性能优先**。为全球用户提供最佳体验。
- **行为：** Route 53 根据用户位置和各 AWS 区域的延迟，把用户路由到**延迟最低**（响应最快）的 AWS 区域。

### D. 故障切换路由（Failover Routing）

- **使用场景：** **灾难恢复（DR）**。主-备（Active-Passive）架构。
- **前提：** 必须配置**健康检查**。
- **行为：**
    - **主记录（Primary）：** Route 53 持续监控它。
    - **备记录（Secondary）：** _只有_当主记录不健康时，流量才会被路由到这里。

### E. 地理位置路由（Geolocation Routing）

- **使用场景：** **合规要求**、内容限制、本地化语言。
- **行为：** 根据**用户的地理位置**（大洲、国家或美国的州）路由流量。
- **示例：** 法国用户访问法国服务器；其他所有人访问默认服务器。
- **与延迟路由的关键区别：** 延迟路由看的是_速度_；地理位置路由看的是_边界_。

### F. 地理临近度路由（Geoproximity Routing）

- **使用场景：** 需要"偏置（Bias）"的复杂地理路由。
- **行为：** 根据用户和资源双方的地理位置来路由流量。
- **偏置（Bias）：** 通过增大/减小偏置值，把流量从一个区域移向另一个区域（扩大或缩小某区域的地理覆盖范围）。
- **要求：** 必须使用 **Route 53 Traffic Flow**（可视化编辑器）。

### G. 基于 IP 的路由（IP-Based Routing）

- **使用场景：** 针对特定 ISP 的性能优化，或对内部网段进行路由。
- **行为：** 根据客户端的 **CIDR（IP 网段）**路由流量。
- **示例：** Route 53 识别出 IP 来自 "Comcast" 或某个特定公司网段，并据此路由。

### H. 多值应答路由（Multi-Value Answer Routing）

- **使用场景：** 相当于"带健康检查的简单路由"。
- **行为：** DNS 查询会返回多个值（IP），但**只返回健康资源的值**。
- **上限：** 最多 8 条记录。
- **注意：** 它不能替代 ELB（负载均衡器），只是一个基本的客户端侧负载均衡。

## 6. 健康检查（Health Checks）

- **目的：** 自动化故障切换、监控状态。
- **类型：**
    1. **端点检查（Endpoint）：** 检查一个 IP 地址或域名（HTTP/HTTPS/TCP）。
    2. **计算型检查（Calculated）：** 组合其他健康检查的结果（AND/OR/NOT 逻辑）。
    3. **CloudWatch 告警检查：** 检查某个 CloudWatch 告警的状态（适合检查 DynamoDB 限流或自定义指标，而不只是连通性）。
- **运行机制：**
    - 全球各地的健康检查器向你的端点发送请求。
    - 默认：30 秒间隔（可以加快到 10 秒）。
    - 默认：连续 3 次失败判定为不健康。
- **私有托管区域：** 如果要监控内部资源（私有 IP），必须创建一个 CloudWatch 告警，并使用 "CloudWatch 告警" 类型的健康检查（Route 53 的公网检查器无法访问你的私有 VPC）。

## 7. 第三方域名与 Route 53

- **场景：** 你在 GoDaddy（注册商）买了 `example.com`，但想在 AWS Route 53 里管理 DNS 记录。
- **步骤：**
    1. 在 Route 53 为 `example.com` 创建一个**托管区域（Hosted Zone）**。
    2. Route 53 会生成 4 个**名称服务器（NS）**。
    3. 登录 GoDaddy（注册商）。
    4. 把 GoDaddy 里的 NS 记录改成 Route 53 提供的这 4 个。
- **注意：** 域名注册和 DNS 托管是两回事。你可以把注册留在 GoDaddy，而把 DNS 区域文件托管在 AWS。

## 8. Route 53 Resolver 与混合 DNS

- **场景：** 你有一个本地数据中心（On-Premise）和一个 AWS VPC，通过 Direct Connect 或 VPN 连接。你希望本地服务器能解析 AWS 的域名，AWS 实例也能解析本地的域名。
- **Route 53 Resolver 入站端点（Inbound Endpoint）：**
    - 允许**本地**资源查询 Route 53 Resolver。
    - 把 DNS 查询从本地 → 转发到 AWS。
- **Route 53 Resolver 出站端点（Outbound Endpoint）：**
    - 允许 **AWS** 资源查询本地 DNS 服务器。
    - 把 DNS 查询从 AWS → 转发到本地（使用转发规则 Forwarding Rules）。

## 考试"陷阱"总结

| 关键词/场景 | 答案通常涉及…… |
| --- | --- |
| **根域名 / Zone Apex** | **Alias 记录**（不是 CNAME） |
| **主备灾难恢复（Active/Passive DR）** | **故障切换路由** |
| **最低延迟 / 最快** | **延迟路由** |
| **法律合规 / 国界限制** | **地理位置路由** |
| **流量偏移 / Bias** | **地理临近度路由**（Traffic Flow） |
| **80% 到 V1，20% 到 V2** | **加权路由** |
| **混合云 DNS** | **Route 53 Resolver**（入站/出站端点） |
| **健康检查私有 IP** | **CloudWatch 告警**类型健康检查 |


---

# 第 7 章：S3 基础（Simple Storage Service）

## 1. S3 概览

- **类型：** 对象存储（不是块存储，也不是文件存储）。
- **结构：**
    - **桶（Buckets）：** 顶层容器。名称必须**全球唯一**（在所有 AWS 区域/账户范围内）。
    - **对象（Objects）：** 存储在桶中的文件。
    - **键（Keys）：** 对象的完整路径（例如 `images/logo.png`）。
- **容量：** 总存储量无上限。单个对象大小从 0 字节到 **5 TB**。
- **持久性：** 标准类为 99.999999999%（11 个 9）。
- **一致性模型：** 所有 GET、PUT、LIST 操作都是强一致性（写后即可读，read-after-write consistency）。

## 2. 安全：IAM 策略 vs 桶策略

S3 的安全由"基于用户的策略"和"基于资源的策略"共同组成。

### IAM 策略（基于用户）

- 附加到 IAM 用户、组或角色上。
- 定义某个 IAM 主体能做什么（例如"用户 A 可以从桶 X 读取"）。
- 最适合你 AWS 账户内部的用户。

### S3 桶策略（基于资源）

- 直接附加到 S3 桶上。
- 定义_谁_能访问_这个特定的桶_。
- JSON 格式。关键元素：`Principal`（主体）、`Effect`（效果）、`Action`（操作）、`Resource`（资源）。
- **使用场景：**
    - 向公众开放桶的访问权限。
    - 授予跨账户访问权限。
    - 强制只允许上传加密的对象。

### 权限评估逻辑

- **策略取并集：** 最终权限是 IAM 权限和桶策略权限的并集。
- **显式拒绝（Explicit DENY）：** 永远覆盖允许（ALLOW）。
- **隐式拒绝（Implicit DENY）：** 没有被显式允许的，一律拒绝。

### 阻止公共访问（Block Public Access）

- 账户级或桶级的设置，能覆盖任何桶策略，防止意外的公开暴露。
- 除非你明确要托管一个公开的静态网站，否则都应该保持开启。

## 3. S3 静态网站托管

- S3 可以托管静态网站（HTML、CSS、JS、图片）。它不能运行服务端代码（PHP、Python 等）。
- **URL 格式：** `http://<桶名>.s3-website-<区域>.amazonaws.com`
- **配置要求：**
    1. 在属性中启用"静态网站托管"。
    2. 设置**索引文档**（例如 `index.html`）和**错误文档**（例如 `error.html`）。
    3. 关闭"阻止公共访问"。
    4. 添加一个**桶策略**，对 `Principal: "*"` 允许 `s3:GetObject`。
- **考试提示：** S3 网站端点**不支持** HTTPS。要用 HTTPS，必须在 S3 前面加 CloudFront。

## 4. S3 版本控制（Versioning）

- 在桶级别启用。一旦启用，只能暂停（suspend），不能彻底关闭。
- **功能：** 在同一个桶中保留一个对象的多个版本。
- **好处：**
    - 防止意外覆盖。
    - 防止误删除（删除时只会添加一个"删除标记 Delete Marker"，而不是真的移除文件）。
    - 轻松回滚到之前的版本。
- **MFA Delete：** 强制要求多因素认证才能更改版本控制状态或永久删除某个对象版本。（必须通过 CLI 启用，控制台不行）。

## 5. S3 复制（Replication）

要进行复制，源桶和目标桶都必须**启用版本控制**。

- **CRR（跨区域复制 Cross-Region Replication）：**
    - 复制到不同的 AWS 区域。
    - 场景：合规、降低全球用户的访问延迟、灾难恢复。

- **SRR（同区域复制 Same-Region Replication）：**
    - 复制到同一个 AWS 区域。
    - 场景：日志聚合、生产环境与测试环境之间的复制。

- **要点：**
    - 启用后只复制_新_对象。（已有对象需要用 "S3 批量复制 Batch Replication"）。
    - 删除标记是否复制？可配置。
    - 永久删除？**不会**被复制（这是一种安全保护）。
    - 需要给 S3 一个 IAM 角色来执行复制。
    - **不支持链式复制：** 如果桶 A 复制到桶 B，桶 B 复制到桶 C，那么上传到桶 A 的对象**不会**被复制到桶 C。

## 6. S3 存储类别

### S3 Standard（标准 - 通用）

- **可用性：** 99.99%
- 用于频繁访问的数据。
- 低延迟、高吞吐量。
- 可承受 2 个设施同时故障。
- **使用场景：** 大数据分析、移动和游戏应用、内容分发。

### S3 不频繁访问类别（Infrequent Access）

针对访问频率较低、但需要时要能快速取回的数据。成本低于 S3 Standard。

- **S3 Standard-IA（标准-不频繁访问）**
    - **可用性：** 99.9%
    - **场景：** 灾难恢复、备份。
- **S3 One Zone-IA（单区-不频繁访问）**
    - **持久性：** 在单个 AZ 内高持久性（99.999999999%）；但 **AZ 被摧毁时数据会丢失**。
    - **可用性：** 99.5%
    - **场景：** 存储本地数据的二级备份副本，或可以重新生成的数据。

### Amazon S3 Glacier 存储类别（归档）

用于归档/备份的低成本对象存储。**计费：** 存储费 + 对象取回费。

- **S3 Glacier Instant Retrieval（即时取回）**
    - 毫秒级取回，适合每季度访问一次的数据。
    - 最短存储时长 90 天。
- **S3 Glacier Flexible Retrieval（灵活取回，原名 S3 Glacier）**
    - **取回选项：** 加急 Expedited（1-5 分钟）、标准 Standard（3-5 小时）、批量 Bulk（5-12 小时，免费）。
    - 最短存储时长 90 天。
- **S3 Glacier Deep Archive（深度归档）** - 用于超长期存储
    - **取回选项：** 标准（12 小时）、批量（48 小时）。
    - 最短存储时长 180 天。

### S3 Intelligent-Tiering（智能分层）

- 收取少量的每月监控和自动分层费用。
- 根据使用情况自动在各访问层之间移动对象。
- **S3 Intelligent-Tiering 没有取回费用。**
- **层级：**
    - **频繁访问层（自动）：** 默认层。
    - **不频繁访问层（自动）：** 30 天未访问的对象。
    - **归档即时访问层（自动）：** 90 天未访问的对象。
    - **归档访问层（可选）：** 可配置为 90 天到 700+ 天。
    - **深度归档访问层（可选）：** 可配置为 180 天到 700+ 天。

## 7. S3 Express One Zone

- **最新的存储类别：** 高性能、单可用区。
- **结构：** 使用**目录桶（Directory Buckets）**（与标准的通用桶不同）。
- **性能：** 提供个位数毫秒级延迟。
- **成本：** 请求费用比 S3 Standard 低 50%。
- **使用场景：** 频繁访问的数据、AI/ML 训练、金融建模、媒体处理。
- **持久性：** 数据只存储在单个 AZ；如果该 AZ 被摧毁，数据会丢失（与区域级类别不同）。


---

# 第 8 章：S3 进阶（Advanced Features）

## 1. S3 生命周期规则（Lifecycle Rules）

自动在存储类别之间移动对象或删除对象，以优化成本。

### **操作类型**

- **转换操作（Transition Actions）：** 把对象移到更便宜的存储类别（例如 Standard → Standard-IA → Glacier）。
- **过期操作（Expiration Actions）：** 在指定时间后删除对象（例如日志文件 365 天后删除）。
- **未完成的分段上传：** 可以配置规则清理未完成的分段上传（multipart upload），节省存储费用。

### **规则与限制**

- **128KB 下限：** 对象必须大于 128KB 才值得转到 Standard-IA 或 One Zone-IA（否则按 128KB 计费）。
- **30 天下限：** 对象通常必须在 Standard 中存放 30 天后，才能转换到 IA 类别（Standard-IA / One Zone-IA）。

### **S3 Analytics - 存储类别分析**

- **考试提示：** "我怎么知道_什么时候_该把对象移到 Standard-IA？"
- 先用 **S3 Analytics**。它分析存储访问模式，生成报告，建议最优的生命周期规则。
- 它专门帮助识别适合转到 **Standard-IA** 或 **One Zone-IA** 的对象。

## 2. S3 事件通知（Event Notifications）

对 S3 桶内发生的事件做出自动响应。

- **触发事件：** `ObjectCreated`（对象创建）、`ObjectRemoved`（对象删除）、`ObjectRestore`（对象恢复）、`Replication`（复制）。
- **目标（Destinations）：**
    1. **SNS**（简单通知服务）- 发送告警/邮件。
    2. **SQS**（简单队列服务）- 解耦，稍后处理事件。
    3. **Lambda** - 立即触发代码（例如上传图片时自动生成缩略图）。
    4. **EventBridge** - 复杂的过滤和路由，可以送到许多其他 AWS 服务。
- **版本控制：** 如果开启了版本控制，确保通知针对特定对象版本也已启用。

## 3. S3 性能优化

S3 会自动扩展，但有一些专门的功能可以进一步提高吞吐量。

### **基准性能**

- 每个前缀（prefix）每秒 **3,500** 次 PUT/COPY/POST/DELETE 请求。
- 每个前缀每秒 **5,500** 次 GET/HEAD 请求。
- _注意：_ 可以通过使用不同的"前缀"（文件夹路径）来并行化请求，从而获得更高的总吞吐量。

### **高性能功能**

1. **分段上传（Multipart Upload）：**
    - 把文件分成多个部分并行上传。
    - 文件 > 100 MB 时**推荐**使用。
    - 文件 > 5 GB 时**必须**使用。
2. **S3 传输加速（Transfer Acceleration）：**
    - 利用 AWS **边缘节点（Edge Locations）**加速通过公网的上传。
    - 数据先进入边缘节点，然后通过 AWS 高速内部网络传到桶。
3. **字节范围获取（Byte-Range Fetches）：**
    - 把文件分段并行下载（加速下载）。
    - 也可以只获取文件的特定部分（例如视频文件的头部）。
4. **S3 Select：**
    - 使用 **SQL 表达式**从对象中只取回需要的那部分数据。
    - **在服务端过滤数据**，大幅减少传输的数据量、降低成本。
    - **支持** CSV、JSON 和 Parquet 文件（即使用 GZIP/BZIP2 压缩过也支持）。

## 4. S3 批量操作（Batch Operations）

托管的解决方案，可以对跨桶的数十亿个对象执行同一个操作。

### **使用场景**

- **加密未加密的对象：** 对数百万个已存在的历史对象补加 SSE-KMS 或 SSE-S3 加密。
- **复制对象：** 桶之间批量复制（例如灾难恢复或迁移）。
- **从 Glacier 恢复：** 批量恢复归档数据。
- **替换标签集：** 更新成本分摊标签。
- **调用 Lambda：** 对列表中的每个对象运行自定义逻辑。

### **工作流程（S3 Inventory + Athena）**

批量操作需要一个对象**清单**才能工作。

1. **S3 Inventory（清单）：** 启用后每天生成 CSV/ORC/Parquet 报告，列出所有对象及其元数据（加密状态、大小等）。
2. **Athena：** 用 SQL 查询 S3 Inventory 文件来**筛选**清单（例如："选出所有加密状态为'无服务端加密'的对象"）。
3. **批量作业：** 把筛选后的清单交给 S3 Batch Operations 执行操作（例如加密）。

## 5. S3 Storage Lens

对整个组织的对象存储使用情况和活动提供"一站式（Single pane of glass）"可视化。

### **免费指标 vs 付费指标**

Storage Lens 的指标分两档：

1. **免费指标：** 默认提供。
    - **汇总：** 总存储量、对象数量、平均对象大小。
    - **成本效率：** 识别未完成的分段上传、旧版本、未压缩文件。
    - **数据保护：** 桶加密状态、复制状态。

2. **高级指标（付费）：** 增加"活动"和"状态码"洞察。
    - **活动指标：** 跟踪请求次数（GET、PUT、HEAD）、上传/下载字节数。
    - **详细状态码指标：** 跟踪 HTTP 错误，如 **403 Forbidden**（拒绝访问）、**404 Not Found**、**5xx** 服务器错误。
    - **访问管理指标：** 跟踪对象所有权设置和加密数量。
    - **性能指标：** 跟踪传输加速的使用情况和请求延迟。
    - **事件指标：** 跟踪 S3 事件通知的投递率。

## 6. S3 请求者付费（Requester Pays）

把数据传输和请求的费用从桶所有者转移给请求者。

### **核心概念**

- **谁付什么钱？**
    - **请求者：** 支付**数据传输**费用和**请求**费用（GET、PUT、LIST）。
    - **桶所有者：** 支付**存储**费用。

### **考试要点**

1. **必须经过认证：** 请求者付费**不能**用于匿名（公开）用户。请求者_必须_是经过认证的 AWS 用户，这样 AWS 才知道向谁收费。
2. **请求头要求：** 请求者必须在 API 调用中带上特定的请求头，明确同意付费。
    - **请求头：** `x-amz-request-payer`
    - **CLI 参数：** `--request-payer requester`
3. **失败场景：** 如果用户访问桶时_没带_这个请求头，即使权限齐全，请求也会以 **403 Access Denied** 失败。

# 第 9 章：S3 安全（S3 Security）

本章涵盖 SAA-C03 考试中的 S3 安全、访问管理与高级功能。

## 1. S3 加密

S3 提供多种静态加密方式。必须理解它们的区别，尤其是密钥管理和审计权限方面。

### 服务端加密（Server-Side Encryption，SSE）

- **SSE-S3（S3 托管密钥）：**
    - **密钥：** 由 Amazon S3 管理。
    - **算法：** AES-256。
    - **轮换：** S3 自动处理。
    - **成本：** 密钥无额外费用。
    - **考试提示：** 没有指定加密方式时的默认选项。实现最简单。
- **SSE-KMS（KMS 托管密钥）：**
    - **密钥：** 由 AWS 密钥管理服务（KMS）管理。
    - **特性：** 可以控制密钥轮换，并通过 CloudTrail 获得**审计跟踪**（谁在什么时候用了密钥）。
    - **限制：** 访问对象会消耗 KMS API 配额（例如 `GenerateDataKey`、`Decrypt`）。大规模访问可能触发 KMS 限流错误。
    - **考试提示：** 如果场景要求密钥使用的**可审计性**或**客户自管**的密钥轮换策略，选这个。
- **SSE-C（客户自带密钥）：**
    - **密钥：** 由你（客户）管理和保存。S3 **不存储**加密密钥。
    - **流程：** 每次请求（上传/下载）都要附带密钥。S3 执行加解密后立即丢弃密钥。
    - **要求：** 必须使用 HTTPS。
    - **考试提示：** 如果公司安全政策规定必须由自己独家持有和管理密钥、但仍希望由 AWS 执行加密运算，用这个。
- **DSSE-KMS（双层服务端加密）：**
    - 在服务端使用 KMS 密钥施加两层加密。
    - 用于严格的合规要求（例如 NSA CNSSP 15）。

### 客户端加密（Client-Side Encryption）

- 你在上传到 S3 **之前**就把数据加密好。S3 永远看不到未加密的数据和密钥。

## 2. S3 默认加密

- **行为：** 现在 S3 桶会自动启用默认加密（SSE-S3）。
- **配置：** 可以把默认改成 SSE-KMS。
- **效果：** 新对象会自动加密。已有对象保持原样，直到被覆盖或复制。

### 默认加密 vs 桶策略强制加密

必须理解"_设置默认值_"和"_通过策略强制_"的区别。

- **默认加密（安全网）：**
    - **工作原理：** 如果用户上传对象时**没有**指定任何加密头，S3 自动应用桶的默认加密设置。上传**成功**。
    - **使用场景：** 确保任何对象都不会以明文存储，同时不破坏那些忘记请求加密的应用。
- **桶策略（守门员）：**
    - **工作原理：** 编写一条策略，明确**拒绝（Deny）**不带特定加密头（例如 `x-amz-server-side-encryption`）的 `PutObject` 请求。
    - **结果：** 用户尝试上传未加密对象时，请求会以 `403 Access Denied` **失败**。
    - **使用场景：** 严格强制合规，要求_客户端_必须明确知道并主动请求特定的加密方式（例如强制使用某个特定的 SSE-KMS 密钥）。

## 3. S3 CORS（跨域资源共享）

- **问题：** 如果请求来自不同的域名（例如 `www.example.com`），浏览器会阻止对 S3 桶域名（例如 `mybucket.s3.amazonaws.com`）的请求。
- **解决：** 在 S3 桶上启用 CORS，允许特定来源访问资源。
- **配置：**
    - **AllowedOrigins（允许的来源）：** 例如 `https://www.example.com`
    - **AllowedMethods（允许的方法）：** 例如 `GET`、`PUT`、`POST`
    - **AllowedHeaders（允许的请求头）：** 例如 `Authorization`
- **考试提示：** 如果 S3 托管的静态网站（或另一个域名下的用户）访问其他桶的资源时遇到 `403 Forbidden` 或"网络错误"，**检查 CORS 配置**。

## 4. S3 MFA Delete

- **目的：** 强制使用多因素认证（MFA）才能删除对象版本或更改桶的版本控制状态。
- **要求：**
    1. **版本控制：** 桶必须开启版本控制。
    2. **Root 用户：** 只有 **Root 账户**（桶所有者）能启用/禁用 MFA Delete。
    3. **仅限 CLI/API：** 不能通过 AWS 管理控制台启用。
- **防范：**
    - 永久删除某个对象版本。
    - 暂停桶的版本控制。
- **考试提示：** 如果题目问如何防止_有特权的_管理员误删，找 **MFA Delete**。记住它需要 **Root** 用户来设置。

## 5. S3 访问日志 vs CloudTrail

- **S3 服务器访问日志（Server Access Logs）：**
    - **范围：** 记录对桶的请求详情（请求者、桶名、请求时间、操作、响应状态等）。
    - **目的地：** 另一个 S3 桶。
    - **延迟：** 投递可能延迟（数小时）。
    - **最佳实践：** **不要**把目标桶设成源桶本身（会造成日志无限循环）。
- **CloudTrail 数据事件（Data Events）：**
    - **范围：** 记录 API 活动（例如 `GetObject`、`PutObject`）。
    - **成本：** 对象级日志需要额外付费。
    - **性能：** 投递比 S3 访问日志快。
- **考试提示：** 要做特定访问的**审计**（谁访问了这个文件？），CloudTrail 通常是更现代的答案。要做**详细的访问分析**或排查服务器响应问题，用访问日志。

## 6. S3 预签名 URL（Pre-signed URLs）

- **使用场景：** 在不改桶策略、不把对象公开的前提下，授予对私有对象的临时访问权限。
- **机制：** 通过 AWS CLI 或 SDK 生成。
- **权限：** URL 继承**生成它的用户/角色**的权限。
- **场景举例：**
    - 允许付费会员下载一个视频文件。
    - 允许用户在没有 AWS 凭证的情况下直接向 S3 上传文件（预签名 PUT）。
- **考试提示：** 如果 URL 的创建者失去了权限（例如用户被删除），即使 URL 还没过期，也会立即失效。

## 7. Glacier Vault Lock 与 S3 Object Lock（WORM）

两者都实现 **WORM（Write Once, Read Many，一次写入多次读取）**模型。

### S3 Object Lock（对象锁定）

- 作用于 S3 中的单个对象。需要开启版本控制。
- **保留模式：**
    1. **Governance（治理）模式：** 普通用户不能覆盖/删除，但拥有特殊权限（`s3:BypassGovernanceRetention`）的用户可以。适合测试或较宽松的合规。
    2. **Compliance（合规）模式：** **严格。** 在保留期内，任何人（包括 **Root 用户**）都不能删除或覆盖该对象。
- **法律保留（Legal Hold）：** 独立于保留期。无限期阻止删除，直到被明确移除。

### Glacier Vault Lock（保管库锁定）

- 作用于整个 Glacier 保管库（Vault）。
- **流程：** 创建 Vault Lock 策略 → 发起锁定 → 有 24 小时验证时间 → 完成锁定。
- **考试提示：** 一旦锁定完成（24 小时窗口过后），策略**不能**再更改或删除。用于严格的长期归档合规（例如美国证监会 SEC Rule 17a-4）。

## 8. S3 访问点（Access Points）

- **问题：** 一个被 100 个团队/应用共用的桶，只靠一个巨大的桶策略来管理，既复杂又危险。
- **解决：** 为桶创建多个**访问点**。
- **特性：**
    - 每个访问点有自己的**策略**。
    - 每个访问点有唯一的 DNS 主机名。
    - 可以限制为只能从特定 **VPC** 访问（VPC 来源）。
- **考试提示：** 用访问点把巨大的桶策略**拆解**成更小的、按应用划分的策略。

## 9. S3 Object Lambda

- **概念：** 在 S3 的 `GET` 请求上挂载你自己的代码（Lambda 函数），在数据返回给应用之前对其进行修改。
- **工作流：** 应用 → S3 Object Lambda 访问点 → Lambda 函数（处理数据）→ S3 桶。
- **使用场景：**
    - **脱敏（Redaction）：** 为特定用户从分析数据中去除 PII（个人身份信息）。
    - **格式转换：** 实时把 XML 转成 JSON。
    - **加水印：** 下载图片时动态添加水印。
- **考试提示：** 应用不需要改代码，只需把访问目标从桶换成 Object Lambda 访问点即可。


---

# 第 10 章：CloudFront 与 Global Accelerator

## 1. CloudFront 概览

- **是什么？** 一个全球内容分发网络（CDN），以低延迟、高传输速度向全球用户分发数据、视频、应用和 API。
- **核心概念：** 内容缓存在离用户最近的**边缘节点（Edge Locations，接入点）**。
- **好处：**
    - 提升读取性能（内容离用户更近）。
    - 减轻源站（Origin）服务器的负载。
    - **DDoS 防护：** 与 AWS Shield（标准版免费）和 AWS WAF（Web 应用防火墙）集成。
- **源站（Origins）：**
    - **S3 桶：** 分发文件并在边缘缓存。
    - **自定义源站（HTTP）：** 应用负载均衡器（ALB）、EC2 实例、S3 静态网站（已启用静态托管），或任何非 AWS 的 HTTP 后端。

## 2. CloudFront - S3 安全（OAC vs OAI）

用 S3 做源站时，必须防止用户绕过 CloudFront 直接访问 S3 桶（那样会绕过 CloudFront 的计费、WAF 和各种限制）。

- **解决方案：** 使用**源访问控制（Origin Access Control，OAC）**（或旧版的源访问身份 Origin Access Identity - OAI）来限制访问。
- **工作原理：**
    1. 在 CloudFront 中创建一个 OAC。
    2. 更新 **S3 桶策略**：拒绝公共访问，**只允许** CloudFront 服务主体（Service Principal）访问。

### OAC vs OAI（考试重点）

|特性|源访问身份（OAI）|源访问控制（OAC）|
|---|---|---|
|**状态**|旧版（已弃用）|**推荐（现代方案）**|
|**安全协议**|较旧的协议|**SigV4**（更强的安全性）|
|**SSE-KMS 支持**|不支持（无法分发 KMS 加密的文件）|**支持**（可用于 SSE-KMS 加密的桶）|
|**HTTP 方法**|仅 GET、HEAD|全部（GET、PUT、POST、DELETE 等）|
|**区域支持**|新区域支持有限|所有 AWS 区域|

> **考试提示：** 如果场景提到"KMS 加密的 S3 对象"或"通过 CloudFront 上传（PUT/POST）"并问安全方案，答案是 **OAC**。

## 3. CloudFront - 以 ALB/EC2 为源站

用 EC2 实例或 ALB 做源站时，CloudFront 通过 HTTP/HTTPS 协议连接它。

- **安全挑战：** 用户可能绕过 CloudFront 直接访问 ALB/EC2，从而绕过 WAF 规则或缓存逻辑。
- **如何限制访问（考试常考）：**
    1. **安全组（网络层）：** 配置 EC2 或 ALB 的安全组，**只**允许来自 CloudFront IP 段的入站流量。
        - _技巧：_ 在安全组的来源中使用 CloudFront 的 **AWS 托管前缀列表（Managed Prefix List）**。
    2. **自定义请求头（应用层）：**
        - 配置 CloudFront 在发往源站的每个请求上添加一个**自定义 HTTP 头**（例如 `X-Custom-Secret`）。
        - 配置 ALB 监听器规则（或后端应用），**只**处理带有这个特定请求头和值的请求。
        - 没有这个头的请求（直接访问）会被丢弃或返回 403 Forbidden。

## 4. CloudFront - 地理限制（Geo Restriction）

- **目的：** 根据观看者的地理位置限制对内容的访问。
- **机制：** 使用第三方的 Geo-IP 数据库判断用户所在国家。
- **模式：**
    - **白名单（Whitelist）：** 只允许特定国家的用户访问。
    - **黑名单（Blacklist）：** 阻止特定国家的用户访问。
- **使用场景：** 遵守版权法（例如体育赛事直播只允许在特定地区观看）。
- **注意：** 这是在 _CloudFront 边缘_ 做的访问限制。（如果要更精细的控制，比如限制特定_文件_，应该用签名 URL / 签名 Cookie，而不是地理限制）。

## 5. CloudFront 安全（AWS WAF 与 Shield）

CloudFront 与 AWS WAF 无缝集成，在边缘提供第 7 层防护。

- **AWS WAF（Web 应用防火墙）：**
    - **防护对象：** SQL 注入、跨站脚本（XSS）以及常见的 Web 攻击。
    - **作用方式：** 创建一个 **Web ACL** 并关联到 CloudFront 分发（全球级资源）。
    - **WAF 地理匹配 vs CloudFront 地理限制：**
        - **CloudFront 地理限制：** "全有或全无"。对_整个_分发屏蔽一个国家。
        - **WAF 地理匹配（Geo Match）：** 精细控制。可以只对特定路径（例如 `/admin`）屏蔽某个国家，或与其他规则组合使用。
- **AWS Shield：**
    - **Shield Standard：** 所有 CloudFront 客户自动启用（免费）。防护常见的 3/4 层 DDoS 攻击。
    - **Shield Advanced：** 付费服务（每月约 3000 美元）。针对复杂攻击提供专家支持、费用保护和高级报告。

## 6. CloudFront - 价格等级（Price Classes）

CloudFront 的价格因边缘节点位置而异。为了省钱，可以排除昂贵的区域。

- **Price Class All（默认）：**
    - 使用全球**所有**边缘节点。
    - **优点：** 全球性能最佳。
    - **缺点：** 最贵。
- **Price Class 200：**
    - 使用大部分区域，但**排除最贵的**那些。
    - _通常排除：_ 南美洲、澳大利亚。
- **Price Class 100：**
    - **只使用最便宜的**区域。
    - _包含：_ 美国、加拿大、欧洲。
    - **优点：** 最便宜。
    - **缺点：** 亚洲、南美等地用户延迟较高。

## 7. CloudFront - 缓存失效（Cache Invalidation）

- **问题：** 当你更新了源站上的文件（例如 `logo.png`），CloudFront 在 TTL（存活时间）到期前会继续提供旧的缓存版本。
- **解决（失效 Invalidation）：** 可以强制 CloudFront 立即从缓存中移除对象。
- **用法：** 指定要失效的路径（例如 `/images/logo.png` 或 `/images/*`）。
- **成本：** 每月前 1,000 个失效路径免费，之后按条收费。
- **更好的替代方案（版本化文件名）：** 与其做失效操作，不如给文件名加版本（例如 `logo_v1.png`、`logo_v2.png`）。这样客户端会立即获取新文件，而且零成本。

## 8. AWS Global Accelerator - 概览

- **是什么？** 一个网络服务，为本地和全球用户提升应用的可用性和性能。
- **工作原理：**
    - 流量在离用户最近的边缘节点进入 AWS 网络。
    - 之后走 **AWS 内部全球骨干网**（而不是公共互联网）到达你的应用端点。
- **关键特性：**
    - **2 个 Anycast 静态 IP：** 你会获得 2 个全球通用的静态 IP 地址。（非常适合 IP 白名单场景）。
    - **支持非 HTTP 协议：** 支持 TCP 和 UDP（CloudFront 只支持 HTTP/S）。
    - **不缓存：** 它_不_缓存内容，只优化网络路径。
    - **快速区域故障切换：** 如果一个区域宕机，Global Accelerator 会在不到 1 分钟内把流量重定向到另一个区域的健康端点（基于 DNS 的故障切换因为 TTL 的原因可能慢得多）。
- **端点类型：** 网络负载均衡器（NLB）、应用负载均衡器（ALB）、EC2 实例或弹性 IP。

```
[ 全球各地客户端 (Clients) ]
        │
        │ 1. 访问固定的 2 个 Anycast 静态 IP (无需 DNS 轮询解析延迟)
        ▼
┌───────────────────────────────────────────────────────────────┐
│                    AWS 边缘站点 (Edge Location)               │
│                                                               │
│  [ Global Accelerator 监听器 (Listener: TCP/UDP 80/443 等) ]   │
└──────────────────────────────┬────────────────────────────────┘
                               │
                               │ 2. 流量走 AWS 全球私有骨干网 (低延迟、抗抖动)
                               ▼
┌───────────────────────────────────────────────────────────────┐
│                     AWS Global Backbone Network               │
└──────────────┬───────────────────────────────┬────────────────┘
               │                               │
               │ (按延迟/权重路由)               │ (跨 Region 容灾/分流)
               ▼                               ▼
┌──────────────────────────────┐ ┌──────────────────────────────┐
│ AWS Region A (如 us-east-1)  │ │ AWS Region B (如 eu-west-1)  │
│                              │ │                              │
│ ┌──────────────────────────┐ │ │ ┌──────────────────────────┐ │
│ │ Endpoint Group (Region A)│ │ │ │ Endpoint Group (Region B)│ │
│ │ 流量拨盘 (Traffic Dial): │ │ │ │ 流量拨盘 (Traffic Dial): │ │
│ │ 100%                     │ │ │ │ 100% (备用/分流)         │ │
│ └────────────┬─────────────┘ │ │ └────────────┬─────────────┘ │
│              │               │ │              │               │
│     ┌────────┴────────┐      │ │     ┌────────┴────────┐      │
│     ▼                 ▼      │ │     ▼                 ▼      │
│ ┌──────────┐    ┌──────────┐ │ │ ┌──────────┐    ┌──────────┐ │
│ │ 端点 1   │    │ 端点 2   │ │ │ │ 端点 1   │    │ 端点 2   │ │
│ │ (ALB/NLB)│    │ (EC2/EIP)│ │ │ │ (ALB/NLB)│    │ (EC2/EIP)│ │
│ └──────────┘    └──────────┘ │ │ └──────────┘    └──────────┘ │
└──────────────────────────────┘ └──────────────────────────────┘
```



### 总结对比：CloudFront vs Global Accelerator

| 特性 | Amazon CloudFront | AWS Global Accelerator |
| --- | --- | --- |
| **主要目标** | 内容分发与缓存 | 网络性能与路径优化 |
| **协议** | HTTP / HTTPS | TCP / UDP（也支持 HTTP/S） |
| **缓存** | **有**（边缘缓存） | **无**（透传） |
| **IP 地址** | 动态（会变） | **2 个静态 Anycast IP** |
| **最适合** | 静态内容（图片/视频）、动态网站 | 游戏（UDP）、物联网（MQTT）、VoIP、HTTP API |
| **网络** | AWS 边缘网络 | AWS 全球骨干网 |


---

# 第 11 章：AWS 存储扩展（Storage Extras）

## 1. AWS Snow 家族概览

Snow 家族用于**离线**数据迁移和边缘计算。当网络容量不足以传输数据量时（例如"通过互联网传输要花好几年"），它就是答案。

### 设备对比

| 设备 | 可用容量 | 关键特性 | 考试场景 |
| --- | --- | --- | --- |
| **Snowcone** | 8 TB（HDD）或 14 TB（SSD） | 便携、坚固、可用电池供电。能装进背包。 | 从恶劣环境（沙漠、油井平台）做小规模数据传输（例如 5TB），或单个物联网采集点。 |
| **Snowball Edge 存储优化型** | 80 TB | 大存储容量。40 个 vCPU。 | 大规模数据迁移（PB 级）。替代物理磁带库。 |
| **Snowball Edge 计算优化型** | 42 TB | 更强的计算能力（52 个 vCPU，可选 GPU）。 | 在把数据送往 AWS _之前_做边缘处理（ML 推理、视频分析）。 |
| **Snowmobile** | 100 PB | 集装箱卡车。 | EB（艾字节）级迁移（例如整个数据中心关停搬迁）。 |

**考试关键规则：** 如果通过网络传输需要**超过一周**，就考虑 Snowball。

## 2. 架构：Snowball 导入 Glacier

常见考题：把 PB 级的归档数据从本地最省钱地迁移到冷存储（Glacier）。

### "生命周期"模式（经典）

1. **本地：** 把数据装载到 Snowball Edge。
2. **运输：** 把设备寄给 AWS。
3. **导入：** AWS 把数据导入到 **S3 Standard** 桶。
4. **转换：** 使用 **S3 生命周期策略**把对象立即（0 天）转换到 S3 Glacier Flexible Retrieval 或 Deep Archive。
    - _缺点：_ 每个对象都要付一笔转换费。

### "直接导入"模式（优化版）

Snowball Edge 现在支持把数据**直接**导入 S3 Glacier 存储类别。

- **做法：** 在控制台创建任务时，把目标存储类别选为 **Glacier Flexible Retrieval** 或 **Glacier Deep Archive**。
- **好处：** 避免 S3 生命周期转换费用。对于归档迁移，这通常就是"最省钱"的答案。

## 3. Amazon FSx

FSx 提供全托管的第三方文件系统。你需要把"操作系统"或"工作负载"与对应的 FSx 类型匹配起来。

|服务|协议|操作系统/兼容性|考试主要场景|
|---|---|---|---|
|**FSx for Windows File Server**|SMB|**Windows**|Windows 应用"直接搬迁（Lift and shift）"。Windows 服务器的共享存储。**Active Directory** 集成。分布式文件系统（DFS）。|
|**FSx for Lustre**|POSIX|**Linux**|**HPC**（高性能计算）、机器学习、视频处理。"高速并行文件系统"。|
|**FSx for NetApp ONTAP**|NFS、SMB、iSCSI|Linux 和 Windows|迁移本地 **NetApp** 工作负载。支持去重、压缩和快照。|
|**FSx for OpenZFS**|NFS|Linux|迁移 ZFS 工作负载。高性能（100 万 IOPS）。|

### FSx for Lustre 部署类型（关键）

考试经常让你根据数据持久性需求和任务时长在这两种模式之间选择。

|特性|**Scratch（临时）文件系统**|**Persistent（持久）文件系统**|
|---|---|---|
|**使用场景**|临时存储、短期处理（数周/数月）。|长期存储、对可用性敏感。|
|**持久性**|**不做副本。** 服务器故障即数据丢失。|在可用区（AZ）内**有副本**。服务器故障可自愈。|
|**性能**|更高的突发吞吐量（最高达基准的 6 倍）。|稳定的基准性能。|
|**成本**|更低。|更高。|
|**考试触发词**|"临时处理"、"成本优化"、"数据可以从 S3 重新生成"。|"高可用"、"长期存储"、"数据不易重新生成"。|

### FSx for Lustre 与 S3 集成

- 可以把一个 FSx for Lustre 文件系统关联到一个 S3 桶。
- S3 对象在文件系统中以文件形式呈现。
- **工作流：**
    1. 启动一个关联 S3 的 **Scratch** 型 FSx for Lustre 文件系统。
    2. 高速处理数据。
    3. 把结果写回 S3。
    4. **删除** FSx 文件系统以省钱。

## 4. AWS Storage Gateway 概览

混合存储服务，让本地应用能使用 AWS 云存储。需要在本地安装一个虚拟或硬件设备（appliance）。

### 1. S3 File Gateway

- **接口：** NFS 或 SMB。
- **后端：** 把文件作为**对象**存储在 S3 桶中。
- **使用场景：** 用户主目录、数据湖、内容仓库——本地要以文件形式访问，但便宜地存在 S3。
- **缓存：** "最近访问"的数据保留在本地，实现低延迟。

### 2. FSx File Gateway

- **接口：** SMB。
- **后端：** Amazon **FSx for Windows File Server**。
- **使用场景：** 本地低延迟访问云端的 Windows 文件共享。FSx 的延伸。

### 3. Volume Gateway（块存储 / iSCSI）

向本地服务器提供磁盘卷（iSCSI）。

- **Stored Volumes（存储卷模式）：** 所有数据存储在**本地**（追求最快速度）。异步备份发送到 AWS（EBS 快照）。
    - _场景：_ 快速本地访问 + 异地备份。
- **Cached Volumes（缓存卷模式）：** 主数据在 **AWS（S3）**。本地只缓存频繁访问的数据。
    - _场景：_ 不买新硬件、低成本地扩展本地存储。

### 4. Tape Gateway（磁带网关）

- **接口：** iSCSI VTL（虚拟磁带库）。
- **后端：** S3（虚拟磁带）→ Glacier / Deep Archive。
- **使用场景：** 淘汰物理磁带基础设施。备份软件（例如 Veeam、Veritas）以为自己在写物理磁带，实际数据进了 AWS。

## 5. AWS Transfer Family

一个无服务器的服务，用于管理进出 AWS 存储的文件传输，无需自己管理基础设施。

- **支持的协议：**
    - **SFTP**（安全文件传输协议）- _考得最多。_
    - **FTPS**（基于 SSL 的 FTP）
    - **FTP**（不加密——除非要求"支持遗留系统"，否则很少是答案）
- **目标：** Amazon S3 或 Amazon EFS。
- **考试场景：** "第三方合作伙伴需要每周通过 SFTP 上传报告，我们不想为此管理 EC2 实例。" → **AWS Transfer Family**。

## 6. AWS DataSync - 概览

一个在线数据传输服务，用于自动化本地存储与 AWS 之间的数据搬运。

- **机制：** 需要在本地安装一个 **DataSync Agent**（虚拟机）。
- **协议：** 使用专有的加速网络协议（不是标准流量），可走互联网或 Direct Connect。
- **速度：** 比标准命令行工具（例如 AWS CLI 的 `cp`）快得多。
- **目标：** S3、EFS、FSx for Windows、FSx for Lustre。
- **调度：** 支持带宽限速和定时（例如"只在夜间传输"）。
- **考试场景：** "周末把一台大型本地 NFS 服务器迁移到 EFS"，或"把本地数据持续复制到 AWS"。

## 7. 所有 AWS 存储选项对比（速查表）

|场景|最佳服务|原因|
|---|---|---|
|**对象存储**（图片、视频、备份）|**S3**|无限容量、无服务器、可通过 Web 访问。|
|**块存储**（系统启动卷、数据库）|**EBS**|低延迟，附加到单台 EC2 实例。|
|**共享文件存储**（Linux EC2）|**EFS**|POSIX 兼容、自动扩展、跨多 AZ。|
|**共享文件存储**（Windows EC2）|**FSx for Windows**|原生 SMB、AD 集成。|
|**HPC / 并行文件系统**|**FSx for Lustre**|高吞吐量、可关联 S3。|
|**本地缓存 + 云端文件**|**S3 File Gateway**|本地 NFS/SMB 接口，数据存在 S3。|
|**本地缓存 + 云端块存储**|**Volume Gateway**|iSCSI 接口，把本地磁盘扩展到云。|
|**替代物理磁带**|**Tape Gateway**|VTL 接口，用 Glacier 做便宜归档。|
|**离线迁移**（PB 级）|**Snowball Edge**|不占用互联网带宽。|
|**在线迁移**（NFS/SMB 到 AWS）|**DataSync**|自动化、加速、基于代理。|
|**合作伙伴 SFTP 上传**|**Transfer Family**|全托管 SFTP 端点。|

## 8. 简化决策树（记忆辅助）

用这些简短的"如果-那么"规则快速答题。

### **规则 1：把数据搬到 AWS**

- **没有网络**（或"要传好几周"）？→ **Snowball**
- **有网络**（高速/Direct Connect）？→ **DataSync**
- **SFTP / FTP 协议？** → **Transfer Family**

### **规则 2：混合访问（Storage Gateway）**

- **需要看到"文件"？**（NFS/SMB）→ **S3 File Gateway**
- **需要一块"磁盘"？**（iSCSI）→ **Volume Gateway**
- **需要替代"磁带"？** → **Tape Gateway**

### **规则 3：FSx 关键词**

- **Windows / Active Directory / SMB** → **FSx for Windows**
- **Linux / HPC / "Scratch"** → **FSx for Lustre**
- **NetApp** → **FSx for ONTAP**

### **规则 4：Lustre 类型**

- **快 + 临时** → **Scratch**（无副本，更便宜）
- **长期 + 安全** → **Persistent**（有副本，可用性更高）


---

# 第 12 章：应用解耦（SQS、SNS、Kinesis、Amazon MQ）

## 1. 消息机制简介

- **同步通信：** 应用组件之间直接通信（例如服务 A 直接调用服务 B）。如果服务 B 挂了或变慢，服务 A 也跟着挂或变慢。
- **异步通信（解耦）：** 在组件之间放一个缓冲区（队列或主题）。
    - 服务 A（生产者）把消息发到缓冲区。
    - 服务 B（消费者）在准备好时从缓冲区读取。
- **好处：**
    - **可扩展性：** 各组件可以独立扩展。
    - **韧性：** 如果消费者宕机，消息会被暂存，不会丢失。

## 2. Amazon SQS - 标准队列概览

- **SQS（Simple Queue Service，简单队列服务）：** 全托管的消息队列服务。基于拉取（Pull）模式。
- **标准队列特性：**
    - **无限吞吐量：** 每秒支持几乎无限次的 API 调用。
    - **至少一次投递（At-Least-Once Delivery）：** 保证消息会被送达，但可能出现**重复**。
    - **尽力而为的排序（Best-Effort Ordering）：** 消息大体按顺序投递，但**不保证**严格顺序。
- **数据保留：** 默认 4 天，最长 14 天。
- **消息大小：** 最大 256KB（更大的消息可以用 SQS Extended Client 库 + S3）。

## 3. SQS - 消息可见性超时（Visibility Timeout）

- **概念：** 当一个消费者拉取到消息后，该消息对其他消费者变为"不可见"，避免被同时处理两次。此时消息还_没有_被删除。
- **默认超时：** 30 秒。
- **场景：** 如果处理时间超过了超时时间，消息会重新出现在队列里，被再次处理（造成重复处理）。
- **解决：** 如果处理很重，消费者应调用 `ChangeMessageVisibility` API 来延长时间。
- **死信队列（Dead Letter Queue，DLQ）：** 如果一条消息多次处理失败（超过最大接收次数阈值），会被送到 DLQ 以便排查。

## 4. SQS - 长轮询（Long Polling）

- **短轮询（默认）：** 即使队列是空的也立即返回（返回一个空响应）。因为 API 调用费用，可能开销不小。
- **长轮询：** 消费者等待（最长 20 秒）消息到达队列。
- **好处：**
    - 减少空响应的数量。
    - 降低延迟（消息一到就立即发给消费者）。
    - **降低成本**（更少的 API 调用）。
- **配置：** 可以在队列级别设置（`ReceiveMessageWaitTimeSeconds`）或在 API 调用级别设置（`WaitTimeSeconds`）。

## 5. SQS - FIFO 队列（先进先出）

- **使用场景：** 顺序至关重要、且不允许重复的场景（例如银行交易）。
- **关键特性：**
    - **先进先出投递：** 严格保持消息顺序。
    - **精确一次处理（Exactly-Once Processing）：** 无重复。
    - **吞吐量受限：** 不批量时 300 条/秒；批量时 3,000 条/秒。
- **去重 ID（Deduplication ID）：** 用于防止生产者发送重复消息。
- **消息组 ID（Message Group ID）：** 允许不同组并行处理（例如公交车 1 号、公交车 2 号），同时保证_组内_顺序。

## 6. SQS + 自动扩展组（ASG）

- **模式：** 基于负载的弹性扩展。
- **指标：** CloudWatch 指标 `ApproximateNumberOfMessagesVisible`（可见消息的近似数量）。
- **工作流：**
    1. 队列长度增加。
    2. CloudWatch 告警触发。
    3. ASG 扩容策略增加 EC2 实例，加快消息处理速度。
- **目标跟踪：** 最有效的扩展策略（例如"每台实例的积压保持在 1000 条"）。

## 7. Amazon SNS（简单通知服务）

- **模式：** 发布/订阅（Pub/Sub）。基于推送（Push）。
- **生产者：** 把一条消息发送到一个 SNS **主题（Topic）**。
- **订阅者：** 可以是 SQS、Lambda、HTTP/S 端点、电子邮件、短信、移动推送。
- **关键特性：**
    - 即时推送投递（无需轮询）。
    - **消息过滤：** 通过 JSON 策略让订阅者只接收消息的一个子集。
    - **加密：** 传输中和静态存储都有标准加密。

## 8. SNS + SQS - 扇出模式（Fan-Out Pattern）

- **问题：** 你想发一条消息（例如"新订单创建"），但要触发多个相互独立的动作（给用户发邮件、更新库存、通知反欺诈服务）。
- **解决：**
    1. 创建一个 SNS 主题。
    2. 创建多个 SQS 队列（库存一个、反欺诈一个，等等）。
    3. 让这些 SQS 队列**订阅** SNS 主题。
    4. 只向 SNS 发布_一次_消息。
- **结果：** SNS 把消息并行推送到所有 SQS 队列。各服务按自己的节奏从各自的队列消费。这既保证了**数据持久性**，又实现了**解耦**。

## 9. Amazon Kinesis Data Streams

- **目的：** 大规模摄取实时流数据（日志、指标、物联网、点击流），用于大数据分析。
- **架构：**
    - **分片（Shards）：** 流被分成多个分片。1 个分片 = 1MB/秒写入 或 1000 条记录/秒。
    - **保留期：** 默认 24 小时，最长 365 天。（和 SQS 不同，数据被消费后_不会_被删除，而是到期自动过期）。
    - **顺序：** _按分片_保证顺序。
- **容量模式：**
    - **预置（Provisioned）：** 你手动选择分片数量。
    - **按需（On-Demand）：** AWS 自动扩展分片。

## 10. Amazon Data Firehose（原名 Kinesis Firehose）

- **目的：** 全托管服务，用于把流数据**加载**到目的地。
- **目的地：** Amazon S3、Redshift、OpenSearch、Splunk、HTTP 端点。
- **性质：** 近实时（最低延迟 60 秒）。
- **特性：**
    - 自动扩展（不需要管理分片）。
    - 数据转换（通过 Lambda），支持格式转换（例如 JSON 转 Parquet）。
    - 压缩（GZIP、Snappy）。
- **考试提示：** 如果题目问"把流数据存到 S3/Redshift"，就想到 Firehose。

## 11. SQS vs SNS vs Kinesis（对比速查表）

|特性|SQS|SNS|Kinesis Data Streams|
|---|---|---|---|
|**模型**|拉取（轮询）|推送（发布/订阅）|拉取（实时流）|
|**实体**|队列|主题|流（分片）|
|**消费方式**|通常 1 个消费者，处理后删除消息|多个订阅者各收到一份副本|多个消费者可以读同一条流（可重放）|
|**持久性**|有（成功处理后删除）|无（未送达则丢失*）|有（保留期到后过期）|
|**使用场景**|任务解耦、批处理|通知、扇出|实时分析、大数据、点击流|

_*注：SNS 对投递失败可以用 DLQ，但不像 Kinesis 那样保留历史。_

## 12. Amazon MQ

- **是什么？** 托管的 Apache ActiveMQ 和 RabbitMQ 服务。
- **什么时候用（考试场景）：**
    - 你在把一个现有的本地应用迁移到 AWS。
    - 该应用使用标准协议，如 **MQTT、AMQP、STOMP、OpenWire**。
    - 你**不想**重写应用代码去适配 SQS/SNS 的 API。
- **架构：**
    - 运行在专用服务器上（不像 SQS/SNS 那样是无服务器的）。
    - 高可用需要 Multi-AZ 部署（主/备模式）。


---

# 第 13 章：AWS 上的容器（ECS、Fargate、ECR 与 EKS）

## 1. Docker 简介

_考试不要求你是 Docker 专家，但必须理解术语，才能看懂架构题。_

- **Docker：** 一个在被称为"容器"的隔离环境中开发、交付和运行应用的平台。
- **容器（Container）：** 一个轻量、独立、可执行的软件包，包含运行某个软件所需的一切（代码、运行时、系统工具、库）。
- **Dockerfile：** 一个文本文件，包含组装镜像所需的全部命令。
- **Docker 镜像（Image）：** 一个只读模板，带有创建 Docker 容器的指令。
- **注册表（Registry）：** 存放镜像的地方（例如 Docker Hub、**Amazon ECR**）。
- **考试核心概念：** 容器解决了"在我机器上明明能跑"的问题，让应用能在不同计算环境之间移植（开发 → 测试 → 生产）。

## 2. Amazon ECS（Elastic Container Service，弹性容器服务）

_ECS 是 AWS 自家的容器编排工具，与其他 AWS 服务深度集成。_

### 核心组件

1. **集群（Cluster）：** 任务或服务的逻辑分组。可以跑在 EC2 实例上，也可以跑在 Fargate（无服务器）上。
2. **任务定义（Task Definition）：** 一份蓝图（JSON 文件），描述容器的配置（镜像 URI、CPU/内存、端口、环境变量等）。类似 Docker Compose 文件。
3. **任务（Task）：** 任务定义的一个运行实例。
4. **服务（Service）：** 确保始终有指定数量的任务在运行。负责替换失败的任务，并把任务注册到负载均衡器。

### 计算选项与启动类型（考试重点）

现在托管任务主要有三种方式，从完全无服务器到完全自管理。

|特性|**Fargate**（无服务器）|**ECS Managed Instances**（混合）|**EC2 启动类型**（自管理）|
|---|---|---|---|
|**管理**|AWS 管理一切。无操作系统访问权。|AWS 负责创建和给 EC2 打补丁。你可以访问操作系统。|你自己管理 EC2（ASG、补丁、系统更新）。|
|**基础设施**|无服务器。|实例在你的账户里，由 AWS 管理。|实例在你的账户里，由你管理。|
|**能力**|有限（无 GPU，存储有特定限制）。|**完整的 EC2 功能**（GPU、本地 NVMe、竞价实例）。|**完整的 EC2 功能**（GPU、本地 NVMe、竞价实例）。|
|**使用场景**|低运维负担、批处理任务、一般 Web 应用。|需要特定实例（如 GPU 做 AI/ML）或高性能应用，但**不想承担运维负担**。|遗留合规要求、精细控制、或特定的成本优化策略。|

## 3. Amazon ECS - 自动扩展

_ECS 的扩展发生在两个层面：服务层（应用）和集群层（基础设施）。_

### 服务自动扩展（应用层）

使用 **Application Auto Scaling** 增减任务数量。

- **目标跟踪：** "把 CPU 利用率保持在 70%。"（最常用）。
- **阶梯扩展：** "告警超过 X 就加 2 个任务。"
- **计划扩展：** "早上 8 点扩到 10 个任务。"

### 集群自动扩展（基础设施层）

- **容量提供者（Capacity Providers）：** ECS 和自动扩展组（ASG）之间的纽带。
- **场景：** 你有 2 台 EC2 实例且已经跑满，此时再启动新任务会失败。
- **解决：** **ECS 容量提供者**检测到任务处于 `PROVISIONING`（等待资源）状态，就会触发 ASG 自动启动新的 EC2 实例。

## 4. Amazon ECS - 解决方案架构

### 模式 A：ALB 后面的微服务

- **架构：** ALB（公有子网）→ ECS 服务（私有子网）。
- **动态端口映射：** 使用 **EC2 启动类型**时，可以把主机端口设为 0。ALB 会自动检测容器被分配到的随机端口并把流量路由过去。这样同一个容器可以在一台 EC2 实例上跑多份副本。

### 模式 B：事件驱动的任务

- **架构：** S3 事件 → EventBridge → ECS 任务。
- **使用场景：** 图片上传到 S3 后，启动一个 ECS 任务把它缩放处理，处理完就关闭。
- **考试提示：** 这里用 **Fargate**。你不会想为了等一个事件而让一台 EC2 实例 7×24 运行付费。

### 模式 C：持久化存储

- **数据持久性：** 容器是临时的（停止后数据丢失）。
- **解决：** 把 **Amazon EFS**（弹性文件系统）挂载到 ECS 任务上。
- **为什么是 EFS？** 它是区域级的，可以被跨不同 AZ 的多个容器同时挂载。（EBS 做不到这一点）。

## 5. Amazon ECR（Elastic Container Registry，弹性容器注册表）

_托管的 Docker 注册表服务。存储、管理和部署容器镜像。_

- **私有 vs 公开：** 可以托管私有仓库（内部使用）或公开仓库（Amazon ECR Public Gallery）。
- **安全扫描：** "基础扫描"（推送时扫描）或"增强扫描"（借助 Amazon Inspector 持续扫描），用于发现镜像中的漏洞。
- **生命周期策略：** 自动清理旧镜像（例如"过期删除 30 天前的镜像"，或"只保留最近 10 个无标签镜像"）。
- **复制：** 支持**跨区域复制（CRR）**和跨账户复制。适用于灾难恢复，或为全球部署降低延迟。

## 6. Amazon EKS（Elastic Kubernetes Service，弹性 Kubernetes 服务）

_AWS 上的托管 Kubernetes。如果你已经在用 Kubernetes 或需要开源兼容性，就选它。_

- **架构：**
    - **控制平面（Control Plane）：** 由 AWS 管理（高可用，跨多个 AZ 运行）。
    - **数据平面（节点）：** 你的 Pod 运行的地方。可以是：
        1. **托管节点组（Managed Node Groups）：** AWS 管理 EC2 的创建和生命周期。
        2. **自管节点（Self-Managed Nodes）：** 你手动管理 EC2。
        3. **AWS Fargate：** 无服务器 Pod（不用管理 EC2）。
- **ECS vs EKS：**
    - **ECS：** 更简单、AWS 原生、与 IAM/ALB 集成更容易。
    - **EKS：** 学习曲线更陡，但可移植到其他云（Azure/GCP），生态庞大（Helm、Istio）。

## 7. AWS App Runner

_运行容器化 Web 应用或 API 最简单的方式。全托管。_

- **概念：** "这是我的源代码"或"这是我的容器镜像" → AWS 直接给你一个 HTTPS URL。
- **底层原理：** 它自动构建镜像、部署负载均衡器、设置自动扩展、管理加密证书。
- **使用场景：** 请求-响应型 Web 服务（API、前端）。
- **不适合：** 长时间运行的批处理任务（用 ECS/Batch）或需要复杂网络的有状态工作负载。

## 8. AWS App2Container（A2C）

_一个用于现代化遗留应用的迁移工具。_

- **功能：** 在你的服务器（本地或 EC2）上运行的 CLI 工具。它分析应用并把它们容器化。
- **支持的应用：**
    - **Java**（Linux）。
    - **.NET**（Windows IIS）。
- **工作流：**
    1. **发现（Discover）：** 识别正在运行的应用（IIS 网站、Java 进程）。
    2. **分析（Analyze）：** 生成分析报告。
    3. **提取（Extract）：** 生成 Dockerfile 并构建容器镜像。
    4. **部署（Deploy）：** 把镜像推送到 **ECR**，并为 **ECS** 或 **EKS** 生成部署产物（CloudFormation/YAML）。
- **考试要点：** 如果题目问如何**"以最小工作量把遗留的 Java/.NET 应用迁移到容器"**，答案是 **App2Container**。

## SAA-C03 速查表

| 场景 | 服务 |
| --- | --- |
| 简单的容器编排、深度 AWS 集成 | **ECS** |
| Kubernetes、开源兼容、多云 | **EKS** |
| 最简单地部署一个容器化 Web 应用并获得 URL | **App Runner** |
| 不管理服务器地运行容器 | **Fargate** |
| 用 GPU 跑容器但让 AWS 管理操作系统 | **ECS Managed Instances** |
| 存储和扫描 Docker 镜像 | **ECR** |
| 把 .NET/Java 迁移到容器 | **App2Container** |
| 容器需要跨 AZ 的持久化存储 | **ECS + EFS** |


---

# 第 14 章：从解决方案架构师视角看无服务器（Serverless）

## 1. 无服务器简介

- **定义：** 一种由云厂商管理基础设施（服务器、操作系统、补丁、扩展）的模式。你只需专注于代码。
- **关键特征：**
    - 无需创建或管理基础设施。
    - 自动扩展（可缩容到零 scale-to-zero）。
    - 按价值付费（代码运行或有请求时才付费）。
    - 默认高可用。

## 2. AWS Lambda 概览

- **概念：** 函数即服务（FaaS）。无需预置服务器就能运行代码。
- **触发方式（事件驱动）：**
    - **同步：** API Gateway、ALB、CLI、SDK（客户端等待响应）。
    - **异步：** S3、SNS、EventBridge（客户端收到 202 accepted；由 Lambda 负责重试/DLQ）。
    - **基于轮询（事件源映射）：** Kinesis、DynamoDB Streams、SQS（由 Lambda 主动轮询这些服务）。
- **计费：** 基于**请求次数**和**运行时长**（GB-秒：时长 × 分配的内存）。

## 3. Lambda 限制（考试重点）

- **执行超时：** 硬性上限 **15 分钟**（900 秒）。
    - _架构上的解法：_ 如果任务超过 15 分钟，用 **AWS Batch**、**Step Functions** 或 EC2。
- **内存：** 128 MB 到 **10 GB**。（CPU 随内存等比增加）。
- **部署包：**
    - 压缩后：**50 MB**。
    - 解压后：**250 MB**。
    - 容器镜像：**10 GB**。
- **载荷（Payload）大小：**
    - 同步（例如 API Gateway）：**6 MB**。
    - 异步：**256 KB**。
- **并发：** 每个区域默认 **1,000** 个并发执行（软限制，可申请提高）。

## 4. Lambda 并发

- **预留并发（Reserved Concurrency）：**
    - 为某个函数保证特定数量的并发实例。
    - **同时也是上限：** 函数不能超过这个数（适合保护下游数据库）。
- **预置并发（Provisioned Concurrency）：**
    - 预先初始化好执行环境。
    - **解决"冷启动（Cold Start）"**延迟问题。
    - 场景：高频交易应用、对低延迟有严格要求的交互式 API。

## 5. Lambda SnapStart

- **目标：** 大幅降低 **Java**、**Python** 和 **.NET** 函数的冷启动延迟（最多降低 90%）。
- **机制：** 对初始化完成的 microVM（内存/磁盘状态）做快照并缓存。
- **成本：** 该功能本身不额外收费（正常的时长/存储费照收）。
- **限制：** 不支持 Node.js 和 Ruby（以当前考试范围为准）。

## 6. Lambda@Edge vs CloudFront Functions

- **使用场景：** 在边缘（离用户更近的地方）运行逻辑，定制内容分发。

|特性|**CloudFront Functions**|**Lambda@Edge**|
|---|---|---|
|**运行时**|仅 JavaScript（ES5）|Node.js、Python|
|**执行时间**|亚毫秒级（< 1ms）|最长 5 秒（观众侧）/ 30 秒（源站侧）|
|**规模**|每秒数百万请求|每秒数千请求|
|**网络访问**|**无**（不能调用外部 API）|**有**（可以访问 DynamoDB、S3、外部 API）|
|**使用场景**|修改请求头、URL 重写、轻量的 JWT 校验、缓存键规范化。|复杂逻辑、图片缩放、访问数据库、高级认证、调用第三方 API。|

## 7. VPC 中的 Lambda

- **默认行为：** Lambda 运行在你的 VPC 之外（有互联网访问，但不能访问 RDS 之类的私有 VPC 资源）。
- **VPC 配置：** 要访问私有资源（RDS、ElastiCache），必须把 Lambda 附加到 VPC。
    - 需要：**私有子网**和**安全组**。
- **互联网访问要求：**
    - 放进 VPC 的 Lambda **会失去默认的互联网访问**。
    - 要恢复互联网访问（例如调用 DynamoDB 的公共端点或第三方 API），VPC 必须在公有子网里有一个 **NAT 网关**。
    - _考试陷阱：_ 不要把 Lambda 放进公有子网来获得公网访问——那样不管用。正确路径是：私有子网 → NAT 网关。

## 8. RDS 与 Lambda 集成

- **从 RDS 调用 Lambda：**
    - PostgreSQL 和 Aurora MySQL 可以直接在 SQL 查询（存储过程）中调用 Lambda 函数。
    - 场景：数据摄取过程中的处理、插入一行数据时发送邮件。
- **RDS 事件通知：**
    - RDS 把关于_数据库实例本身_的通知（例如"数据库关闭"、"故障切换开始"）发送到 **SNS**。
    - SNS 再触发 Lambda。
    - _考试提示：_ 这是运维事件，不是数据变更。要捕捉数据变更，通常要用轮询、应用层逻辑，或改用 DynamoDB（Streams）。

## 9. Amazon DynamoDB（无服务器 NoSQL）

- **基础：** 键值（Key-Value）与文档数据库。任何规模下都是毫秒级延迟。
- **主键：**
    - **分区键（Partition Key / HASH）：** 决定数据的分布。
    - **排序键（Sort Key / RANGE）：** 在分区内对数据排序。
- **读取一致性：**
    - **最终一致性（默认）：** 快，费用只有一半。数据可能有 <1 秒的延迟。
    - **强一致性：** 保证读到最新数据。延迟更高，读取费用（RCU）翻倍。
- **容量模式：**
    - **预置（Provisioned）：** 你指定 WCU/RCU，配合 Auto Scaling。适合可预测的负载。
    - **按需（On-Demand）：** 自动扩展。适合不可预测/突发的负载。单次请求更贵。

## 10. DynamoDB 高级特性

- **DAX（DynamoDB Accelerator）：**
    - DynamoDB 的全托管**内存缓存**。
    - 把性能从毫秒级提升到**微秒级**。
    - 透明接入（不需要改代码）。
- **DynamoDB Streams：**
    - 按时间排序的条目级变更序列（INSERT、UPDATE、DELETE）。
    - 保留期：**24 小时**。
    - 主要对接目标：**Lambda**（处理变更、把数据复制到 OpenSearch 等）。
- **Kinesis Data Streams（用于 DynamoDB）：**
    - 捕捉条目级变更的另一种方案（替代 DynamoDB Streams）。
    - 保留期：最长 **1 年**（相比 DynamoDB Streams 的 24 小时）。
    - 特点：支持更高吞吐量和多个消费者（扇出）而不被限流。
- **全局表（Global Tables）：**
    - **多区域、双活（Active-Active）**复制。
    - 在任何区域写入都会复制到所有其他区域（约 1 秒）。
    - 需要启用 DynamoDB Streams。
- **TTL（存活时间）：**
    - 在某个属性中存储的时间戳过期后自动删除条目。
    - 免费（不消耗 WCU）。非常适合会话存储。

## 11. Amazon API Gateway

- **角色：** 无服务器应用的"前门"。对外暴露 Lambda、HTTP 端点或 AWS 服务。
- **端点类型：**
    - **边缘优化（Edge-Optimized）：** 默认。使用 CloudFront 边缘节点（面向全球客户端）。
    - **区域（Regional）：** 客户端在同一区域。
    - **私有（Private）：** 只能从 VPC 内部访问（通过接口 VPC 端点）。
- **集成方式：**
    - **Lambda 代理（Proxy）：** 把原始请求直接传给 Lambda，Lambda 返回原始响应。（因灵活性而被推荐）。
    - **Mock：** 不发给后端直接返回响应（适合测试）。
- **功能：**
    - **缓存：** 缓存响应以减轻后端负载（TTL 默认 300 秒）。
    - **限流（Throttling）：** 保护后端不被压垮（令牌桶算法）。
    - **阶段（Stages）：** （例如 Dev、Prod、v1、v2）。可以用"金丝雀发布（Canary Release）"把一定比例的流量送到新阶段。

## 12. AWS Step Functions

- **概念：** 无服务器编排服务。把工作流建模为**状态机（State Machines）**。
- **语言：** 使用**Amazon States Language（ASL）**定义（基于 JSON）。
- **使用场景：** 长时间运行的流程、人工审批环节、串联多个 Lambda、错误处理（重试/捕获）。
- **Standard vs Express 工作流：**
    - **Standard：** 长时间运行（最长 **1 年**），精确一次执行。适合订单履约。
    - **Express：** 短时（最长 **5 分钟**），至少一次执行。适合高吞吐事件处理（物联网数据摄取）。

## 13. Amazon Cognito 概览

- **用户池（User Pools）（认证 - "你是谁？"）：**
    - 用户目录（注册、登录）。
    - 支持 MFA、密码策略、社交登录（Google、Facebook、SAML）。
    - 返回 **JWT（JSON Web Token）**。
- **身份池（Identity Pools）（授权 - "你能做什么？"）：**
    - 用登录令牌（来自用户池或社交平台）换取**临时 AWS 凭证**。
    - 把用户映射到 **IAM 角色**。
    - 让用户可以直接访问 AWS 服务（例如把文件上传到 S3 桶）。
- **Web 身份联合模式：**
    1. 用户登录 Cognito 用户池（或 Google）。
    2. 应用收到令牌（Token）。
    3. 应用把令牌发给 Cognito 身份池。
    4. 身份池返回 AWS 凭证（Access Key / Secret Key）。
    5. 应用用这些凭证访问 S3/DynamoDB。


---

# 第 15 章：AWS 数据库全家桶（AWS Databases）

## 1. 选择正确的数据库（考试速查表）

考试常给出场景，让你选_最合适_的数据库。用这张关键词对照表：

| 场景 / 关键词 | 推荐服务 |
| --- | --- |
| **关系型（SQL）**、OLTP、传统应用（ERP、CRM） | **RDS** |
| 关系型、**高性能**、**存储自动扩展**、企业级 | **Aurora** |
| **NoSQL**、键值、超大规模、**个位数毫秒延迟** | **DynamoDB** |
| **内存**缓存、**亚毫秒延迟**、减轻数据库压力 | **ElastiCache** |
| **图**数据、社交网络、推荐引擎 | **Neptune** |
| **MongoDB** 兼容、JSON 文档 | **DocumentDB** |
| **Cassandra** 迁移、宽列存储 | **Keyspaces** |
| **时序**数据、物联网流、DevOps 日志 | **Timestream** |
| **数据湖**、非结构化数据、静态网站托管 | **S3** |
| **数据仓库**、OLAP、SQL 分析 | **Redshift**（注意：和 RDS 是两回事） |

## 2. Amazon RDS（关系型数据库服务）

_面向使用 SQL 作为查询语言的数据库的托管服务。_

- **引擎：** PostgreSQL、MySQL、MariaDB、Oracle、Microsoft SQL Server。
- **关键特性：**
    - **Multi-AZ（多可用区）：**
        - **目的：** **灾难恢复（高可用）**。
        - 同步复制到另一个 AZ 中的备用实例。
        - 主库宕机时自动故障切换（CNAME 记录切换指向）。
        - _不是_用来提升性能的。
    - **只读副本（Read Replicas）：**
        - **目的：** **可扩展性（性能）**。
        - 异步复制。
        - 可以在同一 AZ 内、跨 AZ 或跨区域。
        - 应用必须修改连接字符串才能从副本读取。
    - **备份：**
        - **自动备份：** 每日全量备份 + 事务日志（可精确到 5 分钟粒度的时间点恢复）。保留 1-35 天。
        - **快照：** 手动、由用户发起、可无限期保存。
    - **存储：**
        - **通用型 SSD（gp2/gp3）：** 性价比高、可突发。
        - **预置 IOPS（io1/io2）：** 适合高 I/O、对延迟敏感的工作负载。
    - **加密：** 严格来说只能在创建时启用。要加密一个未加密的数据库，必须：做快照 → 复制快照（同时加密）→ 从加密快照恢复。

## 3. Amazon Aurora

_AWS 专有的关系型数据库，兼容 MySQL 和 PostgreSQL。比 MySQL 快 5 倍，比 PostgreSQL 快 3 倍。_

- **架构：**
    - **计算与存储分离。**
    - **存储：** 数据在 **3 个 AZ 中复制 6 份**。可自愈。
    - 存储自动扩容（无需手动预置存储大小）。
- **端点：**
    - **集群端点（Cluster Endpoint）：** 指向_写入（Writer）_实例。
    - **读端点（Reader Endpoint）：** 在所有_只读副本_之间做连接负载均衡。
- **高可用：**
    - 最多可有 **15 个只读副本**（RDS 的上限是 5 个）。
    - 故障切换比 RDS 快（通常 < 30 秒）。
- **Aurora Serverless：**
    - 根据需求自动扩缩计算容量。
    - 适合：不频繁、间歇性或不可预测的工作负载。
- **Aurora 全球数据库：**
    - 一个主区域（读写），最多 5 个只读从区域。
    - 跨区域延迟 < 1 秒。可在 1 分钟内把从区域提升为主区域。

## 4. Amazon ElastiCache

_托管的 Redis 或 Memcached。用内存缓存提升读取性能。_

- **使用场景：** 减轻 RDS/DynamoDB 的压力、存储用户会话、排行榜。
- **Redis vs Memcached（考试关键）：**

|特性|**Redis**|**Memcached**|
|---|---|---|
|**数据结构**|复杂（集合、列表、有序集合）|简单的字符串/对象|
|**持久化**|支持（可以落盘）|不支持（纯内存）|
|**可用性**|Multi-AZ、故障切换、复制|无复制|
|**扩展**|扩展只读副本|多线程架构（垂直扩展）|
|**考试提示**|需要**高可用**、**备份**或**排序**时选它。|**简单**缓存或**多线程**性能时选它。|

- **缓存策略：**
    - **懒加载（Lazy Loading）：** 只有被请求时才加载数据。（优点：只缓存被用到的数据。缺点：缓存未命中有代价）。
    - **写穿透（Write-Through）：** 数据库更新时同步写入缓存。（优点：数据永不过期。缺点：写入有代价，可能缓存了没人用的数据）。

## 5. Amazon DynamoDB

_全托管、无服务器的 NoSQL 键值存储。_

- **性能：** 任何规模下都是个位数毫秒延迟。
- **容量模式：**
    - **预置：** 你自己设置 RCU（读容量单位）和 WCU（写容量单位）。流量可预测时使用。
    - **按需：** 即时自动扩展。用于不可预测/突发的流量。
- **键：**
    - **分区键（Hash）：** 决定数据分布。
    - **排序键（Range）：** 在分区内排序。
- **DAX（DynamoDB Accelerator）：**
    - DynamoDB 的全托管内存缓存。
    - **关键词：** 把延迟从毫秒级降到**微秒级**。
    - 有"热键（hot key）"问题或极端读负载时使用。
- **DynamoDB Streams：**
    - 捕捉条目级变更（INSERT、UPDATE、DELETE）。
    - 触发 Lambda 函数（例如"新用户注册时发送欢迎邮件"）。
- **全局表（Global Tables）：**
    - 多区域、双活复制。
    - 在多个区域实现高可用和本地读写。

## 6. Amazon S3（作为"数据库"）

_虽然本质上是对象存储，S3 经常作为数据库架构的一环出现。_

- **数据湖：** S3 作为结构化和非结构化数据的中央存储库。
- **就地查询（Query in Place）：**
    - **Amazon Athena：** 直接对 S3 数据执行无服务器 SQL 查询。
    - **S3 Select：** 只从对象中取回需要的那部分数据（提升性能）。
- **一致性：** 所有 GET、PUT、LIST 操作均为强一致性。

## 7. Amazon DocumentDB（兼容 MongoDB）

- **关键词：** **MongoDB**。
- 用于管理 JSON 数据。
- 架构类似 Aurora（存储在 3 个 AZ 复制 6 份）。

## 8. Amazon Neptune

- **关键词：** **图数据库（Graph Database）**。
- **使用场景：**
    - 社交网络（谁和谁是朋友？）。
    - 推荐引擎（买了 A 的人可能喜欢 B）。
    - 欺诈检测（模式匹配）。
    - 知识图谱。
- 查询语言：Gremlin（Apache TinkerPop）、SPARQL。

## 9. Amazon Keyspaces（兼容 Apache Cassandra）

- **关键词：** **Cassandra**。
- 无服务器、可扩展、高可用的宽列存储。
- 兼容 CQL（Cassandra 查询语言）。
- 迁移类考题中提到已有 Cassandra 工作负载时选它。

## 10. Amazon Timestream

- **关键词：** **时序（Time-series）**。
- **使用场景：**
    - 物联网应用（每秒传感器读数）。
    - DevOps（随时间跟踪 CPU/内存使用率）。
    - 工业遥测。
- 架构：
    - **内存存储（Memory Store）：** 存最近的数据（高速摄取）。
    - **磁性存储（Magnetic Store）：** 存历史数据（成本优化）。
    - 内置保留策略，自动在两层之间移动数据。


---

# 第 16 章：AWS 数据分析（Data Analytics & Big Data）

本章涵盖 SAA-C03 考试中常考的数据分析服务和架构模式。

## 1. 核心服务概览

### Amazon Athena

- **是什么：** 无服务器的交互式查询服务。
- **核心功能：** 用标准 **SQL** 直接分析 **Amazon S3** 中的数据。
- **性能与成本优化（考试重点）：**
    - **列式数据：** 使用 **Parquet** 或 **ORC** 格式。Athena _只_扫描需要的列（省钱省时间）；而 CSV/JSON 则要扫描整个文件。
    - **压缩：** 压缩数据（Snappy、Zlib、GZIP）以减小扫描量。
    - **分区：** 按日期/类别组织 S3 文件夹（例如 `s3://bucket/year=2024/month=01/`）。Athena 只扫描相关文件夹（"分区裁剪 partition pruning"）。
- **联邦查询（Federated Query）：**
    - 允许跨关系型、非关系型、对象和自定义数据源（例如 DynamoDB、RDS、Redshift）执行 SQL 查询，_无需_把数据搬到 S3。
    - 使用运行在 **AWS Lambda** 上的**数据源连接器**。
- **考试关键词：** 无服务器、临时（Ad-hoc）查询、S3、SQL、Parquet/ORC、"无需管理基础设施"。

### Amazon Redshift

- **是什么：** PB 级、全托管的**数据仓库**（OLAP）。
- **架构：**
    - **领导节点（Leader Node）：** 处理客户端连接（JDBC/ODBC）、解析 SQL、生成执行计划、聚合结果。_你只和这个节点打交道。_
    - **计算节点（Compute Nodes）：** 并行执行查询并存储数据。数据分布在这些节点的"切片（slices）"上。
    - **Redshift Spectrum：** 一个无服务器功能，让 Redshift 无需加载数据即可直接查询 S3 中的数据。适合"冷"数据或不频繁的查询。
- **数据加载与摄取：**
    - **COPY 命令：** 从 S3 批量加载数据的最快方式。
    - **Kinesis Data Firehose：** 用于**流式摄取**，直接进 Redshift（近实时）。
    - **JDBC/ODBC 驱动：** 连接 BI 工具（Tableau、QuickSight）或应用的标准驱动。
- **安全与网络：**
    - **增强 VPC 路由（Enhanced VPC Routing）：** 强制 Redshift 和 S3 之间的所有 COPY 和 UNLOAD 流量走你的 VPC（通过 VPC 端点），绕开公共互联网。严格合规场景的关键。
    - **灾难恢复（DR）：**
        - **快照：** 自动或手动的时间点备份，存储在 S3。
        - **跨区域复制：** 可以配置 Redshift 自动把快照复制到另一个区域用于 DR。

### Amazon OpenSearch Service（原 Elasticsearch）

- **是什么：** 用于搜索、分析和日志监控的托管服务。
- **核心功能：** 运营分析（应用监控）、全文搜索、日志分析。
- **考试关键词：** 搜索、索引、Kibana（仪表板）、日志分析、"模糊搜索（fuzzy search）"。
- **集成：** 常作为 Kinesis Data Firehose 或 CloudWatch Logs 输出日志的目的地。

### Amazon EMR（Elastic MapReduce）

- **是什么：** 面向大数据框架（Hadoop、Spark、Hive、Presto）的托管集群平台。
- **集群架构（节点类型）：**
    - **主节点（Master Node）：** 管理集群（YARN ResourceManager、HDFS NameNode）。协调数据和任务的分配。
    - **核心节点（Core Node）：** 既运行任务_又_在 HDFS 中存储数据。**不要**轻易终止（有数据丢失风险）。
    - **任务节点（Task Node）：** _只_运行任务（不存数据）。可以随意增减来扩展计算能力。**最适合用竞价实例**。
- **考试关键词：** Hadoop、Spark、大数据迁移、竞价实例（任务节点）。

### Amazon QuickSight

- **是什么：** 无服务器的商业智能（BI）服务。
- **核心功能：** 创建并发布交互式仪表板和可视化。
- **用户与组管理：**
    - **标准版：** 通过 IAM 或邮件邀请管理用户。
    - **企业版：** 支持 **Active Directory（AD）**集成和联合登录。
    - **行级安全（RLS）：** 根据用户或组限制对特定数据行的访问（例如"销售组只能看到销售数据"）。
- **考试关键词：** BI、可视化、仪表板、机器学习洞察、"按会话付费（pay-per-session）"。

### AWS Glue

- **是什么：** 无服务器的数据集成（ETL - 提取、转换、加载）服务。
- **核心功能：** 为分析准备和加载数据。
- **组件：**
    - **数据目录（Data Catalog）：** 元数据（表结构定义）的中央仓库。Athena 和 Redshift Spectrum 都需要它。
    - **爬虫（Crawler）：** 自动扫描 S3 数据、推断表结构并更新数据目录。
    - **Glue 作业（Jobs）：** 用 Python/Scala 脚本转换数据（例如把 CSV 转成 Parquet）。
- **考试关键词：** ETL、无服务器、数据目录、表结构自动发现。

### AWS Lake Formation

- **是什么：** 用于搭建、保护和管理数据湖的服务。
- **核心功能：** 简化安全与治理。它位于 Glue 和 S3 _之上_。
- **考试关键词：** "细粒度访问控制"（行级/列级安全）、"蓝图（Blueprint）"、治理、"几天内建好安全的数据湖"。

### Amazon Managed Service for Apache Flink

- **注意：** 原名 _Kinesis Data Analytics_。
- **是什么：** 全托管的实时流处理服务。
- **核心功能：** 用 Java、Scala、Python 或 SQL 运行有状态的应用来处理数据流。
- **考试关键词：** 实时分析、有状态处理、窗口计算（例如"每 5 分钟计算一次平均值"）、Flink。

### Amazon MSK（Managed Streaming for Apache Kafka）

- **是什么：** 全托管的 Apache Kafka 服务。
- **核心功能：** 用 Kafka 摄取和处理流数据。
- **考试关键词：** "迁移现有 Kafka 集群"、开源兼容性、高吞吐量。

## 2. 常见的大数据摄取管道（架构模式）

SAA-C03 考试非常喜欢考这些服务如何串联。以下是三种最常见的模式：

### 模式 A："现代数据湖"（批处理）

**场景：** 摄取原始数据、转换、然后用 SQL 分析。

1. **摄取：** 数据落地到 **S3**（原始桶）。
2. **编目：** **Glue 爬虫**扫描 S3 并更新 **Glue 数据目录**。
3. **处理（ETL）：** **Glue 作业**（或 EMR）清洗数据并转换为 Parquet。
4. **存储：** 转换后的数据存到 **S3**（处理后桶）或加载到 **Redshift**。
5. **分析：**
    - **Athena** 直接查询 S3（临时查询）。
    - **Redshift** 查询数据仓库（复杂 BI）。
    - **QuickSight** 做结果可视化。

### 模式 B："实时流"管道

**场景：** 实时分析点击流数据或物联网传感器。

1. **摄取：** **Kinesis Data Streams**（或 MSK）捕获高速数据。
2. **处理：** **Managed Service for Apache Flink** 处理数据流（例如聚合指标）。
3. **存储/投递：**
    - **Kinesis Data Firehose** 把处理后的数据投递到 **S3**、**Redshift** 或 **OpenSearch**。
4. **分析：** 在 **OpenSearch**（Kibana）或 **QuickSight** 中做实时仪表板。

### 模式 C：日志分析

**场景：** 集中分析应用日志。

1. **来源：** CloudWatch Logs 或 EC2 日志。
2. **流转：** **Kinesis Data Firehose**。
3. **目的地：** **Amazon OpenSearch Service**。
4. **可视化：** **Kibana**（内置于 OpenSearch），用于搜索和可视化错误日志。

## 3. 快速对比速查表

|特性|**Athena**|**Redshift**|**EMR**|
|---|---|---|---|
|**主要用途**|S3 上的临时查询|数据仓库 / BI|大数据处理（Spark/Hadoop）|
|**基础设施**|无服务器|集群（托管）|集群（托管）|
|**计费**|按扫描的 TB 数|按节点/小时|按实例/小时|
|**速度**|秒级/分钟级|毫秒级/秒级|分钟级/小时级（批处理）|
|**考试触发词**|"S3 上的无服务器 SQL"|"复杂关联查询"、"数据仓库"|"Apache Spark"、"Hadoop 迁移"|

| 特性 | **Kinesis Data Streams** | **Kinesis Data Firehose** |
| --- | --- | --- |
| **实时性** | 是（实时） | 近实时（60 秒缓冲） |
| **功能** | 摄取自定义数据流 | 把数据**加载**到目的地 |
| **目的地** | Lambda、Flink、EC2 | S3、Redshift、OpenSearch、Splunk |
| **管理** | 手动扩展（分片） | 完全无服务器（自动扩展） |


---

# 第 17 章：AWS 机器学习服务（Machine Learning）

对于 **SAA-C03** 考试，你通常不需要会_写代码_使用这些服务，而是要**根据业务问题识别出正确的服务**。

核心概念：这些大多是"无服务器"的"托管服务"（自带高可用）。

## 1. Amazon Rekognition

**"计算机视觉 / 图像与视频分析"**

- **做什么：** 用机器学习分析图像和视频。你发给它一张图，它告诉你里面有什么。
- **关键功能：**
    - **物体与场景检测：** 识别汽车、宠物、海滩、城市。
    - **面部分析：** 检测人脸、性别、年龄范围、情绪（高兴/悲伤）。
    - **面部识别：** 把一张脸与私有人脸库比对（例如员工刷脸门禁）。
    - **内容审核：** 自动检测不安全、冒犯性或不当内容（裸露、暴力）。
- **考试场景：**
    - "公司需要自动标记用户上传到社交应用的不当图片。" → **用 Rekognition 内容审核。**
    - "商店想跟踪顾客在货架间行走的路径。" → **用 Rekognition 视频路径分析。**

## 2. Amazon Transcribe

**"语音转文本（Speech-to-Text）"**

- **做什么：** 把音频（MP3、WAV 等）转换成文本。
- **关键功能：**
    - **说话人识别：** 能分辨"说话人 A"和"说话人 B"。
    - **自动语言识别：** 检测主要语言。
- **考试场景：**
    - "公司需要为视频内容自动生成字幕。" → **用 Transcribe。**
    - "需要把客服通话录音转成可搜索的文字记录。" → **用 Transcribe。**

## 3. Amazon Polly

**"文本转语音（Text-to-Speech）"**

- **做什么：** 把文本转换成逼真的语音（MP3）。是 Transcribe 的反向操作。
- **关键功能：**
    - **神经网络 TTS：** 更高质量、更自然的声音。
    - **词典（Lexicons）：** 自定义特定词的发音（例如把 "AWS" 读成 "A-W-S" 而不是 "Aws"）。
    - **SSML（语音合成标记语言）：** 用标记标签控制呼吸、音量、语速和重音。
- **考试场景：**
    - "一个应用需要为视障用户朗读文章。" → **用 Polly。**

## 4. Amazon Translate

**"语言翻译"**

- **做什么：** 提供高质量的神经网络机器翻译。
- **关键功能：**
    - 实时翻译和批量翻译。
    - 支持自定义术语（例如确保品牌名不被直译）。
- **考试场景：**
    - "公司想把网站内容动态本地化成 10 种语言。" → **用 Translate。**

## 5. Amazon Lex + Amazon Connect

**"聊天机器人与呼叫中心"**

### Amazon Lex（"大脑"）

- **做什么：** 提供自动语音识别（ASR）和自然语言理解（NLU）。这就是 **Alexa** 背后的技术。
- **使用场景：** 为文本或语音构建对话式界面（聊天机器人）。

### Amazon Connect（"电话系统"）

- **做什么：** 云端的**联络中心即服务（Contact Center as a Service）**。
- **集成方式：** 用 **Connect** 处理电话线路和路由，用 **Lex** 构建自动化的 "IVR"（交互式语音应答），在人工客服接听前先与客户对话。
- **考试场景：**
    - "公司需要一个能应对流量高峰、并用 AI 回答常见问题的云呼叫中心。" → **用 Amazon Connect 集成 Amazon Lex。**

## 6. Amazon Comprehend

**"自然语言处理（NLP）"**

- **做什么：** 分析文本，理解其中的_含义_和_关系_。
- **关键功能：**
    - **情感分析：** 判断文本是正面、负面、中性还是混合。
    - **实体识别：** 提取人物、地点、品牌、日期。
    - **PII 检测：** 检测个人身份信息（邮箱、社保号）以便脱敏。
- **考试场景：**
    - "公司想分析客户邮件，判断他们是满意还是愤怒。" → **用 Comprehend 情感分析。**

## 7. Amazon Comprehend Medical

**"医疗领域的 NLP"**

- **做什么：** 专门针对医疗文本训练，识别病症、药物、剂量和受保护的健康信息（PHI）。
- **关键功能：**
    - **符合 HIPAA 要求。**
    - 检测 PHI（患者健康信息）以便匿名化。
- **考试场景：**
    - "医院需要为医生的病历笔记建立索引，识别患者诊断和处方药。" → **用 Comprehend Medical。**（不要选普通版 Comprehend）。

## 8. Amazon SageMaker

**"面向开发者的 ML / 自定义模型"**

- **做什么：** 全托管服务，用于构建、训练和部署**自定义**机器学习模型。
- **核心概念：** 当"开箱即用"的 AI 服务（如 Rekognition 或 Polly）不够用、你需要自己造算法时使用。
- **考试场景：**
    - "数据科学团队需要一个托管环境来运行 Jupyter Notebook、训练 TensorFlow 模型。" → **用 SageMaker。**
    - "需要部署一个带自动扩展的自定义欺诈检测模型。" → **用 SageMaker。**

## 9. Amazon Kendra

**"企业搜索"**

- **做什么：** 由机器学习驱动的智能文档搜索服务。可以理解为公司内部文档的"谷歌搜索"。
- **关键功能：**
    - 从 PDF、Word 文档、HTML、SharePoint 等中提取答案。
    - 支持**自然语言查询**（例如问"IT 支持台在哪？"而不是只匹配关键词 "IT"）。
- **考试场景：**
    - "公司希望员工能用自然语言问题搜索内部的 PDF 手册库。" → **用 Kendra。**

## 10. Amazon Personalize

**"实时个性化推荐"**

- **做什么：** 用于创建实时个性化推荐的机器学习服务。与 Amazon.com "猜你喜欢"用的是同一套技术。
- **考试场景：**
    - "电商网站想根据用户购买历史展示'为你推荐'栏目。" → **用 Personalize。**（Personalize 能做到的事，不要去 SageMaker 自建模型）。

## 11. Amazon Textract

**"超级 OCR / 文档分析"**

- **做什么：** 从扫描文档中提取文本、手写内容和**结构化数据**。
- **与普通 OCR 的关键区别：** 普通 OCR 只给你一坨文字。Textract 能理解**结构**（表格、表单、键值对）。
- **考试场景：**
    - "公司需要从数千张扫描版 PDF 发票中提取总金额和供应商名称，并存入数据库。" → **用 Textract。**

## 机器学习总结速查表

|服务|关键词 / 触发词|
|---|---|
|**Rekognition**|图片、视频、人脸、内容审核（安全）、标签。|
|**Transcribe**|语音转文本、字幕、会议纪要、音频转文字。|
|**Polly**|文本转语音、会说话的应用、SSML、语音。|
|**Translate**|本地化、语言翻译。|
|**Lex**|聊天机器人、对话式 AI、Alexa 技术。|
|**Connect**|云联络中心、呼叫中心、电话系统。|
|**Comprehend**|情感分析、NLP、实体、PII 脱敏。|
|**SageMaker**|自定义模型、Jupyter Notebook、自建算法的构建/训练/部署。|
|**Kendra**|内部搜索、文档搜索、自然语言查询。|
|**Personalize**|推荐系统、"买过此商品的人还买了……"、零售 ML。|
|**Textract**|扫描文档、表单、表格、发票、收据、OCR。|


---

# 第 18 章：AWS 监控与审计（CloudWatch、CloudTrail 与 Config）

## 1. AWS 监控 - 章节引言

要保持卓越运营和性能效率（Well-Architected 框架的两大支柱），你必须了解 AWS 资源的运行状况以及谁在访问它们。

- **CloudWatch：** "现在正在发生什么？"（性能与日志）
- **CloudTrail：** "谁做了什么？"（审计与 API 历史）
- **Config：** "资源的配置长什么样？"（配置与合规）

## 2. CloudWatch 指标（Metrics）

CloudWatch 提供数据和可执行的洞察，用于监控应用、响应系统级性能变化、优化资源利用率，并获得运行健康状况的统一视图。

- **指标（Metrics）：** 要监控的变量（例如 `CPUUtilization`，CPU 利用率）。
- **命名空间（Namespaces）：** CloudWatch 指标的容器（例如 `AWS/EC2`）。
- **维度（Dimensions）：** 帮助唯一标识某个指标的名称/值对（例如 `InstanceId`）。
- **时间戳：** 所有指标都带时间戳。
- **指标分辨率：**
    - **标准分辨率：** 1 分钟粒度（自定义指标的默认值）。
    - **高分辨率：** 1 秒粒度（费用更高）。
- **仪表板（Dashboards）：** 全球访问（可以在一个仪表板里看不同区域的数据）。

**考试提示：**

- 默认情况下，EC2 指标每 **5 分钟**发送一次（基础监控）。可以开启**详细监控（Detailed Monitoring）**，每 **1 分钟**一次（额外收费）。
- **内存（RAM）使用率不是 EC2 的默认指标**。必须以_自定义指标_的方式自行推送。

## 3. CloudWatch Logs

集中式日志管理。可以存储、访问和监控来自 EC2 实例、AWS CloudTrail、Route 53 等来源的日志。

- **日志组（Log Groups）：** 通常代表一个应用或特定来源（例如 `/aws/lambda/my-function`）。
- **日志流（Log Streams）：** 应用内的实例或日志文件（例如某个容器实例的日志）。
- **指标过滤器（Metric Filters）：** 可以在日志中搜索特定短语（例如 "Error" 或 "404"），并把它变成一个数值指标来触发告警。
- **导出：** 日志可以导出到 **S3**（长期归档），或流式发送到 **Kinesis/Lambda/OpenSearch**（实时分析）。

**考试提示：**

- 导出到 S3 的日志最长可能要 12 小时才可用。要实时处理，用**订阅过滤器（Subscription Filters）**把日志发到 Kinesis 或 Lambda。
- 日志默认永不过期（无限保留）。必须设置保留策略才会自动删除。

## 4. CloudWatch Logs - Live Tail

- 提供一个交互式的滚动视图，实时显示新摄取的日志事件。
- 类似 Linux 里的 `tail -f` 命令。
- 帮助开发/运维团队实时快速排障，不用反复刷新控制台或跑查询。

## 5. CloudWatch Agent 与 CloudWatch Logs Agent

要从 EC2 实例内部获取日志或系统级指标（如磁盘空间或内存），需要安装代理（agent）。

- **CloudWatch Logs Agent（旧）：** 只发送日志。已弃用。
- **统一 CloudWatch Agent（新）：**
    - **同时**收集日志和系统级指标（内存、Swap、磁盘空间、Netstat 等）。
    - 支持 EC2 和本地服务器。
    - 配置存储在 SSM 参数存储（Parameter Store）中，便于在大规模机群上部署。

**考试提示：**

- 如果题目问如何监控 EC2 的"内存利用率"或"可用磁盘空间"，答案永远是 **CloudWatch Agent**（推送自定义指标）。

## 6. CloudWatch 告警（Alarms）

告警用于在指标越过阈值时触发通知或动作。

- **三种状态：**
    1. **OK：** 指标在阈值范围内。
    2. **ALARM：** 指标越过阈值。
    3. **INSUFFICIENT_DATA：** 数据不足（例如新实例、连接问题）。
- **动作（目标）：**
    - **SNS：** 发送邮件/短信。
    - **Auto Scaling：** 扩容或缩容（ASG）。
    - **EC2 动作：** 停止、终止、重启或**恢复（Recover）**实例。

**考试提示：**

- **EC2 实例恢复：** 如果实例的_系统状态检查_失败（硬件问题），CloudWatch 告警可以自动"恢复"实例。这会把实例迁移到新硬件上，但保持实例 ID、私有 IP 和弹性 IP 不变。

## 7. CloudWatch Synthetics（Canaries，金丝雀）

- **是什么：** 按计划运行的可配置脚本（Node.js 或 Python），用于监控你的端点和 API。
- **功能：** 模拟用户浏览你的应用（"合成流量"）。
- **使用场景：** 验证应用是否在线、检查延迟、确保 UI 元素（比如结账按钮）正常工作——即使当时没有真实用户流量。

## 8. EventBridge 概览（原 CloudWatch Events）

EventBridge 是一个无服务器事件总线，用来自你自己的应用、SaaS 应用和 AWS 服务的数据连接各个应用。

- **演进：** CloudWatch Events 正在被 EventBridge 取代（底层技术相同，功能更多）。
- **结构：**
    - **来源（Source）：** 产生事件的服务（例如"EC2 实例状态变化"）。
    - **规则（Rules）：** 过滤传入的事件（例如"只关注 EC2 停止事件"）。
    - **目标（Target）：** 要触发的服务（Lambda、SNS、SQS、Kinesis、Step Functions）。
- **Schema 注册表：** 用于发现、创建和管理事件的 Schema。
- **计划事件（Scheduled Events）：** 相当于"定时任务 Cron Job"（例如每晚 2 点触发一个 Lambda 函数）。

## 9. CloudWatch Insights 与运维可见性

用于更深入分析的高级功能。

- **Container Insights：** 从容器化应用（ECS、EKS、Fargate、Kubernetes）收集指标/日志。
- **Lambda Insights：** 无服务器函数的监控与排障（冷启动、内存使用）。
- **Contributor Insights：** 分析日志数据找出"前 N 名"贡献者（例如"访问我负载均衡器最多的前 10 个 IP 是谁？"）。
- **Application Insights：** 用 AI/ML 检测 .NET 和 SQL Server 数据库（及其他技术栈）中的错误/问题。

## 10. CloudTrail 概览

治理、合规、运营与风险审计。CloudTrail 记录你 AWS 账户内的 **API 调用**。

- **记录什么：** 谁（用户/角色）、何时（时间）、做了什么（API 操作）、在哪里（资源与来源 IP）。
- **默认：** 默认开启。控制台中保留 90 天历史（管理事件）。
- **事件类型：**
    1. **管理事件（Management Events）：** 对 AWS 账户的操作（例如 `AttachRolePolicy`、`CreateVPC`、`ConsoleLogin`）。默认开启。
    2. **数据事件（Data Events）：** 资源内部的操作（例如 S3 对象上传、Lambda 函数调用、DynamoDB 写入条目）。**量大，默认关闭。**
    3. **洞察事件（Insights Events）：** 检测异常活动（资源创建量的突增）。
- **存储：** 要保留超过 90 天的日志，必须配置一个 **Trail**（跟踪），把日志发送到 S3。

**考试提示：**

- 如果需要知道"谁终止了 EC2 实例？"或"谁删除了 S3 桶？"，答案是 **CloudTrail**。

## 11. CloudTrail 与 EventBridge 集成

CloudTrail 让你能对 API 调用_做出反应_。流程：CloudTrail 记录 API 调用 → 发送事件到 EventBridge → EventBridge 触发目标。

- **使用场景：** 安全自动化。
    - _场景：_ 某用户创建了一个公开的 S3 桶。
    - _流程：_ CloudTrail 记录 `CreateBucket` → EventBridge 规则匹配模式 → 触发 Lambda → Lambda 删除该桶或修正策略。

## 12. AWS Config - 概览

对 AWS 账户中资源配置的详细视图。

- **资产清单：** 记录资源配置及其随时间的变化（相当于给基础设施做版本管理）。
- **合规：** 可以设置 "Config 规则"（托管规则或自定义规则）。
    - _例如：_ "SSH（22 端口）是否对全网开放？" "EBS 卷是否加密？"
- **自动修复（Remediation）：** 可以用 SSM 自动化文档自动修复不合规的资源。
- **通知：** 资源违反规则时发送 SNS 通知。

**考试提示：**

- 如果需要查看**配置变更的历史**（例如"昨天安全组规则长什么样？"），用 **AWS Config**。
- 它是_区域级_服务。

## 13. 总结：CloudWatch vs CloudTrail vs Config

| 特性 | **CloudWatch** | **CloudTrail** | **AWS Config** |
| --- | --- | --- | --- |
| **核心问题** | "性能怎么样？" | "谁调用的？" | "配置是怎样的？" |
| **关注点** | 性能、指标、日志、告警 | 审计、安全、API 历史 | 合规、资产清单、变更历史 |
| **典型场景** | CPU 很高；应用报 500 错误。 | 谁删了生产数据库？ | 我的 S3 桶公开了吗？安全组变过吗？ |
| **实时响应** | 告警 + EventBridge | EventBridge 集成 | Config 规则 + 自动修复 |
| **保留期** | 指标 15 个月；日志无限期 | 90 天（控制台）；S3（无限期） | 无限期（取决于设置） |

**速记口诀：**

- **Watch（看）** = 性能（速度、错误、负载）
- **Trail（踪迹）** = 审计（盯着用户、API 调用）
- **Config（配置）** = 规则（策略、设置、历史）


---

# 第 19 章：身份与访问管理（IAM）- 高级 与 Organizations

## 1. AWS Organizations - 概览

AWS Organizations 帮助你在扩展 AWS 资源时集中管理和治理环境。它允许你把多个 AWS 账户合并到一个由你创建并集中管理的组织中。

- **核心概念：**
    - **管理账户（Management Account）：** 创建组织的那个账户。它负责付账单（合并账单），并拥有完全的管理控制权。**关键：** 服务控制策略（SCP）_不适用于_管理账户。
    - **成员账户（Member Accounts）：** 组织中的其他所有账户。
    - **组织单元（OU，Organizational Units）：** 根（root）下的账户容器。可以对 OU 应用策略，策略会作用于该 OU 内的所有账户。
    - **合并账单（Consolidated Billing）：** 汇总所有账户的用量成本。合并用量后可以享受批量折扣（例如 S3 存储阶梯价、EC2 数据传输）。
- **服务控制策略（SCP，Service Control Policies）：**
    - SCP 用于限制成员账户中的用户和角色可以访问哪些 AWS 服务、资源和 API 操作。
    - SCP 构成账户的**权限边界**。它_不授予_权限，只做过滤。用户要执行某个操作，既需要 IAM 权限允许，_又_需要 SCP 允许。
    - **继承：** 策略从根 → OU → 成员账户逐级向下过滤。
    - **Allow vs Deny：** SCP 中的显式拒绝（Deny）会覆盖 IAM 中的任何允许（Allow）。

> **考试提示：** 如果成员账户中的用户在 IAM 里拥有完整的 AdministratorAccess，但 SCP 拒绝了 `ec2:*`，该用户就无法访问 EC2。SCP 是终极护栏。

## 2. Organizations - 标签策略（Tag Policies）

标签策略是一类帮助你在组织内所有账户的资源上统一标签规范的策略。

- **目的：** 强制一致的标签规范（例如确保每个资源都有 `CostCenter` 标签，且值在允许的范围内）。
- **强制方式：**
    - 可以定义合规的标签键和值。
    - 可以阻止在受支持的资源上进行不合规的打标操作。
- **集成：** 配合 AWS Resource Groups 和 Tag Editor 审计合规情况。

> **考试提示：** 标签策略用于成本分摊与治理。如果题目问如何确保组织内创建的资源都带特定的标签键/值，答案就是标签策略（Tag Policies）。

## 3. IAM - 高级策略

除了基本的允许/拒绝，IAM 还支持高级条件和边界。

- **IAM 权限边界（Permission Boundaries）：**
    - 高级功能，用于设定基于身份的策略能授予某个 IAM 实体（用户或角色）的**最大权限**。
    - 常用于委派管理权（例如允许开发者创建角色，但限制这些角色只能访问 S3）。
    - _逻辑：_ 有效权限 =（基于身份的策略）与（权限边界）的**交集**。
- **策略结构与全局条件键：**
    - `aws:SourceIp`：限制只能从特定 IP 段访问。
    - `aws:RequestedRegion`：限制只能调用特定的 AWS 区域。
    - `aws:PrincipalOrgID`：限制资源只能被属于你 AWS 组织的主体访问（阻止外部账户访问）。
    - `aws:MultiFactorAuthPresent`：对敏感操作强制要求 MFA。

```json
/* 示例：拒绝在指定区域之外的访问 */
{
    "Effect": "Deny",
    "NotAction": [
        "cloudfront:*",
        "iam:*",
        "route53:*",
        "support:*"
    ],
    "Resource": "*",
    "Condition": {
        "StringNotEquals": {
            "aws:RequestedRegion": [
                "us-east-1",
                "us-west-1"
            ]
        }
    }
}
```

## 4. IAM - 基于资源的策略 vs IAM 角色

授予跨账户访问或复杂权限主要有两种方式。

### 基于资源的策略（Resource-based Policies）

- **定义：** 直接附加在资源上的策略（例如 S3 桶策略、SQS 队列策略、SNS 主题策略、Lambda 函数策略、KMS 密钥策略）。
- **主体（Principal）：** 你在策略里直接指定 `Principal`（用户、角色或账户 ID）。
- **使用场景：** 受支持资源（S3、SQS、SNS）的简单跨账户访问。
- **优点：** 主体不需要放弃自己当前的权限就能访问该资源；他们保持在自己账户的上下文中。

### IAM 角色（基于身份）

- **定义：** 一个拥有特定权限的身份，可被受信任的实体代入（assume）。
- **机制：** 用户通过 `sts:AssumeRole` 代入角色，获得临时安全凭证。
- **使用场景：** 对_没有_资源策略的服务（例如 DynamoDB、EC2）做跨账户访问，或用户需要完全"切换上下文"时。
- **"混淆代理人（Confused Deputy）"问题：** 第三方利用它对你资源的访问权限，去访问其他资源。
    - **解决：** 允许第三方供应商在你账户中代入角色时，在信任策略（Trust Policy）中使用 `sts:ExternalId` 条件。

> **考试提示：**
>
> - S3 跨账户访问？→ S3 桶策略（基于资源）通常是最简单的答案，除非需要以编程方式访问多个服务。
> - EC2 跨账户访问？→ 必须用 IAM 角色。

## 5. IAM - 策略评估逻辑

AWS 是如何决定允许还是拒绝一个请求的？

1. **隐式拒绝：** 默认一切都被拒绝。
2. **显式拒绝：** 评估器查找任何写着 `"Effect": "Deny"` 的策略（身份策略、资源策略、SCP、边界）。
    - **规则：** 显式拒绝**永远**覆盖任何允许。
3. **显式允许：** 如果没有显式拒绝，评估器查找至少一个 `"Effect": "Allow"`。
4. **最终裁定：**
    - 找到显式拒绝 → **拒绝**
    - 没找到任何允许 → **拒绝**（隐式拒绝）
    - 找到允许 + 没有显式拒绝 → **允许**

**边界的交集逻辑：**

- 身份策略 AND 权限边界
- 身份策略 AND SCP（如果在组织中）
- 身份策略 AND 资源策略（同账户内一般取并集；但跨账户时_双方_都必须允许）。

## 6. AWS IAM Identity Center（AWS SSO 的继任者）

- **目的：** 为所有 AWS 账户和商业应用（Office 365、Salesforce）提供集中登录。
- **身份来源：**
    - 可以使用内置身份存储。
    - 可以通过 **SAML 2.0** 连接外部身份提供商（IdP）（例如 Okta、Ping、Azure AD）。
    - 可以连接 Active Directory（通过 AWS Directory Services）。
- **功能：**
    - **单点登录（SSO）：** 用户登录一次门户，点击图块即可用指定的权限集访问特定的 AWS 账户。
    - **多账户权限：** 为组分配针对特定账户的权限集（Permission Sets）。

> **考试提示：** 如果题目问"集中管理对多个 AWS 账户的访问"或"用现有的企业凭证（SAML 2.0）登录控制台"，答案是 IAM Identity Center。

## 7. AWS Directory Services

把 Microsoft Active Directory（AD）带到 AWS。

|类型|描述|主要使用场景|
|---|---|---|
|**AWS Managed Microsoft AD**|真正的 AD 运行在 AWS 基础设施上。两个域控制器分布在不同的 AZ。|• 与本地 AD 建立**信任关系（Trust Relationships）**。<br>• 需要复杂的 AD 功能（组策略、Schema 扩展）。<br>• 让 EC2 Windows 实例无缝加入域。|
|**AD Connector**|通往本地 AD 的代理（网关）。不缓存凭证。|• 想使用本地凭证，但_不_把它们同步到云端。<br>• 用本地 AD 为 AWS 服务（Workspaces、QuickSight）做认证。|
|**Simple AD**|基于 Samba 4 的独立目录。|• 成本低。<br>• **不能**与本地 AD 建立信任关系。<br>• 适合小型纯云部署的基础用户管理。|

> **考试提示：**
>
> - 需要信任关系？→ **Managed Microsoft AD**。
> - 用户只留在本地、但要用他们做认证？→ **AD Connector**。
> - 便宜、纯云、不需要连接本地？→ **Simple AD**。

## 8. AWS Control Tower

AWS Organizations 之上的一层抽象。

- **目的：** 搭建和治理一个安全的多账户 AWS 环境的最简单方式。它自动化了"着陆区（Landing Zone）"的搭建。
- **核心功能：**
    - **着陆区（Landing Zone）：** 基于最佳实践搭建多账户环境（日志账户、审计账户）。
    - **护栏（Guardrails）：**
        - **预防性护栏：** 阻止不合规的操作（通过 **SCP** 实现）。例如："禁止创建公开的 S3 桶。"
        - **检测性护栏：** 对不合规发出告警（通过 **AWS Config** 实现）。
    - **账户工厂（Account Factory）：** 用预先批准的网络和安全配置自动化新账户的创建。

> **考试提示：** 如果题目提到"自动化搭建多账户环境"、"Landing Zone"或"蓝图（Blueprints）"，选 Control Tower。


---

# 第 20 章：AWS 安全与加密（KMS、SSM Parameter Store、Shield、WAF）

## 1. AWS 安全 - 章节引言

- **责任共担模型（Shared Responsibility Model）：**
    - **AWS 负责：** 云_本身_的安全（物理设施、硬件、虚拟化层、托管服务）。
    - **客户负责：** 云_中_的安全（数据、操作系统补丁、防火墙配置、IAM、加密）。
- **最小权限原则：** 永远只授予完成任务所需的权限。
- **考试提示：** 如果题目问"给 Guest 操作系统打补丁"（EC2）或"配置安全组"，那永远是**客户**的责任。如果问"报废损坏的硬盘"，那是 **AWS** 的责任。

## 2. 加密基础 101

- **传输中加密（Encryption in Flight，TLS/SSL）：**
    - 数据发送前加密，接收后解密。
    - 防范中间人（MITM）攻击。
    - 由 HTTPS 端点处理。
- **静态加密（Encryption at Rest）：**
    - 数据在存储时（磁盘、数据库、S3）被加密。
    - 通过密钥（KMS）或客户端加密实现。
- **考试提示：** 任何提到"合规要求对穿越网络的数据加密"的场景，指的都是**传输中加密**（HTTPS/TLS）。

## 3. KMS（密钥管理服务）概览

- **功能：** 用于创建和控制加密密钥的托管服务。
- **范围：** 区域级服务。
- **考试概念 - 信封加密（Envelope Encryption）：**
    - KMS 的 `Encrypt` API 有大小限制（4KB）。
    - 对于大数据（文件），使用**信封加密**：
        1. KMS 生成一个数据密钥（`GenerateDataKey` API）。
        2. 你用数据密钥在本地加密文件。
        3. 加密后的数据密钥与文件一起存储。
- **考试提示：** 要加密**大于 4KB** 的文件，不能直接用 `Encrypt` API，**必须**用**信封加密**（GenerateDataKey）。

## 4. KMS 密钥类型（考试重点）

考试会测试你按**管理方式**（成本/控制权）和**密钥材料来源**区分密钥的能力。

### A. 按管理方式（谁来管理？）

| 特性 | **AWS 拥有的密钥（AWS Owned）** | **AWS 托管的密钥（AWS Managed）** | **客户托管的密钥（CMK）** |
| --- | --- | --- | --- |
| **示例** | S3 默认加密（SSE-S3） | `aws/s3`、`aws/ebs` | 你自己创建的密钥（`my-app-key`） |
| **可见性** | **不可见**（KMS 控制台里看不到） | **可见** | **可见** |
| **轮换** | 由 AWS 管理 | 每 **1 年**自动轮换（强制） | 每 **1 年**自动轮换（可选——需手动开启） |
| **权限** | 无控制权 | 无控制权（策略只读） | **完全控制**（密钥策略） |
| **成本** | 免费 | 免费 | 收费（约每月 1 美元 + API 调用费） |
| **使用场景** | 基本合规 | 低管理负担、需要审计 | **共享 AMI**、跨账户访问、特定轮换需求 |

### B. 按加密算法

- **对称（AES-256）：** 加解密用同一把密钥。AWS 服务（S3、EBS）使用。密钥永远不会以未加密形式离开 KMS。
- **非对称（RSA/ECC）：** 公钥/私钥对。用于**签名**操作，或让没有 AWS 凭证的用户在 AWS 之外做加密。

### C. 按密钥材料来源

- **AWS 生成（默认）：** 随机性由 AWS 生成。
- **导入（BYOK - 自带密钥）：**
    - 你在本地生成密钥材料并上传。
    - **限制：** **不支持自动轮换**。要轮换必须手动重新导入。
    - **风险：** 你要自己负责密钥的持久性；源密钥丢失即数据丢失。
- **自定义密钥库（Custom Key Store）：** 密钥在 **CloudHSM**（单租户硬件）中生成，但通过 KMS 管理。

## 5. KMS - 多区域密钥（Multi-Region Keys）

- **标准 KMS 密钥：** 严格限于区域内。不能用 `us-east-1` 的密钥解密 `eu-west-1` 的数据。
- **多区域密钥：**
    - 允许把主密钥复制到其他区域成为副本密钥。
    - **密钥 ID：** 在各区域**保持相同**。
    - **使用场景：** 灾难恢复（DR）或 DynamoDB 全局表——在故障切换区域需要立即解密数据，而无需重新加密。
- **考试提示：** 如果场景涉及"全局表"或"灾难恢复"，且在新区域重新加密数据"太耗时"，答案就是**多区域密钥**。

## 6. 带加密的 S3 复制

- **场景：** 把对象从源桶（区域 A）复制到目标桶（区域 B）。
- **未加密对象：** 自动复制（若已启用复制）。
- **加密对象（SSE-KMS）：**
    - 默认**不会**被复制。必须在复制配置中专门启用。
    - **所需权限：**
        1. 复制用的 IAM 角色必须对源密钥有 `kms:Decrypt` 权限。
        2. 该 IAM 角色必须对目标密钥有 `kms:Encrypt` 权限。
    - **限流：** 复制过程中如果达到 KMS 限额，可能收到 `ServiceUnavailable` 错误（申请提高 KMS 配额）。
- **考试提示：** 如果 S3 复制**只有**加密对象失败，检查 IAM 角色对_两个_区域 KMS 密钥的权限。

## 7. 加密 AMI 的共享流程

- **规则：** **不能**直接共享用默认 AWS 托管密钥加密的 AMI。
- **规则：** 如果 AMI 是用_共享方_的自定义密钥加密的，也不能直接共享（接收方无法访问那把密钥）。
- **正确流程：**
    1. 从 EC2 实例创建 AMI。
    2. **复制**该 AMI（镜像）到同区域或新区域。
    3. 复制过程中勾选"重新加密"，选择一把你控制的**客户托管密钥（CMK）**。
    4. 把目标账户 ID 加入该 CMK 的**密钥策略（Key Policy）**。
    5. 把 AMI 共享给目标账户。
    6. （可选但推荐）目标账户复制这个共享的 AMI，并用_他们自己的_密钥重新加密，从而彻底独立。
- **考试提示：** 用"默认" AWS 密钥加密的 AMI 永远不能共享。**必须**先复制并用 **CMK**（客户托管密钥）重新加密。

## 8. SSM Parameter Store 概览

- **所属服务：** AWS Systems Manager（SSM）。
- **目的：** 用于配置数据管理和密钥管理的安全、分层存储。
- **类型：** String、StringList、SecureString（使用 KMS 加密）。
- **层级（Tiers）：**
    - **标准：** 免费，上限 10,000 个参数，单个最大 4KB。
    - **高级：** 收费，可存 100,000+ 个参数，单个最大 8KB，支持策略（如过期策略）。
- **层次结构：** `/prod/db/password` 或 `/dev/app/url`。（可以基于路径设置 IAM 权限）。
- **考试提示：** 如果题目问"存储配置字符串"或"密钥"，但**没有**提到自动轮换，选 **SSM Parameter Store**（更便宜/免费）。

## 9. AWS Secrets Manager - 概览

- **目的：** 专门针对密钥（密码、API Key）的较新服务。
- **与 SSM Parameter Store 的关键区别：**
    - **自动轮换：** Secrets Manager 可以用 Lambda 函数自动轮换 RDS、Redshift 和 DocumentDB 的凭证。
    - **跨账户访问：** 跨账户共享密钥更容易。
- **成本：** 比 SSM Parameter Store 贵。
- **考试提示：** 关键词是**"自动轮换（Automatic Rotation）"**。如果场景要求每 X 天自动轮换数据库密码，答案是 **Secrets Manager**。

## 10. AWS Certificate Manager（ACM）

- **目的：** 申请、管理和部署 SSL/TLS 证书。
- **集成：** 负载均衡器（ALB/NLB）、CloudFront、API Gateway。
- **公有证书：** 免费，自动续期（如果通过 CNAME 做了 DNS 验证）。
- **私有证书：** 收费（通过私有 CA）。
- **限制：**
    - **不能**导出公有 ACM 证书（私钥）直接安装到 EC2 实例上。要用 ACM 证书，必须把 EC2 放到负载均衡器或 CloudFront 后面。
- **考试提示：** 如果一个 EC2 上的遗留应用需要把证书_直接_装在操作系统上（而不是负载均衡器上），就**不能**用 ACM 公有证书。必须从外部购买证书导入，或使用 ACM 私有 CA。

## 11. AWS CloudHSM（硬件安全模块）- 概览

- **定义：** AWS 云中的专用物理硬件安全模块。
- **关键特征：** 单租户（专用）硬件。
- **访问控制：** 你对硬件和用户账户有完全控制权。AWS **完全无法访问**（连丢失的密钥都无法帮你找回）。
- **合规：** **FIPS 140-2 Level 3**（比 KMS 更高的安全标准）。
- **支持的接口：** 行业标准协议：
    - **PKCS#11**
    - **JCE**（Java 加密扩展）
    - **Microsoft CNG**（下一代加密技术）
- **使用场景：**
    - 要求专用硬件的严格监管合规。
    - 把 Web 服务器（如 Nginx/Apache）的 SSL 卸载到专用硬件。
    - Oracle TDE（透明数据加密），主密钥放在 HSM 中。

## 12. AWS CloudHSM vs AWS KMS（对比）

- **AWS KMS：** 多租户托管服务。硬件信任根由 AWS 管理。
- **AWS CloudHSM：** 单租户专用硬件。由你自己管理。

| 特性 | AWS KMS | AWS CloudHSM |
| --- | --- | --- |
| **租户模式** | **多租户**（共享硬件） | **单租户**（专用硬件） |
| **密钥找回** | AWS 保证持久性。 | 凭证丢失则**永久丢失**。 |
| **合规** | FIPS 140-2 Level 2（标准）。 | **FIPS 140-2 Level 3**（高安全）。 |
| **协议** | 仅 AWS API / CLI / SDK。 | PKCS#11、JCE、Microsoft CNG。 |
| **管理** | AWS 负责管理。 | **你**负责管理。 |

- **考试提示：** 看到关键词"单租户"、"专用硬件"、"FIPS 140-2 Level 3"或 "PKCS#11"，**永远**指向 **CloudHSM**。

## 13. Web 应用防火墙（WAF）

- **层级：** 第 7 层（应用层）。
- **防护对象：** 常见 Web 攻击（SQL 注入、跨站脚本 XSS、恶意爬虫）。
- **可部署在：**
    - 应用负载均衡器（ALB）。
    - API Gateway。
    - CloudFront。
    - AppSync。
- **组件：** Web ACL（访问控制列表）→ 规则（IP 集合、地理匹配、字符串匹配）。
- **考试提示：** 要拦截含 **SQL 注入**的攻击或屏蔽**特定国家**的流量，用 **WAF**。

## 14. AWS Shield - DDoS 防护

- **Shield Standard：**
    - 免费，所有客户默认开启。
    - 防护第 3 层（网络层）和第 4 层（传输层）攻击（SYN 洪水、UDP 洪水）。
- **Shield Advanced：**
    - 收费（约每月 3,000 美元）。
    - 防护复杂的第 3、4、7 层攻击。
    - **权益：**
        - 7×24 小时联系 DDoS 响应团队（DRT）。
        - **费用保护：** 补偿因 DDoS 导致的账单激增（例如攻击流量导致的自动扩容费用）。
        - 免费附带 WAF。
- **考试提示：** 如果公司担心 DDoS 攻击导致 ASG 扩容带来**"费用激增"**，答案是 **Shield Advanced**（费用保护）。

## 15. AWS Firewall Manager

- **目的：** 从一个中心位置管理 AWS 组织内**所有**账户的安全规则。
- **前提：** 需要 AWS Organizations。
- **管理内容：** WAF 规则、AWS Shield Advanced、安全组、Network Firewall 策略。
- **使用场景：** "确保组织内每个 ALB 都应用了某条 WAF 规则。"
- **考试提示：** 题目问"跨**多个账户**集中管理 WAF 规则"，选 **Firewall Manager**。

## 16. DDoS 防护最佳实践（考试场景）

1. **缩小攻击面：** 用 **CloudFront**（在边缘缓存内容）+ **WAF**（过滤恶意请求）+ **Shield**（DDoS 缓解）。
2. **弹性扩展：** 用**自动扩展组**吸收攻击流量。
3. **隐藏源站：** 用安全组确保 ALB/EC2 只接受来自 CloudFront IP 段（托管前缀列表）的流量。

- **考试提示：** "终极" DDoS 架构几乎总是让 **CloudFront** 站在你的应用（S3 或 ALB）前面。

## 17. Amazon GuardDuty

- **类型：** 智能威胁检测。
- **机制：** 用机器学习分析日志。
- **数据来源：**
    1. CloudTrail 管理事件。
    2. VPC 流日志（Flow Logs）。
    3. DNS 日志。
    4. EKS 审计日志 / RDS 登录事件。
- **告警示例：** "实例与已知恶意 IP 通信"、"检测到加密货币挖矿"、"异常的 API 调用"。
- **动作：** 它_不会_自动拦截流量。它把发现（findings）发到 Security Hub / EventBridge（后者可以触发 Lambda 做修复）。
- **考试提示：** GuardDuty 能检测**"加密货币挖矿"**。同时记住它只_检测_（基于日志），不_拦截_（它不是防火墙）。

## 18. Amazon Inspector

- **类型：** 自动化漏洞管理。
- **范围：**
    - **EC2 实例：** 扫描操作系统漏洞（CVE）和意外的网络暴露。深度扫描需要 SSM Agent（Systems Manager）。
    - **容器镜像（ECR）：** 扫描推送到 ECR 的镜像漏洞。
    - **Lambda 函数：** 扫描代码漏洞。
- **关键词：** "评估应用的漏洞/暴露面"、"CVE 检查"。
- **考试提示：** 题目提到**"软件包漏洞"**或**"CVE（常见漏洞与暴露）"**，答案是 **Inspector**。

## 19. Amazon Macie

- **类型：** 数据隐私与安全。
- **目标：** 仅限 **Amazon S3**。
- **机制：** 用机器学习和模式匹配发现**敏感数据**（PII——个人身份信息，如信用卡号、社保号、护照号）。
- **告警：** 在未加密/公开的桶中发现敏感数据时，通过 EventBridge 发出告警。
- **考试提示：** Macie = **S3** 中的 **PII**。它不适用于数据库（DynamoDB/RDS），只针对 S3。


---

# 第 21 章：网络 - VPC

## 1. 章节引言

网络在解决方案架构师助理考试中占很大比重。本章聚焦 VPC（虚拟私有云），它是你在 AWS 云中的隔离网络环境。理解如何保护、路由和连接 VPC 是通过考试的关键。

- **考试提示：** 网络 + 安全加起来常占考试场景的约 50%。尽早掌握 VPC 基础。

## 2. CIDR、私有 IP vs 公有 IP

**CIDR（无类别域间路由）：**

- 用于定义 IP 网段（例如 `10.0.0.0/16`）。
- **斜杠表示法（子网掩码）：**
    - `/32` = 1 个 IP 地址。
    - `/24` = 256 个 IP 地址。
    - `/16` = 65,536 个 IP 地址。
- **VPC CIDR：** 最小 `/28`（16 个 IP），最大 `/16`（65,536 个 IP）。

**私有 IP vs 公有 IP（RFC 1918）：**

- **私有 IP：** 仅在网络内部可访问，在互联网上不可路由。
    - `10.0.0.0` - `10.255.255.255`（A 类）
    - `172.16.0.0` - `172.31.255.255`（B 类）
    - `192.168.0.0` - `192.168.255.255`（C 类）
- **公有 IP：** 可从互联网访问，全网唯一。
- **考试提示：** 背下 RFC 1918 私有网段。如果题目里私有数据库配了公网 IP（例如 `54.x.x.x`），那是一个安全警示信号。

## 3. 默认 VPC 概览

每个新 AWS 账户在每个区域都自带一个默认 VPC。

- **属性：**
    - CIDR 块为 `172.31.0.0/16`。
    - 在每个可用区（AZ）有一个公有子网。
    - 附加了一个互联网网关（Internet Gateway）。
    - 默认路由表把 `0.0.0.0/0` 路由到互联网网关。
    - **用途：** 快速上手。
- **考试提示：** 对于生产场景或"安全架构"类题目，答案通常是创建**自定义 VPC**，而不是用默认 VPC。

## 4. VPC 概览

- **VPC（虚拟私有云）：** AWS 云中逻辑隔离的一部分。
- **范围：** 绑定区域（VPC 不能跨区域）。
- **限额：** 每个账户每个区域默认 5 个 VPC（软限制）。
- **租户模式：**
    - **默认：** 共享硬件。
    - **专用（Dedicated）：** 为你的 VPC 提供专用硬件（昂贵）。
- **考试提示：** 记住 VPC 属于特定**区域**，而子网属于特定**可用区**。

## 5. 子网概览

- **范围：** 绑定可用区（子网不能跨 AZ）。
- **AWS 保留 IP：** AWS 在_每个_子网 CIDR 中保留前 4 个和最后 1 个 IP（共 5 个）。
    - 示例：`10.0.0.0`（网络地址）、`10.0.0.1`（路由器）、`10.0.0.2`（DNS）、`10.0.0.3`（保留）、`10.0.0.255`（广播）。
- **公有子网：** 路由表中有一条把 `0.0.0.0/0` 指向互联网网关（IGW）的路由。
- **私有子网：** _没有_通向 IGW 的路由。
- **考试提示：** 如果题目问一个 `/28` 子网有多少可用 IP，答案是 16 - 5 = **11**。永远记得减 5。

## 6. 互联网网关（IGW）与路由表

**互联网网关（IGW）：**

- 让 VPC 内的资源能访问互联网。
- 水平扩展、冗余、高可用（没有瓶颈）。
- 每个 VPC 只能有**一个** IGW。

**路由表（RT）：**

- 包含决定网络流量去向的规则（路由）。
- **主路由表：** 自动创建。
- **自定义路由表：** 可以创建并关联到特定子网。
- **规则：** 匹配最具体的路由（最长前缀匹配）。
- **考试提示：** 一个 VPC 不能附加多个 IGW。互联网访问的高可用由 IGW 内部自行保障，不需要两个。

## 7. 堡垒机（Bastion Hosts / 跳板机）

- 位于**公有子网**的一台 EC2 实例，用于 SSH/RDP 跳转到私有子网中的私有实例。
- **安全：**
    - 安全组应只允许来自你公司/家庭 IP 的 SSH（22 端口），不要开放 `0.0.0.0/0`。
    - 安全地使用密钥对。
- **考试提示：** 如果场景提到"安全地管理私有子网中的实例"且 Session Manager 不在选项里，选堡垒机。确保安全组把入站限制到特定 IP 段（CIDR）。

## 8. NAT 实例（旧方案）

- **概念：** 在公有子网中启动的一台 EC2 实例，让私有实例可以访问互联网（仅出站）。
- **配置：** 必须在该实例上禁用**源/目标检查（Source/Destination Check）**。
- **缺点：**
    - 天生不是高可用的。
    - 带宽受实例类型限制。
    - 需要用户自己创建和维护安全补丁。
- **考试提示：** NAT 实例的关键步骤是禁用**源/目标检查**。不做这一步它就无法转发流量。

## 9. NAT 网关（NAT Gateways）

- **概念：** AWS 托管的 NAT（网络地址转换）服务。允许私有子网发起出站互联网访问，但阻止外部主动进入。
- **范围：** 创建在特定的 AZ 中。
- **要求：** 必须部署在**公有子网**，并需要一个**弹性 IP（EIP）**。
- **高可用：** 要实现 HA，需要在每个 AZ 各建一个 NAT 网关。
- **带宽：** 自动扩展，最高 45 Gbps。
- **成本：** 按小时收费 + 数据处理费。
- **考试提示：** 如果题目要求 NAT 的"高可用"，必须在**每个**可用区各部署一个 NAT 网关。一个 NAT 网关不能跨多个 AZ。

## 10. NACL 与安全组（考试重中之重）

|特性|安全组（SG）|网络 ACL（NACL）|
|---|---|---|
|**层级**|实例级|子网级|
|**状态**|**有状态：** 允许流量进入后，返回流量自动被允许出去。|**无状态：** 必须分别显式允许进和出的流量。|
|**规则**|只有允许规则。（末尾隐式拒绝）。|既有允许规则也有拒绝规则。|
|**顺序**|评估所有规则后再做决定。|按编号顺序处理（编号小的先匹配）。|
|**作用范围**|作用于特定 ENI/实例。|作用于子网内的所有实例。|

- **临时端口（Ephemeral Ports）：** 对 NACL 来说，记得放行临时端口（1024-65535）上的返回流量。
- **考试提示：** 如果需要**屏蔽**某个特定的恶意 IP，**必须**用 **NACL**。安全组不能显式拒绝流量（它只能"允许"）。

## 11. VPC 对等连接（VPC Peering）

- 通过 AWS 私有网络连接两个 VPC。
- **功能：** 两边的实例表现得就像在同一个网络里。
- **规则：**
    - CIDR 网段不能重叠。
    - **不可传递（Non-Transitive）：** A 与 B 对等、B 与 C 对等，A 不能和 C 通信。（A 必须直接与 C 对等）。
    - 支持跨区域和跨账户。
    - 必须更新两个 VPC 的路由表。
- **考试提示：** 小心"传递对等"陷阱。A 不能借道 B 与 C 通信，A 必须直接与 C 对等。

## 12. VPC 端点（PrivateLink）

允许你通过私有网络连接 AWS 服务（不需要互联网网关）。

1. **接口端点（Interface Endpoints）：**
    - 使用带私有 IP 的 **ENI（弹性网络接口）**。
    - 由 PrivateLink 提供支持。
    - 支持大多数 AWS 服务（CloudWatch、SageMaker 等）。
    - 收费（按小时 + 数据量）。
2. **网关端点（Gateway Endpoints）：**
    - 通过**路由表**中的目标实现。
    - 只支持 **S3** 和 **DynamoDB**。
    - **免费**。

- **考试提示：** 题目专门提到 "S3" 或 "DynamoDB"，找**网关端点**。几乎所有其他 AWS 服务（如 Kinesis、CloudWatch），找**接口端点**。

## 13. VPC 流日志（Flow Logs）

- 捕获进出网络接口的 IP 流量信息。
- **级别：** VPC、子网或 ENI 级别。
- **目的地：** S3、CloudWatch Logs 或 Kinesis Data Firehose。
- **用途：** 排查连通性问题、安全监控。
- **注意：** 它捕获的是元数据（源 IP、目标 IP、端口、动作），不是数据包的实际内容。
- **考试提示：** 流日志**不**捕获数据包内容（载荷）。如果题目要求检查_内容_（例如恶意软件特征），流日志是错误答案（应该用流量镜像 Traffic Mirroring 或 Network Firewall）。

## 14. 站点到站点 VPN、虚拟私有网关与客户网关

通过**公共互联网**把本地数据中心连接到 AWS VPC。

- **虚拟私有网关（VGW）：** **AWS 这一侧**的 VPN 集中器。
- **客户网关（CGW）：** **客户（本地）这一侧**的物理设备或软件。
- **站点到站点 VPN：** 连接 VGW 和 CGW 的 IPsec 加密隧道。
- **搭建：** 部署快，但延迟取决于互联网状况。
- **考试提示：** VGW 附加在 VPC 上；CGW 代表客户办公室的物理设备。不要弄反。

## 15. AWS VPN CloudHub

- **概念：** 借助 AWS VPN 基础设施，让一个远程站点与另一个远程站点安全通信。
- **架构：** **中心辐射（Hub-and-Spoke）**模型，AWS VPC（准确说是虚拟私有网关）充当中心（Hub）。
- **要求：** 多个客户网关（每个远程站点一个）必须连接到_同一个_虚拟私有网关（VGW）。
- **路由：** 使用 **BGP**（边界网关协议）。每个客户网关必须使用唯一的 BGP 自治系统号（ASN）。
- **成本：** 低成本；走公共互联网。
- **考试提示：** 场景描述多个分支办公室既要互相通信又要连 AWS，还要求低成本方案（避开 MPLS/Direct Connect）时，选 **VPN CloudHub**。

## 16. Direct Connect（DX）与 Direct Connect Gateway

通过**专用的私有物理光纤连接**把本地连到 AWS。

- **优点：** 稳定的网络性能、高带宽、更低的数据流出费率。
- **安全：** 流量是私有的，但默认**不加密**。
- **Direct Connect Gateway：** 让一条 Direct Connect 连接能对接**多个区域**的 VPC。
- **搭建：** 需要数周/数月（要拉物理线路）。
- **考试提示：** Direct Connect 默认**不**加密。如果要求"专线"+"加密"，需要 DX + VPN。

## 17. Direct Connect + 站点到站点 VPN

- 当你既需要**专用连接**（Direct Connect）又需要**加密**（VPN）时。
- 在 Direct Connect 上建立公共虚拟接口（VIF），然后在这条连接上建 IPsec VPN 隧道。
- 结果：安全、加密、专用、高带宽的连接。
- **考试提示：** 这是"高安全合规（加密）"+"高带宽/可靠性"的标准答案。

## 18. Transit Gateway（TGW，中转网关）

- **中心辐射模型：** 可连接数千个 VPC 和本地网络。
- **传递路由：** 与对等连接不同，TGW 支持传递路由（A ↔ TGW ↔ B）。
- **跨区域：** TGW 之间可以跨区域对等。
- **复杂度：** 大幅降低网状对等网络的复杂度。
- **组播：** TGW 是唯一支持 IP 组播（Multicast）的服务。
- **考试提示：** 题目提到"简化网络拓扑"或连接"数百/数千个 VPC"，答案是 **Transit Gateway**。

## 19. VPC 流量镜像（Traffic Mirroring）

- 允许你捕获并检查网络流量。
- **机制：** 从一个弹性网络接口（源）复制流量，发送到一个目标（Target）。
- **目标：** 网络负载均衡器（NLB）或另一个 ENI。
- **使用场景：** 入侵检测系统（IDS）、数据包检查、合规监控。
- **考试提示：** 这用于"深度包检测"或分析实际载荷以发现安全威胁——这是流日志做不到的。

## 20. VPC 的 IPv6

- VPC 可以以双栈模式（IPv4 + IPv6）运行。
- **寻址：** IPv6 地址是全球唯一的（公有）。在 AWS VPC 语境下没有私有 IPv6 网段。
- 每个实例会获得一个全球单播 IPv6 地址。
- **考试提示：** （目前）不能为 VPC 禁用 IPv4；IPv6 是在 IPv4 _基础上追加_启用的（双栈）。

## 21. 仅出站互联网网关（Egress-Only Internet Gateway）

- **仅限 IPv6。**
- 类似 NAT 网关，但服务于 IPv6。
- 允许实例通过 IPv6 主动发起对互联网的出站连接，但**阻止入站**连接。
- **更新路由表：** 把 `::/0` 路由到 Egress-Only Internet Gateway。
- **考试提示：** Egress-Only Internet Gateway 是 **IPv6 专属**的。IPv4 场景不要选它。

## 22. VPC 章节总结

- **安全：** NACL 做无状态的子网级拦截，安全组做有状态的实例级放行。
- **连接：** 简单的 1 对 1 用对等连接（Peering），复杂的多对多用 Transit Gateway。
- **混合云：** 要快速/加密用 VPN，要可靠/高吞吐用 Direct Connect。
- **访问：** SSH 用堡垒机，内网访问 AWS 服务用 VPC 端点。
- **出站：** IPv4 用 NAT 网关 / IPv6 用 Egress-Only IGW。
- **考试提示：** 永远确认流量是否需要加密。DX 是私有但不加密的，VPN 走公网但是加密的。

## 23. AWS 网络费用

- **入站数据：** 一般免费。
- **出站数据：** 收费。
- **私有 IP 通信：** 一般比公有 IP 便宜。
- **NAT 网关：** 按小时收费 + 每 GB 数据处理费（可能变得很贵）。
- **VPC 端点：** 接口端点按小时收费；网关端点免费。
- **跨 AZ 数据传输：** 收费（使用公有 IP 或对等连接时）。
- **考试提示：** 同区域内实例之间用私有 IP 传数据通常是最省钱的方式。

## 24. AWS Network Firewall

- 托管的、有状态的网络防火墙和入侵检测/防御服务（IDS/IPS）。
- **OSI 层级：** 保护第 3 到第 7 层。
- **能力：**
    - Web 过滤（域名）。
    - 检查 VPC 流量。
    - 兼容 "Suricata" 规则。
- **部署：** 在 VPC 中通过"防火墙子网"部署。必须更新路由表，让流量经过防火墙端点。
- **考试提示：** 如果题目要求对整个 VPC 做"域名过滤"（例如为公司实例屏蔽 `facebook.com`），AWS Network Firewall 是有力候选（DNS Firewall 是另一个，但 Network Firewall 覆盖面更广）。


---

# 第 22 章：AWS 灾难恢复与迁移（Disaster Recovery & Migrations）

## 1. AWS 中的灾难恢复（DR）

SAA-C03 考试非常侧重 **RTO/RPO** 与**成本**之间的权衡。

- **RPO（恢复点目标）：** 可接受的最大数据丢失量（以时间衡量，例如"最多丢 1 小时的数据"）。
- **RTO（恢复时间目标）：** 可接受的最长停机时间（多久之后服务必须恢复）。

### 四种 DR 策略（从最便宜/最慢 到 最贵/最快）

|策略|描述|RTO/RPO|成本|
|---|---|---|---|
|**备份与恢复（Backup & Restore）**|把数据备份到 S3 或快照。只有灾难发生时才恢复。基础设施是灾难发生_之后_才创建的。|高（数小时/数天）|$（最低）|
|**引燃灯（Pilot Light）**|核心数据持续复制（例如 RDS 只读副本），但应用服务器是关闭的或规模极小。灾难时"点亮"并扩容服务器。|中（几十分钟）|$$|
|**温备（Warm Standby）**|完整环境的缩小版一直在运行（例如最小容量的自动扩展组）。故障切换时扩容。|低（分钟级）|$$$|
|**多站点/双活（Multi-Site / Active-Active）**|多个区域/AZ 同时运行全量容量。流量做负载均衡（Route 53）。|接近零|$$$$（最高）|

## 2. Elastic Disaster Recovery（AWS DRS）

- **目的：** AWS 灾难恢复的标准服务（取代了 CloudEndure DR）。
- **工作原理：**
    1. 在源服务器（本地或其他云）上安装 DRS Agent。
    2. 持续的块级复制到 AWS 的**暂存区（Staging Area）**（低成本的轻量 EC2 实例）。
    3. **故障切换：** 触发时，DRS 基于复制的数据启动配置完整的 EC2 实例。
- **考试关键词：** "持续复制"、"块级"、"物理机到云的 DR"、"亚秒级 RPO"。

## 3. 数据库迁移服务（DMS）

- **目的：** 快速、安全地把数据库迁移到 AWS。
- **关键特性：** 迁移期间源数据库保持**完全可用**。
- **同构迁移：** 相同引擎（例如 Oracle 迁到 EC2/RDS 上的 Oracle）。不需要转换表结构。
- **异构迁移：** 不同引擎（例如 Oracle 迁到 Aurora PostgreSQL）。需要 **SCT**。
- **AWS Schema Conversion Tool（SCT）：** 必须在 DMS _之前_运行，把源引擎的表结构（表、索引、视图）转换成目标引擎的格式。
- **CDC（变更数据捕获）：** 在 DMS 中启用后，初始加载完成后可持续同步增量变更。

## 4. RDS 与 Aurora 迁移

### RDS

- **快照与恢复：** 移动数据或创建环境的标准方式。
- **只读副本：**
    - 可以在同区域内或**跨区域**。
    - **DR 策略：** 创建一个跨区域只读副本。主区域故障时，把副本**提升（Promote）**为独立主库（RTO 为分钟级）。

### Aurora

- **Aurora 全球数据库：**
    - 跨多个 AWS 区域。
    - 物理复制（非常快，延迟 < 1 秒）。
    - **快速故障切换：** 主区域挂掉时提升从区域（RTO < 1 分钟）。
- **Aurora 克隆：**
    - 几乎瞬间从现有集群创建一个新集群。
    - **使用场景：** 测试/预发环境。_通常不_用于 DR，因为克隆在同一个区域内。

### ⚡ 具体迁移场景（考试常客）

#### 1. RDS MySQL → Aurora MySQL（或 RDS Postgres → Aurora Postgres）

- **方案 A：Aurora 只读副本（停机最短，推荐）**
    1. 从源 RDS 实例创建一个 **Aurora 只读副本**。
    2. 等复制延迟降到 0。
    3. 停止对 RDS 实例的写入。
    4. 把 Aurora 只读副本**提升**为独立的 Aurora 集群。
- **方案 B：从快照迁移**
    - 给 RDS 做快照 → "迁移快照"到 Aurora。
    - **缺点：** 更慢，恢复数据到新集群期间需要停机。

#### 2. 外部数据库（本地/EC2）→ Aurora

- **外部 MySQL → Aurora MySQL**
    - **Percona XtraBackup（物理备份）：** 用 Percona XtraBackup 做物理备份 → 上传到 **S3** → 直接恢复到 Aurora。（比逻辑导出快得多）。
    - **mysqldump（逻辑备份）：** 标准 SQL 导出（大数据集较慢）。
- **外部 PostgreSQL → Aurora PostgreSQL**
    - **从 S3 导入：** 用 `pg_dump` 导出数据 → 上传到 **S3** → 用 Aurora 的 `aws_s3` 扩展导入数据。
    - **DMS：** 用于持续复制和最短停机的切换。

## 5. 本地环境与 AWS 结合的策略（混合云）

### 虚拟机管理与迁移规划

- **Amazon Linux 2 AMI：**
    - 可以下载 Amazon Linux 2 的虚拟机镜像（`.iso` 格式）。
    - 可运行于：VMware、KVM、VirtualBox（Oracle VM）、Microsoft Hyper-V。
    - **用途：** 在本地用与 AWS 完全相同的操作系统开发和测试应用。
- **VM Import/Export：**
    - **迁入：** 把现有的虚拟机镜像导入为 EC2 实例。
    - **DR 策略：** 在 AWS 为本地虚拟机建立一个灾备仓库。
    - **导出：** 也可以把实例从 EC2 导出回你本地的虚拟化环境。
- **AWS Application Discovery Service：**
    - **目的：** 收集本地服务器的信息以规划迁移。
    - **收集内容：** 服务器利用率（CPU/内存）和**依赖关系映射**（哪台服务器和哪台通信）。
    - **跟踪：** 与 **AWS Migration Hub** 集成，跟踪迁移进度。
- **战略级迁移服务（本地场景）：**
    - **DMS：** 支持 本地 → AWS、AWS → AWS、AWS → 本地 的复制。支持各种数据库（Oracle、MySQL、DynamoDB 等）。
    - **MGN（Application Migration Service）：** 把本地正在运行的服务器增量复制到 AWS。

### 存储策略

1. **AWS Storage Gateway：** 把本地应用连接到云存储。
    - **S3 File Gateway：**
        - 接口：NFS 或 SMB。
        - 后端：S3。
        - 场景：把文件搬到 S3、内容仓库。本地缓存让常用文件访问飞快。
    - **Volume Gateway：**
        - 接口：iSCSI（块存储）。
        - **Stored 模式：** 所有数据留在本地；异步备份到 S3（适合对_全部_数据都要低延迟访问）。
        - **Cached 模式：** 主数据在 S3；本地只缓存常用数据（适合扩展本地存储）。
    - **Tape Gateway：**
        - 接口：虚拟磁带库（VTL）。
        - 后端：S3 Glacier / Deep Archive。
        - 场景：替代物理磁带备份（如 BackupExec、Veeam）。
2. **AWS DataSync**
    - **目的：** 自动化本地与 AWS 存储（S3、EFS、FSx）之间的大规模数据搬运。
    - **技术：** 使用专有协议（不是走广域网的标准 NFS/SMB），比开源工具最快可达 **10 倍**。
    - **代理：** 需要在本地部署 DataSync Agent。

## 6. AWS Backup

- **目的：** 集中式托管服务，自动化各 AWS 服务的备份。
- **支持的服务：** EC2、EBS、RDS、DynamoDB、Aurora、EFS、FSx、Storage Gateway、S3。
- **关键功能：**
    - **备份计划：** 定义频率（cron）和保留期。
    - **跨区域备份：** 自动把备份复制到另一个区域用于 DR。
    - **跨账户备份：** 通过 AWS Organizations 管理。
    - **Vault Lock：** 强制执行 **WORM**（一次写入多次读取）合规。防止备份被删除（抵御勒索软件）。

## 7. Application Migration Service（MGN）

- **目的：** 把服务器迁移到 AWS 的主力"直接搬迁（Lift and Shift / Rehost）"工具。
- **取代：** Server Migration Service（SMS）和 CloudEndure Migration。
- **工作原理：**
    - 基于代理的块级复制。
    - 把源服务器（物理机、VMware、Hyper-V、Azure 虚拟机）转换成原生 AWS EC2 实例。
- **切换（Cutover）：** 停机极短。你可以定义一个"切换窗口"。

## 8. 传输大数据集（"Snow" 家族）

**经验法则：** 如果通过互联网/Direct Connect 传输要超过 1 周，就用 Snowball。

|设备|容量|使用场景|计算能力？|
|---|---|---|---|
|**Snowcone**|8 TB|小巧、便携、恶劣环境。可以邮寄。|基础（物联网/校验）|
|**Snowball Edge 存储优化型**|80 TB|海量数据传输（PB 级）。|标准 EC2/Lambda|
|**Snowball Edge 计算优化型**|42 TB|边缘计算（边缘 AI/ML 推理）+ 传输。|强大的 EC2/GPU|
|**Snowmobile**|100 PB|EB 级数据传输（真的是一辆卡车）。|无|

前三种确实是通过**快递邮寄**的方式往返，但最后一个 **Snowmobile 不是快递，而是一辆真正的 45 英尺重型集装箱卡车**。

- **OpsHub：** 用于在本地管理 Snow 设备的图形界面软件。
- **Direct Connect（DX）：** 专用私有光纤连接。适合持续的高吞吐量，但搭建耗时（数月）。DX 用于_长期_混合云连接，Snowball 用于_一次性_海量迁移。

## 9. VMware Cloud on AWS

- **概念：** 在 AWS **裸金属（Bare Metal）**基础设施上运行完整的 VMware 软件栈（vSphere、vSAN、NSX）。
- **主要使用场景：**
    - 无需重构应用的快速迁移。
    - 继续用现有的 VMware 工具（vCenter）管理 AWS 资源。
- **vMotion：** 可以把虚拟机从本地数据中心在线迁移（vMotion）到 VMware Cloud on AWS，**零停机**。
- **集成：** 可以通过弹性网络接口（ENI）直接访问原生 AWS 服务（S3、RDS 等）。

### ⚡ 考试速查口诀

1. **异构数据库迁移？** → **DMS + SCT**。
2. **服务器直接搬迁？** → **Application Migration Service（MGN）**。
3. **物理磁带？** → **Tape Gateway**。
4. **NFS/SMB 到 S3？** → **File Gateway**。
5. **iSCSI 块存储？** → **Volume Gateway**。
6. **需要 < 15 分钟的 RPO/RTO 又想省钱？** → **Pilot Light**（如果 RTO 要求分钟级，则 Warm Standby）。
7. **网速慢还要传 500 TB？** → **Snowball Edge**。
8. **防止备份被误删？** → **AWS Backup Vault Lock**。
9. **外部 MySQL 迁 Aurora？** → **Percona XtraBackup 到 S3**。


---

# 第 23 章：更多解决方案架构（More Solution Architectures）

## 1. AWS 中的事件处理

用 SQS、SNS 和 Lambda 解耦应用。

### SQS + Lambda

- **模式：** SQS 队列 → Lambda 函数。
- **机制：** Lambda **轮询** SQS 队列。
- **行为：** 处理失败时，消息会回到队列并重试。多次失败后可以进入死信队列（DLQ）。

### SNS + Lambda

- **模式：** SNS 主题 → Lambda 函数。
- **机制：** **异步**推送。SNS 把消息推给 Lambda。
- **行为：** Lambda 失败时，SNS 可以重试（可配置）。可以给 Lambda 挂一个 DLQ 收集失败的事件。

### 扇出模式（Fan Out）

- **问题：** 想把一条消息发送给多个独立的处理队列。
- **解决：** 向 SNS 发布一次 → 多个 SQS 队列订阅这个 SNS 主题。
- **架构：** SDK → SNS 主题 → 队列 A（处理 A）和 队列 B（处理 B）。

### S3 事件通知

- **触发事件：** `S3:ObjectCreated`、`S3:ObjectRemoved` 等。
- **功能：** 可按对象名过滤（例如 `*.jpg`）。
- **目的地：** SNS、SQS、Lambda。
- **延迟：** 通常是秒级，但也可能要几分钟。
- **模式：** 上传图片 → S3 → SQS → Lambda（生成缩略图）。_中间加 SQS 可以保证 Lambda 忙不过来时消息不丢失。_

### S3 + EventBridge

- **演进：** S3 可以把事件发送到 **Amazon EventBridge**。
- **能力：**
    - 高级过滤（元数据、对象大小）。
    - 超过 18 种 AWS 服务目的地（Step Functions、Kinesis 等）。
    - 事件归档/重放能力。

### 拦截 API 调用（EventBridge）

- **使用场景：** 对基础设施变更做出反应（例如某个 DynamoDB 表被删除）。
- **流程：** 用户调用 `DeleteTable` → CloudTrail 记录 API 调用 → EventBridge 规则匹配事件 → SNS（发告警）。

### API Gateway + Kinesis

- **模式：** 高吞吐量数据摄取。
- **流程：** 客户端 → API Gateway → Kinesis Data Streams → Kinesis Data Firehose → Amazon S3（以 `.json` 格式存储）。

## 2. AWS 中的缓存策略

缓存能降低延迟，并减轻计算/数据库资源的压力。

|层级|技术|细节|
|---|---|---|
|**边缘**|**CloudFront**|把内容缓存在离用户近的地方。TTL、地理路由。最适合静态内容。|
|**网关**|**API Gateway**|缓存 API 响应。减轻后端 Lambda/EC2 的负载。|
|**计算**|**EC2 / Lambda**|本地缓存（如适用/有状态时）或自行管理缓存逻辑。|
|**数据库**|**ElastiCache / DAX**|**Redis/Memcached：** 给 RDS/数据库做缓存。**DAX：** 专门给 DynamoDB 的内存缓存。|

## 3. 在 AWS 中屏蔽一个 IP 地址

策略取决于流量从哪里进入。

### 1. VPC / 子网层面（NACL）

- **组件：** 网络 ACL（NACL）。
- **机制：** NACL 支持**拒绝（Deny）**规则。
- **操作：** 为该 IP 创建一条拒绝规则，放在最前面（编号最小）。
- **优点：** 高效，流量在到达实例之前就被拦截。

### 2. EC2 实例层面（安全组）

- **组件：** 安全组（SG）。
- **机制：** 安全组_只_支持**允许（Allow）**规则。
- **局限：** **不能**显式屏蔽某个 IP。只能通过"不给它写允许规则"来间接屏蔽（但如果公共 Web 服务器必须允许 `0.0.0.0/0`，这就行不通了）。
- **结论：** 想在放行全网的同时屏蔽特定 IP，安全组帮不上忙。

### 3. 应用负载均衡器（ALB）层面

- **组件：** **AWS WAF**（Web 应用防火墙）或 NACL（在 ALB 所在子网）。
- **WAF：** 给 ALB 附加一个 Web ACL，用 **IP 地址过滤**规则屏蔽恶意 IP。
- **NACL：** 可以在 ALB 所在的子网屏蔽 IP。

### 4. CloudFront 层面（边缘）

- **场景：** 客户端 → CloudFront → ALB → EC2。
- **问题：** ALB 看到的是 CloudFront 的 IP，不是客户端的 IP。因此 **ALB 子网上的 NACL 无法**屏蔽客户端 IP。
- **解决：**
    - **AWS WAF：** 把 WAF 附加到 **CloudFront** 上，在边缘屏蔽 IP。
    - **CloudFront 地理限制：** 屏蔽整个国家。

## 4. AWS 上的高性能计算（HPC）

运行复杂的计算密集型工作负载。

### 数据管理与传输

- **AWS Direct Connect：** 通过私有安全网络以 GB/s 级速度传数据。
- **Snowball / Snowmobile：** 以物理方式搬运 PB 级数据。
- **AWS DataSync：** 自动化本地与 S3/EFS/FSx 之间的数据传输。

### 计算与网络

- **EC2 实例：** 使用 CPU 优化或 GPU 优化的实例。
- **Spot Fleet：** 为无状态工作负载省钱。
- **置放群组：** 使用 **Cluster（集群）**置放群组，获得实例间低延迟、10Gbps 以上的网络（同机架/同 AZ）。
- **增强网络（ENA）：** 最高 100 Gbps。
- **Elastic Fabric Adapter（EFA）：**
    - 绕过 Linux 操作系统实现低延迟传输。
    - 非常适合 **MPI**（消息传递接口）和紧耦合的工作负载。

### 存储

- **实例存储（Instance Store）：** 性能最高（数百万 IOPS）、低延迟，但是临时的（停止即丢数据）。
- **EBS（io2 Block Express）：** 最高 256,000 IOPS。
- **Amazon FSx for Lustre：** 面向 HPC 优化的分布式文件系统，可对接 S3。

### 自动化

- **AWS Batch：** 多节点并行作业、自动调度实例。
- **AWS ParallelCluster：** 部署 HPC 集群的开源工具（自动化 VPC、子网、EFA 的搭建）。

## 5. EC2 实例的高可用

确保一台 EC2 实例（或其服务）保持可用或快速恢复的几种方法。

### 方法 1：CloudWatch Events + Lambda（被动方案）

- **架构：**
    - 活跃实例：绑定一个**弹性 IP**。
    - 备用实例：处于停止状态。
- **流程：** CloudWatch 告警（监控）→ 触发 Lambda → Lambda 启动备用实例并把弹性 IP 挪过去。

### 方法 2：自动扩展组（ASG）+ User Data

- **架构：**
    - ASG 设置：最小 1、最大 1、期望 1。跨 ≥ 2 个 AZ。
    - **弹性 IP** 初始未绑定，或从故障实例上解绑。
- **流程：**
    - 实例故障，ASG 终止它。
    - ASG 在另一个 AZ 启动新实例。
    - 新实例启动时运行 **EC2 User Data** 脚本 → 调用 API **把那个特定的弹性 IP 绑到自己身上**。
    - _要求：_ EC2 实例角色必须允许绑定 IP 的 API 调用。

### 方法 3：ASG + EBS 快照（有状态恢复）

- **场景：** 恢复时需要保留数据（状态）。
- **流程：**
    - **终止时（生命周期钩子）：** 给 EBS 卷做快照。
    - **启动时（生命周期钩子）：** 新实例查找最新快照 → 从快照创建卷 → 把卷挂载上去。


---

# 第 24 章：其他服务（Other Services）

## 1. AWS CloudFormation

- **概念：** 基础设施即代码（IaC）。用 JSON 或 YAML 模板定义基础设施，CloudFormation 负责创建。
- **考试关键场景：**
    - **漂移检测（Drift Detection）：** 用于检测是否有人绕过 CloudFormation 栈手动改了资源（例如改了安全组规则）。
    - **StackSets：** 用一次操作跨_多个账户_和_多个 AWS 区域_创建、更新或删除栈。
    - **DeletionPolicy（删除策略）：** 控制栈被删除时资源的命运。
        - `Retain`：保留资源（适合不想丢失的 S3 桶/数据库）。
        - `Snapshot`：删除前先做快照（适合 RDS/EBS）。
    - **DependsOn：** 控制资源的创建顺序（例如确保 S3 桶先于使用它的 Lambda 函数创建）。

## 2. CloudFormation - 服务角色（Service Role）

- **问题：** 某个用户（例如初级开发者）需要创建一个包含 IAM 角色或 VPC 的栈，但他本人没有直接创建这些资源的权限。
- **解决：** 给 CloudFormation 栈分配一个**服务角色**。
- **工作原理：**
    1. 管理员创建一个拥有强大权限（例如 `AdministratorAccess`）的 IAM 角色（服务角色）。
    2. 管理员授予初级开发者把该角色 _PassRole_ 给 CloudFormation 的权限。
    3. 初级开发者启动栈时选择这个服务角色。
    4. CloudFormation 用**服务角色的权限**（而不是初级开发者本人的权限）来创建资源。
- **考试关键词：** 在允许用户部署经批准的基础设施的同时，对用户本身保持"最小权限"。

## 3. Amazon SES（Simple Email Service）

- **主要用途：** 大规模发送和接收**事务性邮件**。
- **场景：**
    - 应用需要发送密码重置、订单确认或一次性验证码（OTP）。
    - 自己管理名单和内容逻辑的营销邮件。
    - **混合：** 本地应用需要通过 AWS 发邮件（使用 SMTP 接口）。
- **区分点：** 如果题目提到"营销活动"、"用户互动"或"分析"，应该看 Pinpoint。SES 是底层引擎，Pinpoint 是营销大脑。

## 4. Amazon Pinpoint

- **主要用途：** **营销通信**、用户互动与人群细分。
- **关键功能：**
    - **渠道：** 发送邮件、短信、推送通知和语音消息。
    - **细分（Segmentation）：** "给 30 天没打开应用的所有用户发一张优惠券。"
    - **活动与旅程（Campaigns & Journeys）：** 编排多步骤的用户体验。
- **考试提示：** 找关键词"营销活动"、"用户行为分析"、"目标人群"或"推送通知"。

## 5. SSM Session Manager（Systems Manager）

- **概念：** 安全地管理 EC2 实例，无需开放任何入站端口（不需要 SSH/22 端口）。
- **架构：**
    - 基于代理（必须安装并运行 SSM Agent）。
    - 实例需要一个带 `AmazonSSMManagedInstanceCore` 的 IAM 角色。
    - 流量通过 AWS Systems Manager API（HTTPS）传输，而不是 SSH。
- **考试场景：**
    - "如何在没有堡垒机的情况下安全访问私有子网中的实例？" → **Session Manager**。
    - "需要审计谁访问过实例、执行过什么命令？" → **Session Manager**（会话数据可记录到 S3 或 CloudWatch Logs）。

## 6. SSM 其他服务（高概率考点）

- **SSM Parameter Store：**
    - 存储配置数据（数据库连接串、许可证代码）和密钥（密码）。
    - **有免费档**（标准层）。
    - **集成：** 与 CloudFormation、ECS、Lambda 原生集成。
    - _对比 Secrets Manager：_ Parameter Store 更便宜/免费，但没有自动轮换（除非自己写 Lambda）。Secrets Manager 内置轮换。
- **SSM Patch Manager：**
    - 自动为托管实例打补丁（操作系统更新）。
    - 用**补丁基线（Patch Baselines）**定义批准的补丁（例如"只装关键更新"）。
    - 可以在**维护窗口（Maintenance Window）**内为大规模 EC2 机群和本地服务器打补丁。
- **SSM Run Command：**
    - 远程、安全地在一批实例上执行脚本/命令（不需要 SSH）。

## 7. AWS Cost Explorer

- **主要用途：** 可视化和分析**历史**成本与用量数据。
- **功能：**
    - 查看过去 12 个月的成本。
    - 预测未来 12 个月的支出。
    - **规格优化建议（Rightsizing Recommendations）：** 识别利用率低的 EC2 实例（例如"你在用 m5.large，但其实 m5.medium 就够了"）。
- **考试提示：** 题目问"分析支出趋势"或"预测未来成本"，就是 Cost Explorer。

## 8. AWS Cost Anomaly Detection（成本异常检测）

- **主要用途：** 用机器学习**自动监控**、检测异常的支出激增。
- **工作原理：** 它学习你的基线使用模式。当成本意外飙升（例如某个开发者忘了关一台大实例），它会发出告警（SNS 邮件）。
- **区分点：**
    - **Cost Explorer：** 你主动去看数据。
    - **Anomaly Detection：** 出问题时它主动告警。
    - **AWS Budgets：** 你设置静态上限（例如"支出超过 100 美元就提醒我"）。Anomaly Detection 负责发现那些"未知的未知"。

## 9. AWS Outposts

- **概念：** 混合云。在你自己数据中心的**本地环境**运行 AWS 基础设施和服务（EC2、EBS、RDS、EKS）。
- **物理形态：** AWS 送到你现场的一整机架服务器。
- **使用场景：**
    - **低延迟：** 需要与本地机器/系统保持个位数毫秒延迟的应用。
    - **数据驻留：** 出于监管原因数据必须留在特定物理位置（而当地没有 AWS 区域）。
- **对比 Local Zones：** Outposts = 放在你的数据中心。Local Zones = AWS 在特定城市的基础设施（由 AWS 管理，不在你的楼里）。

## 10. AWS Batch

- **主要用途：** 高效运行成百上千个**批处理计算任务**。
- **架构：**
    - 根据任务量动态创建计算资源（例如 EC2 竞价实例）。
    - 使用 Docker 容器。
- **对比 Lambda：**
    - Lambda 有 **15 分钟超时**。
    - 任务超过 15 分钟 → 用 **AWS Batch**。
    - 工作负载有复杂依赖或巨大的磁盘需求 → 用 **AWS Batch**。

## 11. Amazon AppFlow

- **概念：** 全托管的集成服务，在 **SaaS 应用**和 AWS 之间安全传输数据。
- **连接器：** Salesforce、ServiceNow、Slack、Zendesk、Marketo、Google Analytics。
- **目的地：** S3、Redshift、Snowflake。
- **考试场景：** "公司想在 Redshift 中分析 Salesforce 数据，怎样以最少的运维开销传输数据？" → **Amazon AppFlow**。（不用写代码）。

## 12. AWS Amplify

- **目标人群：** 前端 Web 和移动开发者。
- **功能：** 一套构建、部署和托管全栈应用的工具。
- **关键功能：**
    - **Amplify Hosting：** 静态 Web 应用的 CI/CD（连接 Git 仓库）。
    - **Amplify Studio：** 可视化界面搭建后端（认证、数据存储），不需要深厚的云知识。
    - **认证：** 对 Amazon Cognito 的简易封装。

## 13. Instance Scheduler on AWS

- **重要：** 这是一个 **AWS 解决方案**（一个你自行部署的预置 CloudFormation 模板），不是控制台服务列表里的独立服务。
- **功能：** 按计划自动启动和停止 EC2 与 RDS 实例（例如周五晚 6 点停、周一早 8 点启）。
- **架构（考试考的搭配）：**
    - 用 **Amazon EventBridge** 触发一个 **Lambda 函数**。
    - 配置（计划表）存储在 **DynamoDB** 中。
    - 用标签（Tags）标识哪些实例要被调度。
- **好处：** 大幅降低非生产环境（开发/测试）的成本。


---

